# Changelog do devtools

Histórico do que o agente adicionou em cada execução. Rodava 1x/dia até
2026-08-05; a partir de 2026-08-06 roda de hora em hora (`hourly-agent.sh`,
modelo free via opencode) com no máximo 1 item por rodada. Lido por ele
mesmo no início de cada rodada, pra nunca repetir algo que já existe.

<!-- NOVAS ENTRADAS ENTRAM NO TOPO, formato:
## AAAA-MM-DD
- <tipo do item>: <nome> — <rota> (<descrição curta>)
-->

## 2026-08-12
- Front-end: Gerador de Modal/Dialog CSS — `/frontend/css-modal-generator` (monta modais, drawers e bottom sheets funcionais usando só CSS + a pseudo-classe `:target`: escolhe tipo centralizado/drawer esquerda/direita/topo/baixo/bottom sheet, dimensões, padding, border-radius, cor do fundo, cor/opacidade do overlay, sombra, animação de entrada fade/scale/slide, duração, easing e botão de fechar com tamanho/cor/posição; preview ao vivo injeta o CSS exato gerado, então você abre e fecha o modal real clicando no botão, overlay ou ×; gera CSS pronto para copiar, HTML semântico de exemplo com links `href="#id"`/`href="#"`, `role="dialog"`, `aria-modal` e `aria-labelledby`, e bloco completo CSS + HTML; Alert explica o truque do `:target`, o fechamento via hash vazio e as limitações reais — scroll lock, Esc e foco inicial precisam de JS; o motor em `src/utils/cssModalGenerator.js` é 100% client-side — `buildModalCss` monta as regras de :target, overlay e content com transform/opacity, enquanto `buildModalHtml` gera o markup; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`css-tabs-generator`, `css-breadcrumbs-generator`, `css-tooltip-generator`, etc.) como a primeira ferramenta dedicada a modais/dialogs; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Tabs CSS — `/frontend/css-tabs-generator` (monta abas funcionais usando só CSS + radio buttons: orientação horizontal/vertical, 4 estilos visuais — underline, pills, cards e borda —, cores editáveis de texto, fundo, hover, ativo e bordas, padding, gap, border-radius, espessura da borda ativa, padding do conteúdo e duração da transição; preview ao vivo injeta o CSS exato gerado e os inputs radio reais, então você clica nas abas e vê a troca de painel em tempo real; gera CSS pronto para copiar, HTML semântico de exemplo (`<input type="radio">`, `<label for="...">`, `role="tablist"` e `role="tabpanel"`) e bloco completo CSS + HTML; Alert explica o truque do `:checked`, o vínculo `for`/`id` e o limite de acessibilidade para conteúdo estático; o motor em `src/utils/cssTabsGenerator.js` é 100% client-side — `buildTabsCss` monta as regras do container, inputs ocultos, labels, seletores `:checked` e painéis, enquanto `buildTabsHtml` gera o markup com grupo de radio buttons e painéis; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`css-breadcrumbs-generator`, `css-tooltip-generator`, `css-transition-generator`, `css-spinner-generator`) como a primeira ferramenta dedicada a tabs/abas; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Progress Bar CSS — `/frontend/css-progress-bar-generator` (monta barras de progresso e anéis/donuts usando só CSS: modo linear horizontal/vertical com largura, altura, bordas arredondadas, cores de track/preenchimento/borda/texto e animação de stripes; modo circular com diâmetro, espessura do anel, ponta arredondada e preenchimento via `conic-gradient` controlado pela variável `--value`; valor atual e máximo editáveis, label de percentual opcional, duração da transição/animação ajustável e 6 presets de um clique (linear padrão, sucesso, aviso, vertical, circular, donut); preview ao vivo injeta o CSS exato gerado, então você vê o resultado final em tempo real; gera CSS pronto para copiar, HTML semântico de exemplo com atributos `role="progressbar"`/`aria-*` e bloco completo CSS + HTML; Alert explica o truque do `width`/`height` para linear, do `conic-gradient` para circular e a necessidade de atributos ARIA; o motor em `src/utils/cssProgressBarGenerator.js` é 100% client-side — `buildProgressBarCss` monta as regras para linear, vertical e circular, `buildProgressBarHtml` gera o markup acessível e `buildProgressBarFullDemo` junta tudo; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`css-tabs-generator`, `css-breadcrumbs-generator`, `css-tooltip-generator`, `css-transition-generator`, `css-spinner-generator`) como a primeira ferramenta dedicada a progress bars; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Breadcrumbs CSS — `/frontend/css-breadcrumbs-generator` (monta breadcrumbs acessíveis usando só CSS: 9 separadores de um clique — barra `/`, seta `>`, chevron `›`, pipe `|`, bullet `•`, `»`, `//`, til `~` e seta `→` —, direção LTR/RTL, cores editáveis de texto, texto ativo, separador e fundos (normal, hover e ativo), padding, gap, border-radius e opção de sublinhar no hover; 4 presets de um clique — padrão, pills, minimalista e pasta —; preview ao vivo injeta o CSS exato gerado, então você vê o resultado final em tempo real; gera CSS pronto para copiar, HTML semântico de exemplo (`<nav aria-label="breadcrumb">`, `<ol>` e `aria-current="page"`) e bloco completo CSS + HTML; Alert explica o uso de pseudo-elemento `::after` para os separadores (sem poluir o markup), `margin-inline-start` para suporte a RTL e a estrutura acessível correta; o motor em `src/utils/cssBreadcrumbsGenerator.js` é 100% client-side — `buildBreadcrumbsCss` monta a regra a partir das opções, escapa o caractere do separador para o `content` e usa className customizável, `buildBreadcrumbsHtml` gera o markup semântico e `buildBreadcrumbsFullDemo` junta tudo; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`css-tooltip-generator`, `css-transition-generator`, `css-spinner-generator`, `gradient-text-generator`, `custom-properties-generator`, `css-columns-generator`) como a primeira ferramenta dedicada a breadcrumbs; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Ferramenta: Testador de Eventos de Teclado — `/tools/keyboard-event-tester` (captura eventos `KeyboardEvent` em tempo real no navegador: mostra `key`, `code`, `keyCode`/`which`, `location`, `repeat`, `composed` e o estado dos modificadores Ctrl/Alt/Shift/Meta; área de captura clicável, campo de input para testar em contexto real, histórico das últimas 20 teclas pressionadas e snippet de handler `keydown` pronto para copiar; Alert explica a diferença crucial entre `event.key` (caractere gerado, muda com o layout do teclado) e `event.code` (posição física da tecla, não muda com o layout); o motor em `src/utils/keyboardEventTester.js` é 100% client-side — `formatEvent` normaliza os campos do evento, `getLocationLabel` traduz a localização, `escapeKey` escapa caracteres especiais para o snippet e `buildEventSnippet` monta o listener de exemplo; algoritmo-fonte exibido num Collapse; complementa o snippet `useKeyPress` como a primeira ferramenta interativa do projeto dedicada a inspecionar eventos de teclado; página bilíngue PT/EN e registrada no menu de Ferramentas; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Seletores CSS — `/references/css-selectors-cheatsheet` (referência pesquisável dos seletores CSS — 50 entradas em 8 categorias: básicos (`*`, `element`, `.class`, `#id`, agrupamento por vírgula), combinadores (descendente, filho direto, irmão adjacente, irmão geral), atributos (`[attr]`, `[attr=value]`, `[attr~=value]`, `[attr|=value]`, `[attr^=value]`, `[attr$=value]`, `[attr*=value]`, case-insensitive), pseudo-classes estruturais (`:first-child`, `:last-child`, `:nth-child`, `:nth-last-child`, `:only-child`, `:first-of-type`, `:last-of-type`, `:nth-of-type`, `:only-of-type`, `:empty`), pseudo-classes de estado (`:hover`, `:focus`, `:focus-visible`, `:active`, `:checked`, `:disabled`, `:enabled`, `:valid`, `:invalid`, `:required`, `:optional`, `:read-only`, `:placeholder-shown`), pseudo-classes de navegação (`:link`, `:visited`, `:target`, `:root`, `:not()`, `:is()`, `:where()`, `:has()`), pseudo-elementos (`::before`, `::after`, `::first-line`, `::first-letter`, `::selection`, `::placeholder`, `::marker`, `::backdrop`) e avançados (escaping de caracteres especiais, `:scope`, combinador de coluna `||`, `::part()` e `::slotted()` de Shadow DOM); busca por seletor/conceito/descrição, filtro por categoria em botões de rádio com contagem, botão de copiar cada snippet e copiar a lista filtrada como Markdown; Alert explica o impacto da especificidade e a diferença entre `:is()` e `:where()`; complementa o `/tools/css-selector-tester` (testa correspondência) e o `/tools/css-specificity-calculator` (calcula especificidade) como a primeira referência exclusivamente dedicada a seletores CSS; página bilíngue PT/EN e registrada no menu de Referências; 100% client-side, nada sai do navegador)
- Front-end: Gerador de CSS Columns (Multicol) — `/frontend/css-columns-generator` (monta layouts multi-coluna com as propriedades `column-*`: escolhe `column-count` (1–6 ou automático), `column-width` mínima, `column-gap`, `column-rule` com espessura/estilo/cor, `column-span` para títulos que atravessam todas as colunas, `column-fill` (balance/auto) e `break-inside` para evitar quebras internas; 4 presets de um clique — revista, jornal, galeria responsiva e duas colunas —; preview ao vivo mostrando texto fluindo entre colunas conforme as regras geradas; gera CSS pronto para copiar e bloco completo CSS + HTML de exemplo; Alert explica a interação entre `column-count` e `column-width`, o uso de `column-span: all` e o cuidado com `break-inside: avoid` criando espaços vazios; o motor em `src/utils/cssColumnsGenerator.js` é 100% client-side — `buildColumnsCss` monta as regras do container e dos filhos, `buildContainerStyle`/`buildChildStyle` devolvem objetos de estilo para o preview e `buildFullDemo` junta tudo; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`grid-areas-generator`, `container-query-generator`, `scroll-snap-generator`, `clamp-generator`) como a primeira ferramenta dedicada a layouts multi-coluna; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de CSS Custom Properties — `/frontend/custom-properties-generator` (monta um sistema de design tokens em CSS puro: 9 cores semânticas editáveis via ColorPicker — primária, secundária, sucesso, aviso, erro, info, fundo, superfície e texto — com variantes light/dark geradas automaticamente no espaço HSL; prefixo customizável para as variáveis; escala de espaçamento com unidade base e número de passos ajustáveis; bordas arredondadas sm/md/lg; sombras sm/md/lg com cor própria; tipografia em 5 tamanhos; 4 presets de um clique — padrão, escuro, pastel e floresta —; preview ao vivo injetando o :root gerado na página, mostrando cards, botões e badges usando os tokens reais; gera o bloco `:root` pronto para copiar e um snippet HTML completo de demonstração; Alert explica o uso do prefixo, o funcionamento do ajuste HSL e o poder de mudar um token só; o motor em `src/utils/customPropertiesGenerator.js` é 100% client-side — `buildCustomPropertiesCss` monta o bloco de tokens, `buildColorScale` deriva light/dark via `adjustHexLightness` e `hexToRgb`/`rgbToHsl`/`hslToRgb`/`rgbToHex` fazem a conversão de cores; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`gradient-generator`, `box-shadow-generator`, `text-shadow-generator`, `css-filter-generator`, `container-query-generator`, `scroll-snap-generator`, `css-transition-generator`, `css-tooltip-generator`, `gradient-text-generator`) como a primeira ferramenta dedicada a design tokens / CSS custom properties; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Texto Gradiente CSS — `/frontend/gradient-text-generator` (cria títulos com gradiente usando `background-clip: text`: escolhe entre gradiente linear (com ângulo) ou radial, adiciona/remove stops de cor com posicionamento em %, ajusta tamanho da fonte (16–120px), peso (300–900), alinhamento e cor de fallback; 8 presets de um clique — pôr do sol, oceano, floresta, frutas vermelhas, meia-noite, cítrico, neon e fogo; preview ao vivo aplica exatamente o CSS gerado no texto editável; gera a classe CSS pronta com as regras `-webkit-background-clip: text`, `background-clip: text`, `-webkit-text-fill-color: transparent` e `display: inline-block`; Alert explica o truque do efeito, a necessidade de cor de fallback e o cuidado de não usar `display: inline`; o motor em `src/utils/gradientTextGenerator.js` é 100% client-side — `buildGradientTextCss` ordena os stops, monta o gradiente e embrulha as regras de clip em uma classe, enquanto `buildPreviewStyle` devolve o objeto de estilo para o preview; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`gradient-generator`, `box-shadow-generator`, `text-shadow-generator`, `css-filter-generator`) como a primeira ferramenta dedicada a texto com gradiente; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Tooltip CSS — `/frontend/css-tooltip-generator` (monta tooltips acessíveis usando só CSS: escolhe posição — `top`, `bottom`, `left`, `right` — e alinhamento `start`/`center`/`end`, cores de fundo e texto, padding, border-radius, tamanho de fonte, largura máxima, seta opcional com tamanho ajustável, distância do alvo, animação de entrada (`fade`, `scale`, `slide`) com duração/delay, e sombra opcional; 4 presets de um clique — dark, light, slide azul e minimal; preview ao vivo injeta o CSS exato gerado e mostra um botão e um texto com tooltip real; gera o CSS da classe, o HTML de exemplo e um bloco completo pronto para copiar; Alert explica o uso de `position: relative` no wrapper, o truque das bordas transparentes na seta e o cuidado com tooltips cortadas pela viewport; o motor em `src/utils/cssTooltipGenerator.js` é 100% client-side — `buildTooltipCss` monta as regras do wrapper, do tooltip absoluto e do pseudo-elemento `::after`, calcula posicionamento a partir de `position` + `align`, monta as transformações de entrada/saída conforme a animação e emite CSS pronto para colar, enquanto `buildTooltipHtml` gera o markup e `buildFullDemo` junta tudo; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`css-transition-generator`, `scroll-snap-generator`, `transform-generator`, `text-shadow-generator`) como a primeira ferramenta dedicada a tooltips; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de CSS Transition — `/frontend/css-transition-generator` (monta a declaração `transition` do CSS de forma visual: escolhe as propriedades a animar (ou `all`), duração em ms, função de timing (`ease`, `linear`, `ease-in/out`, `ease-in-out`, `step-start`, `step-end`, `cubic-bezier` e `steps`), delay e `will-change`; 5 presets de um clique — suave, rápida, elástica, stepped e fade lento —; preview ao vivo com caixa que troca de estado ao clicar num botão, mostrando a transição exata que será copiada; gera tanto o shorthand `transition` quanto as propriedades longas (`transition-property`, `transition-duration`, `transition-timing-function`, `transition-delay`), além de um bloco completo CSS + HTML de exemplo; Alert explica a ordem do shorthand, o uso moderado de `will-change` e a regra de que a propriedade animada precisa ser a mesma que muda no estado-alvo; o motor em `src/utils/cssTransitionGenerator.js` é 100% client-side — `buildTransitionCss` monta o shorthand e a versão expandida, `buildTimingValue` formata `cubic-bezier` e `steps`, `buildPreviewStyle` devolve o objeto de estilo para o preview e `buildFullDemo` junta CSS + HTML; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`scroll-snap-generator`, `transform-generator`, `keyframe-generator`, `clamp-generator`) como a primeira ferramenta dedicada a `transition`; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Scroll Snap CSS — `/frontend/scroll-snap-generator` (monta carrosséis, galerias e listas paginadas usando só CSS: escolhe o eixo (`x`, `y`, `inline`, `block`), a rigidez do snap (`mandatory`/`proximity`), o alinhamento dos itens (`start`/`center`/`end`), a parada (`normal`/`always`), `scroll-padding`, `scroll-margin`, dimensões dos itens, gap, suavização e barra de rolagem oculta; 4 presets de um clique (carrossel horizontal, galeria vertical, paginação full-width, lista vertical); preview ao vivo mostra o snap funcionando conforme as opções, com tags resumindo as regras ativas; gera CSS, HTML de exemplo e bloco completo pronto para copiar; Alert explica a diferença entre `mandatory` e `proximity`, o uso de `scroll-padding` no container vs `scroll-margin` nos itens, e o cuidado ao esconder a barra de rolagem; o motor em `src/utils/scrollSnapGenerator.js` é 100% client-side — `buildScrollSnapCss` monta as regras do container e dos itens, `buildScrollSnapHtml` gera o markup e `buildFullDemo` junta tudo com estilo visual mínimo; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`container-query-generator`, `clamp-generator`, `transform-generator`) como a primeira ferramenta dedicada a `scroll-snap`; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- DevOps: Gerador de Makefile — `/devops/makefile-generator` (monta um Makefile a partir de variáveis e targets editáveis: 6 presets de um clique — Genérico, Node.js, Python, Go, Rust e Docker — com variáveis, dependências, comentários e flags `.PHONY` pré-preenchidos; controle de indentação dos comandos (tab real ou 2/4 espaços), inclusão automática da diretiva `.PHONY`, ordenação alfabética dos targets e cabeçalho livre; o motor em `src/utils/makefileGenerator.js` é 100% client-side — `buildMakefile` renderiza variáveis, targets, `.PHONY` e gera o texto final respeitando o caractere de indentação escolhido; preview ao vivo com estatísticas de linhas/bytes e botões de copiar/baixar `Makefile`; Alert explica as pegadinhas reais do Makefile — o tab obrigatório no início dos comandos, `.PHONY` evitando conflitos com arquivos de mesmo nome, expansão de variáveis com `$(NOME)` e supressão de eco com `@`; algoritmo-fonte exibido num Collapse; complementa os geradores de config do projeto (`gitignore-generator`, `editorconfig-generator`, `license-generator`, `dockerignore-generator`, `prettierrc-generator`) como a primeira ferramenta dedicada a Makefile; página bilíngue PT/EN e registrada no menu de DevOps; 100% client-side, nada sai do navegador)
- DevOps: Gerador de .prettierrc — `/devops/prettierrc-generator` (monta o arquivo `.prettierrc` (JSON) do Prettier com as opções mais usadas: `printWidth`, `tabWidth`, `useTabs`, `semi`, `singleQuote`, `jsxSingleQuote`, `trailingComma`, `bracketSpacing`, `bracketSameLine`, `arrowParens`, `endOfLine`, `quoteProps`, `proseWrap`, `htmlWhitespaceSensitivity`, `embeddedLanguageFormatting`, `singleAttributePerLine` e `$schema` opcional; 6 presets de um clique — Padrão Prettier, Estrito, Mínimo, Node.js, React/Vite e Com Tabs —, preview ao vivo do JSON gerado com estatísticas de linhas/bytes e botões de copiar/baixar `.prettierrc`; Alert explica os caminhos válidos do arquivo (`.prettierrc`, `.prettierrc.json` ou chave `prettier` no `package.json`), a diferença entre `printWidth` flexível e limite rígido, o `trailingComma: 'all'` para diffs menores e o `useTabs` como escolha de acessibilidade; o motor em `src/utils/prettierrcGenerator.js` é 100% client-side — `buildPrettierrc` monta o objeto a partir das opções e emite `JSON.stringify(out, null, 2)`; algoritmo-fonte exibido num Collapse; complementa os geradores de config do projeto (`gitignore-generator`, `editorconfig-generator`, `license-generator`, `dockerignore-generator`) como a primeira ferramenta dedicada a `.prettierrc`; página bilíngue PT/EN e registrada no menu de DevOps; 100% client-side, nada sai do navegador)
- DevOps: Gerador de .dockerignore — `/devops/dockerignore-generator` (monta o arquivo `.dockerignore` do projeto combinando presets por linguagem com regras customizadas: 8 presets — Genérico, Node.js, Python, Go, Java/Maven/Gradle, Ruby, PHP/Composer e Rust — cobrindo dependências, build/cache, logs, variáveis de ambiente, IDEs, arquivos de SO e documentação; checkbox permite selecionar múltiplos presets e o motor em `src/utils/dockerignoreGenerator.js` é 100% client-side — `buildDockerignore` une os blocos, remove padrões duplicados, preserva comentários e negações (`!`) e deduplica linhas em branco; textarea para padrões customizados; preview ao vivo com estatísticas de linhas/bytes e botões de copiar/baixar `.dockerignore`; Alert explica o funcionamento como `.gitignore` para o contexto do Docker, a importância de ignorar `node_modules/` e ambientes virtuais, e a pegadinha das negações com diretórios já excluídos; algoritmo-fonte exibido num Collapse; complementa os geradores de config do projeto (`gitignore-generator`, `editorconfig-generator`, `license-generator`) como a primeira ferramenta dedicada a `.dockerignore`; página bilíngue PT/EN e registrada no menu de DevOps; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Container Queries CSS — `/frontend/container-query-generator` (monta regras de `@container` visualmente para responsividade baseada no elemento-pai, não na viewport: define o nome do container, escolhe o `container-type` (`inline-size`, `size` ou `normal`) e cria breakpoints editáveis com estilos por faixa — cor de fundo, cor do texto, direção flex, tamanho de fonte, padding, gap e border-radius; preview ao vivo com container redimensionável mostra qual breakpoint está ativo em tempo real; CSS gerado inclui a regra do container, a classe base do item e os blocos `@container` com condições `min-width`/`max-width`; HTML de exemplo pronto para copiar; o motor em `src/utils/containerQueryGenerator.js` é 100% client-side — `buildContainerCss` monta a regra do container e os blocos de query, `buildBreakpointQuery` formata cada faixa e estilos, e `buildDemoHtml` entrega o esqueleto HTML; Alert explica a diferença entre `inline-size`/`size`/`normal`, a necessidade de dimensão definida no container e o suporte em navegadores modernos; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS do projeto (`gradient-generator`, `box-shadow-generator`, `css-filter-generator`, `clip-path-generator`, `border-radius-generator`, `text-shadow-generator`, `transform-generator`) como a primeira ferramenta dedicada a container queries; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- DevOps: Gerador de LICENSE — `/devops/license-generator` (monta o arquivo LICENSE do repositório a partir dos templates mais usados: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, MPL-2.0, CC0-1.0, Unlicense, WTFPL e Proprietária; campos de ano, titular e nome do projeto preenchem os placeholders {{year}}, {{holder}} e {{project}}; preview ao vivo mostra o texto gerado, com estatísticas de linhas/bytes e botões de copiar/baixar `LICENSE`; Alert deixa claro que os textos são de referência e não substituem aconselhamento jurídico, além de indicar o texto oficial completo para licenças copyleft fortes; o motor em `src/utils/licenseGenerator.js` é 100% client-side — `buildLicense` seleciona o template e faz o replace dos placeholders; algoritmo-fonte exibido num Collapse; primeira ferramenta do projeto dedicada a gerar arquivos de licença de software; página bilíngue PT/EN e registrada no menu de DevOps; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Transform CSS — `/frontend/transform-generator` (monta transformações 2D e 3D interativamente: translate X/Y/Z, rotate X/Y/Z, scale X/Y, skew X/Y, perspectiva, origem e transform-style; preview ao vivo mostra a caixa exatamente com o CSS gerado, com efeito de hover demonstrativo; 9 presets de um clique — nenhum, rotação X/Y, isométrico, virar cartão, perspectiva, inclinar, escalar e hover 3D; o motor em `src/utils/transformGenerator.js` é 100% client-side — `buildTransform` emite só as funções com valores diferentes do padrão na ordem correta e `buildClassCss` embrulha tudo numa classe pronta com `perspective`, `transform-origin` e `transform-style`; Alert explica a importância da ordem das funções, o uso de `perspective` no pai e `preserve-3d` para profundidade real; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS (`box-shadow-generator`, `gradient-generator`, `css-filter-generator`, `clip-path-generator`, `border-radius-generator`, `text-shadow-generator`) como a primeira ferramenta do projeto dedicada a `transform`; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Text Shadow — `/frontend/text-shadow-generator` (monta sombras em texto com uma ou várias camadas: deslocamento X/Y, desfoque, cor e opacidade por camada; preview ao vivo com texto e tamanho editáveis, fundo escuro para destacar brilhos/neons; 6 presets de um clique — suave, brilho, neon, retrô, profundidade e contorno; o motor em `src/utils/textShadowGenerator.js` é 100% client-side — `hexToRgba` converte hex + opacidade para rgba, `buildLayerCss` formata cada camada, `buildTextShadow` une tudo com vírgulas e `buildClassCss` embrulha numa classe pronta; Alert explica a diferença crucial pro `box-shadow` (`text-shadow` não aceita `inset` nem `spread`) e o truque do contorno com `blur: 0`; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS (`box-shadow-generator`, `gradient-generator`, `css-filter-generator`, `clip-path-generator`, `border-radius-generator`) como a primeira ferramenta do projeto dedicada a `text-shadow`; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Ferramenta: Calculadora de Data e Hora — `/tools/date-time-calculator` (calcula a diferença entre duas datas em visão calendárica e total, e adiciona/subtrai intervalos de uma data: o núcleo em `src/utils/dateTimeCalculator.js` é 100% client-side — `diffCalendar` faz a quebra anos → meses → dias → horas → minutos → segundos respeitando meses de tamanhos diferentes, `diffTotals` devolve a duração bruta em ms/seg/min/horas/dias/semanas, e `addDuration` usa `dayjs.add`; a página tem dois modos via Segmented (diferença entre datas e adicionar/subtrair), DatePickers com hora, presets de um clique (agora, amanhã, próxima semana/mês/ano) e presets de +/- 1h/1d/7d/30d no modo adicionar, botão de trocar datas e cópia do resultado; Alert explica a diferença entre visão calendárica e total; algoritmo-fonte exibido num Collapse; complementa o `timestamp-converter` (converte instantes) e o `days-until` (conta até uma data) como a primeira ferramenta do projeto para operações entre duas datas e intervalos; página bilíngue PT/EN e registrada no menu de Ferramentas; 100% client-side, nada sai do navegador)

## 2026-08-11
- Front-end: Simulador de Daltonismo — `/frontend/color-blindness-simulator` (simula como uma cor é percebida pelos principais tipos de deficiência cromática — protanopia, deuteranopia, tritanopia e acromatopsia — usando matrizes de conversão no espaço de cor linear; o motor em `src/utils/colorBlindness.js` é 100% client-side: `simulate` faz gamma expansion do sRGB, aplica a matriz 3×3 correspondente ou a luminância relativa para monocromacia, e comprime o gamma de volta para sRGB; a página tem input de cor tipo `color` + campo hex, 6 presets de cores problemáticas, cards lado a lado mostrando a cor original e as quatro simulações com hex e botão de copiar, Alert explicando limitações do modelo, e o algoritmo-fonte exibido num Collapse; complementa o `/frontend/contrast-checker` (que mede contraste, mas não simula CVD) e é a primeira ferramenta do projeto dedicada a acessibilidade cromática; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de @font-face CSS — `/frontend/font-face-generator` (monta o bloco CSS `@font-face` para carregar fontes customizadas no navegador: família, stack de fallback (sans/serif/mono/system/cursive/fantasy/nenhum), `font-display` (auto/block/swap/fallback/optional), pesos 100–900 e estilos normal/italic, caminho base opcional, `unicode-range`, arquivos por peso nos formatos woff2/woff/ttf/eot, classe utilitária com o fallback stack, e `<link rel="preconnect">` opcional; o motor em `src/utils/fontFaceGenerator.js` é 100% client-side — `buildSingleFace` ordena as fontes corretamente (eot primeiro para IE legado, depois woff2/woff/ttf/otf), aplica `font-display` e `unicode-range`, e `buildFamilyFallback` monta a stack; preview ao vivo com texto editável, peso e estilo aplicados via `font-family` real; 3 presets de um clique (mínimo, completo e exemplo de CDN/Google Fonts); botão de copiar o CSS completo; Alert explica as boas práticas (woff2 primeiro, font-display swap, caminhos absolutos, unicode-range); algoritmo-fonte exibido num Collapse; complementa a família de geradores de configuração front-end (`meta-tags-generator`, `sitemap-generator`, `web-app-manifest`) — é a primeira ferramenta do projeto dedicada a `@font-face`; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Loading Spinner CSS — `/frontend/css-spinner-generator` (monta loaders/spinners usando só CSS, sem imagens, SVG ou bibliotecas: 6 tipos — borda girando, anéis duplos, pulso, pontos saltitantes, barras girando e rolo contínuo — com controles de tamanho (8–120px), espessura da borda, duração da animação, quantidade de elementos (dots/bars) e duas cores (principal e secundária); o motor em `src/utils/cssSpinnerGenerator.js` é 100% client-side — `buildSpinnerCss` monta a classe `.loading-spinner` com keyframes únicos por tipo, `parseColor` normaliza as cores do ColorPicker e `alphaColor` deriva a faixa transparente a partir de hex; preview ao vivo injeta o CSS exato gerado num `<style>` local, então o que se vê é o que se copia; presets de um clique por tipo; botão de copiar o CSS completo; Alert explica as boas práticas de performance (animar só `transform`/`opacity`) e o cuidado com sub-pixel em telas Retina; algoritmo-fonte exibido num Collapse; complementa os exemplos de estilo `/styles/bouncing-dots-loader` e é a primeira ferramenta parametrizada do projeto dedicada a spinners/loaders CSS; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Web App Manifest — `/frontend/web-app-manifest` (monta o arquivo `manifest.json` (ou `.webmanifest`) de uma PWA a partir de campos editáveis: `name`, `short_name`, `description`, `start_url`, `scope`, `id`, `display` (fullscreen/standalone/minimal-ui/browser), `orientation`, `theme_color`, `background_color`, `lang` e `dir`; gerencia arrays de ícones (`src`, `sizes`, `type`, `purpose` — incluindo `maskable`), screenshots (`src`, `sizes`, `type`, `form_factor`, `label`) e shortcuts (`name`, `short_name`, `description`, `url`); seletor de categorias da spec W3C e toggle `prefer_related_applications`; 3 presets de um clique (PWA mínimo, Dashboard/App, Blog/Conteúdo); validação ao vivo dos campos obrigatórios e srcs vazios; output com pretty print/minificado, botões de copiar e baixar `.webmanifest`; o motor em `src/utils/webAppManifest.js` é 100% client-side — `buildManifest` omite campos vazios e emite JSON válido, `validateManifest` aponta campos obrigatórios e arrays incompletos; algoritmo-fonte exibido num Collapse; complementa o `/frontend/meta-tags-generator` e o `/frontend/sitemap-generator` como mais uma peça de configuração de site/app web; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Sitemap XML — `/frontend/sitemap-generator` (monta um arquivo `sitemap.xml` válido no protocolo Sitemaps.org a partir de uma lista editável de URLs: cada entrada tem `<loc>`, `<lastmod>`, `<changefreq>` e `<priority>`, com host base que prefixa URLs relativas automaticamente; validação ao vivo de URL absoluta, formato ISO de data, frequência permitida (always/hourly/daily/weekly/monthly/yearly/never) e prioridade 0.0–1.0; detecta URLs duplicadas e destaca linhas com erro; opções de pretty print ou minificado, presets de um clique (landing, blog, e-commerce), botões de copiar e baixar `.xml`; o motor em `src/utils/sitemapGenerator.js` é 100% client-side — `buildSitemapXml` monta a string XML escapando tudo com `escapeXml`, `validateEntry` aponta problemas por campo e `buildWarnings` encontra locais repetidos; algoritmo-fonte exibido num Collapse; complementa o `/frontend/meta-tags-generator` e o `/security/robots-txt-generator`, fechando o trio de SEO/robots/sitemap do projeto; página bilíngue PT/EN e registrada no menu de Front-end; 100% client-side, nada sai do navegador)
- DevOps: Gerador de .editorconfig — `/devops/editorconfig-generator` (monta um arquivo `.editorconfig` válido para padronizar indentação, quebras de linha, charset e espaços em branco entre editores e IDEs: opções de `root`, `indent_style` (space/tab), `indent_size`, `tab_width`, `end_of_line` (lf/crlf/cr), `charset`, `trim_trailing_whitespace`, `insert_final_newline` e `max_line_length`; 6 presets de um clique (Genérico, Node.js, Python, Web, Go, Rust); seções customizáveis por padrão glob para sobrescrever regras em arquivos específicos; botões de copiar e baixar o arquivo; o motor em `src/utils/editorconfigGenerator.js` é 100% client-side — `buildEditorConfig` monta a string linha a linha e `PRESETS` centraliza os modelos; algoritmo-fonte exibido num Collapse; complementa os geradores de config do projeto (`gitignore-generator`, `dockerfile-generator`, `caddyfile-generator`, `docker-compose-generator`) — é o primeiro item do projeto dedicado a `.editorconfig`; página bilíngue PT/EN e registrada no menu de DevOps; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Padrões de Fundo CSS — `/frontend/pattern-background-generator` (cria texturas de fundo repetíveis usando apenas gradientes CSS: 11 padrões — listras horizontais, verticais e diagonais, crosshatch, dots, polka dots, checkerboard, grid, graph paper, diamonds e chevron — com controle de duas cores, tamanho da repetição, ângulo (para listras diagonais) e opacidade; o motor em `src/utils/cssPatternBackground.js` gera `background-color`, `background-image`, `background-size` e `background-position` conforme o padrão escolhido; preview ao vivo mostra a textura exata que será copiada, 6 presets de um clique e botão de copiar a classe CSS completa; Alert explica o princípio dos gradientes repetidos e a pegadinha de custo GPU; algoritmo-fonte exibido num Collapse; complementa a família de geradores de CSS (`gradient`, `box-shadow`, `border-radius`, `filter`, `clip-path`, `keyframe`, `scrollbar`, `css-triangle`, `grid-areas`, `clamp`) — é a primeira ferramenta do projeto dedicada a padrões de fundo CSS; 100% client-side, nada sai do navegador)
- Ferramenta: Calculadora de Proporção (Aspect Ratio) — `/tools/aspect-ratio-calculator` (simplifica proporções e calcula dimensões proporcionais para imagens, vídeos e containers responsivos: o núcleo em `src/utils/aspectRatio.js` é 100% client-side — `gcd` pelo algoritmo de Euclides, `simplifyRatio` reduz `1920×1080` para `16:9`, `ratioDecimal` devolve o valor decimal, `fitDimension` calcula largura ou altura faltante mantendo a proporção, e `buildAspectRatioCss` gera o snippet com `aspect-ratio` moderno e o fallback `padding-bottom` para suporte antigo; a página tem modo Simplificar/Calcular dimensão, campos de largura/altura com botão de troca, 12 presets comuns — 1:1, 4:3, 3:2, 16:9, 16:10, 21:9, 9:16, 2.35:1, 2.39:1, 5:4, 3:4, 32:9 —, exibição da proporção simplificada, valor decimal e dimensões proporcionais arredondadas, além de botão para copiar o CSS gerado; Alert explica a pegadinha real — `aspect-ratio` é ignorada quando `width` e `height` são fixados simultaneamente — e o algoritmo-fonte é exibido num Collapse; complementa o `css-unit-converter` e é a primeira ferramenta do projeto dedicada a proporção de tela/imagem; 100% client-side, nada sai do navegador)
- Ferramenta: Conversor XML ↔ JSON — `/tools/xml-json-converter` (converte XML em JSON e vice-versa, 100% client-side: `xmlToJson` usa `DOMParser` para ler a árvore e caminha recursivamente coletando atributos com prefixo configurável (padrão `"@"`), agrupando filhos por tag — repetidos viram array — e guardando texto solto sob uma chave configurável (padrão `"#text"`); `jsonToXml` faz o caminho inverso, transformando chaves com `@` em atributos, `#text` em conteúdo textual, arrays em tags repetidas e objetos aninhados em elementos filhos; a página tem exemplos de um clique nos dois sentidos, opções de indentação, declaração XML, prefixo/chave de texto e modo "sempre array", além de Alert com as pegadinhas reais da conversão aproximada — namespaces, tipos de XML sempre string e ordem de atributos; o algoritmo-fonte em `src/utils/xmlJson.js` é exibido num Collapse; complementa os conversores `json-to-yaml` e `json-to-typescript` e é a primeira ferramenta do projeto para XML ↔ JSON)
- Referências: Cheat Sheet de Tailwind CSS — `/references/tailwind-cheatsheet` (referência pesquisável das classes utilitárias do Tailwind CSS — o framework CSS que virou padrão em muitos projetos React/Vite: 81 entradas em 14 categorias — layout & posicionamento (container, display, position, inset-0, z-index, overflow, object-fit), flexbox (flex, flex-1/flex-none, direção, alinhamento, gap, order, grow/shrink/basis), grid (grid-cols, col-span, row-span, place-items), spacing (p/m px/py/mx-auto, margens negativas, space-x/y), sizing (w-full, h-screen, min/max), typography (text-* , font-*, leading, tracking, truncate, line-clamp), backgrounds (bg-*, gradientes, bg-cover/center), borders (border-*, rounded-*, ring), effects (shadow, opacity, mix-blend, backdrop-blur), transitions & animations (transition, duration, ease, animate-*), transforms (scale, rotate, translate), interactivity & a11y (cursor, pointer-events, select-none, sr-only), tables (table-auto/fixed, border-collapse) e SVG (fill-current, stroke-current); busca por classe/conceito/descrição, filtro por categoria em botões de rádio com contagem, botão de copiar cada snippet e copiar a lista filtrada como Markdown; Alert explica o design system embutido de cores/espaçamento/breakpoints e o uso de prefixos hover:/focus:/md:; complementa os geradores de CSS do projeto e os cheat sheets de Flexbox/CSS Grid — este é o primeiro catálogo rápido de classes Tailwind no devtools; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Grid Areas CSS — `/frontend/grid-areas-generator` (desenhe visualmente áreas nomeadas de um grid CSS e copie o `grid-template-areas` pronto: matriz editável clicando ou arrastando, presets Holy Grail/sidebar/duas colunas/galeria/dashboard, controle de linhas/colunas/gap, células vazias como buracos `.`, validação de retângulos sólidos, cores automáticas por área, renomear/remover áreas; gera CSS completo com `display: grid`, `grid-template-columns/rows`, `gap`, `grid-template-areas` e classes `.area-X`, além do HTML esqueleto e snippet completo; algoritmo-fonte em `src/utils/gridAreas.js` exibido na página; complementa o `/references/css-grid-cheatsheet` (que documenta as propriedades) e é a primeira ferramenta visual do projeto para montar `grid-template-areas`; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Triângulo CSS — `/frontend/css-triangle-generator` (cria triângulos só com CSS via clássico truque das bordas transparentes: o usuário escolhe direção (cima/baixo/esquerda/direita), tipo isósceles ou equilátero — neste último a altura é calculada automaticamente como `base × √3 / 2` —, base, altura e cor; o núcleo em `src/utils/cssTriangle.js` é 100% client-side, com `buildTriangle` mapeando a direção para as quatro bordas e `buildTriangleClass` embrulhando a regra numa classe pronta para colar; preview ao vivo mostra o triângulo exato gerado, com cor de fundo ajustável; presets de um clique para cada direção e para equilátero; copy do CSS gerado, Alert explicando o truque e as pegadinhas de meio-pixel em telas Retina, e o algoritmo-fonte exibido num Collapse; página bilíngue PT/EN, registrada no menu de Front-end; complementa a família de geradores de CSS e é a primeira ferramenta do projeto dedicada a triângulos CSS)
- Ferramenta: JSON Flatten & Unflatten — `/data/json-flatten` (transforma JSON aninhado num objeto "achatado" de caminhos `a.b.0.c` e reconstrói a árvore no caminho inverso; o motor em `src/utils/jsonFlatten.js` é 100% client-side: flatten faz depth-first walk, achata arrays em índices numéricos, preserva objetos/arrays vazios e escapa chaves que contêm o delimitador com `\`; unflatten reconstrói a árvore criando arrays automaticamente quando o próximo segmento é número; a página tem modo flatten/unflatten via Segmented, exemplos de um clique (usuário aninhado, lista de objetos, casos especiais com chaves contendo ponto), delimitador customizável, tabela de caminhos/valor/tipo no flatten, estatísticas de caminhos/arrays/objetos/folhas, cópia do JSON resultante, Alert sobre arrays e chaves com delimitador e o algoritmo-fonte exibido num Collapse; complementa o `json-formatter`, `json-path-explorer` e `json-to-yaml` — é a primeira ferramenta do projeto dedicada a achatar/reconstruir estruturas JSON)
- Ferramenta: Testador de Seletores CSS — `/tools/css-selector-tester` (digita um seletor CSS e vê, em tempo real, quais elementos de um trecho de HTML correspondem: preview renderizado num iframe sandbox `allow-same-origin` — scripts do usuário não executam —, destaque vermelho nos matches, tabela com tag, id, classes, texto e caminho simplificado; exemplos de um clique (seletores comuns, pseudo-classes, atributos e combinadores), cópia do seletor, Alert com as pegadinhas de `querySelectorAll` (pseudo-elementos e estados `:hover`/`:focus` não casam, escaping de caracteres especiais, sandbox) e o algoritmo-fonte exibido num Collapse; complementa o `/tools/css-specificity-calculator` (calcula especificidade, mas não testa correspondência) e o `/tools/regex-tester` (lógica parecida, mas para regex) — é a primeira ferramenta do projeto dedicada a testar seletores CSS contra HTML; 100% client-side, nada sai do navegador)
- Ferramenta: URL Parser & Query Editor — `/tools/url-parser` (cola uma URL absoluta e vê cada componente separado — protocolo, usuário, senha, host, hostname, porta, caminho, query string, hash — com query params editáveis em tabela: adicionar, remover e editar chave/valor reconstrói a URL ao vivo; exemplos de um clique (URL simples, com autenticação e mailto), cópia da URL reconstruída, Alert com as pegadinhas reais de URL e o algoritmo-fonte de parse/rebuild exibido num Collapse; complementa o `/tools/url-encoder`, que só escapa texto, não decompõe a URL nem edita query params — é a primeira ferramenta do projeto de análise/manipulação de URL completa; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Elementos HTML — `/references/html-cheatsheet` (cheat sheet pesquisável da linguagem de marcação em que o próprio projeto é construído — o primeiro item do projeto que cataloga os ELEMENTOS HTML em si, fechando o gap do `markdown-syntax` (que é Markdown, não HTML) e da família de formatadores/conversores `html-formatter`/`html-to-jsx`/`html-to-markdown`/`html-entity-encoder` (que processam HTML mas não documentam as tags): 101 entradas em 8 categorias — documento & metadados (`html` a raiz com o `lang`, `head`, `body`, `title` da aba, `meta` (charset/viewport/OG), `link` (CSS/favicon/preload), `base` de URLs relativas, `style`, `script` com `async/defer`, `noscript` de fallback), seções & estrutura (a família semântica `header`/`nav`/`main` — exatamente UM `main` por página —/`section`/`article`/`aside`/`footer`, headings `h1–h6` com a regra de um h1 único por página, `address`), agrupamento de conteúdo (`div` o contêiner sem semântica própria, `p`, `hr`, `pre` com whitespace preservado, `blockquote`, `ol` com `start`/`reversed`/`ul`/`li`, a lista de descrição `dl`/`dt`/`dd`, `figure`+`figcaption`, `menu`), texto & semântica inline (`a`, `em` vs `i`, `strong` vs `b`, `small`/`s`/`cite`/`q`/`dfn`/`abbr`, `time` com `datetime` ISO 8601, `code`/`var`/`samp`/`kbd`, `sub`/`sup`, `mark`, ruby com `rt`/`rp`, bidi `bdi`/`bdo`, `span`, `br`, `wbr`), mídia & incorporação (`img` com `alt` obrigatório e `srcset`, `picture`+`source` responsivo, `audio`/`video` com `poster`, `track` WebVTT de legenda, `iframe` com `sandbox`, `embed`/`object`/`param`, `map`/`area`, `canvas` de desenho JS), tabelas (`table` que nunca é pra layout, `caption`, `colgroup`/`col` com `span`, `thead`/`tbody`/`tfoot`, `tr`, `th` com `scope`, `td` com `colspan`/`rowspan`), formulários (`form` com `action`/`method`, `label` com `for` focando o campo, `input` com os dezenas de `type`, `button` com `type` submit/button/reset, `select`/`optgroup`/`option`, `textarea`, `output` de cálculo, `progress` vs `meter`, `fieldset`+`legend` agrupando controles, `datalist` de autocomplete) e interativos (`details`+`summary` que abrem/fecham SEM JS, `dialog` com `showModal()`, `template` clonável por JS, `slot` de web component) —, cada entrada com a tag em `<code>`, Tag colorida da categoria, selo `void` nos 14 elementos sem tag de fechamento (area/base/br/col/embed/hr/img/input/link/meta/param/source/track/wbr — o `/` de `<br />` é opcional e ignorado) e descrição PT/EN; busca que casa tag ou descrição, filtro por categoria em botões de rádio com contagem de resultados e botão de copiar a lista filtrada como tabela Markdown; um Alert destaca o modelo mental — cada elemento tem uma função (nada de `div` pra tudo: section/ul/table existem pra isso), a hierarquia de headings com UM h1 por página, `main`/`nav`/`article` comunicando estrutura ao leitor de tela e buscadores —; complementa o `markdown-syntax` e a família de formatadores HTML, que não catalogam as tags)
- Referências: Cheat Sheet de Testes JS (Jest & Vitest) — `/references/js-testing-cheatsheet` (cheat sheet pesquisável de teste de JavaScript — o primeiro item do projeto sobre TESTES, cobrindo o gap do `javascript-cheatsheet`/`typescript-cheatsheet` (que ensinam a escrever código, não a testá-lo): o par conceitual `vitest` (runner nativo do Vite, reusa tsconfig/aliases/plugins) vs `jest` (runner clássico independente de bundler) — os DOIS usam a MESMA API de estrutura (`describe`/`it`/`test`/hooks) e os MESMOS matchers (`expect`); só o objeto de mock muda (`vi.*` no Vitest, `jest.*` no Jest), então migrar é ~1:1 —, com 70 entradas em 6 categorias: setup & comandos (install de vitest/jest/jsdom e da stack `@testing-library/react`+`jest-dom`, script `vitest run` pro CI vs `vitest` em watch, `vitest --coverage` exigindo `@vitest/coverage-v8`, filtro por nome com `-t`, o comentário mágico `// @vitest-environment jsdom` por arquivo e `globals: true` liberando describe/it/expect sem import), estrutura & hooks (`describe` aninhando com hooks próprios, `it`/`test` como alias, `it.only` — o debug que não pode subir no CI —, `it.skip`/`it.todo`, `beforeEach`/`afterEach`/`beforeAll`/`afterAll` na ordem certa, `it.each`/`describe.each` data-driven com os placeholders `%i`/`%s` e `expect.hasAssertions()` pegando teste sem assert), expect & matchers (`toBe` igualdade por REFERÊNCIA pra primitivos vs `toEqual` profunda recursiva vs `toStrictEqual` que também veta fields undefined e tipos divergentes `1` vs `"1"`, `toBeTruthy`/`toBeFalsy` com a coerção de if, numéricos `toBeGreaterThan`/`toBeLessThan` + `toBeCloseTo` pra float E VETADO toBe direto em decimal, `toHaveLength`/`toContain`/`toHaveProperty` com caminho por ponto, `toMatch` regex, `toMatchObject` por subconjunto, `toThrow` aceitando msg/regex/classe, `not`, `toHaveBeenCalledWith`/`toHaveBeenCalledTimes`, e os placeholders `expect.any(Number)`/`stringContaining`/`arrayContaining`/`objectContaining`), mocks & spies (`vi.fn()` registrador de chamadas, `mockReturnValue` vs `mockReturnValueOnce` de sequência, `mockResolvedValue`/`mockRejectedValue`/`mockImplementation`, `vi.spyOn` com `mockRestore` trazendo o original de volta, `mockName`, `vi.mock` de módulo inteiro ou com factory, e a quadrilha `clearAllMocks`/`resetAllMocks`/`restoreAllMocks` na ordem certa + timers fake `vi.useFakeTimers`/`advanceTimersByTime`/`runAllTimers`/`useRealTimers` no afterEach), testes assíncronos (callback async que o runner ESPERA antes do veredito, `resolves`/`rejects` sem then/catch e `expect.assertions` pegando a promise "órfã" que passa de mentira) e React & Testing Library (render que limpa sozinho, `getBy` que falha-rápido vs `queryBy` que volta null pra asserção de ausência vs `findBy` assíncrono = getBy + waitFor, `getByRole` por papel+nome acessível como PRIORIDADE e `getByTestId` como último recurso, `userEvent` mais realista que `fireEvent`, os matchers do jest-dom `toBeInTheDocument`/`toBeDisabled`/`toHaveTextContent` e `waitFor` de polling pro async); busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem, botão de copiar cada comando ou a lista filtrada como tabela Markdown e Alert com o modelo mental — teste é sempre A-A-A (arrange/act/assert), toBe vs toEqual/quando usar cada um, e a promise órfã que "passa de mentira" sem await/return —, tudo 100% client-side

## 2026-08-11
- Ferramenta: Glob Pattern Tester (gitignore) — `/tools/glob-tester` (testa globs no dialeto do `.gitignore` — o mesmo usado por `.dockerignore`/`.npmignore`/`.editorconfig` e pelo `paths:` do GitHub Actions — contra uma árvore de arquivos editável e vê, arquivo a arquivo, se casa ou não: o motor vive em `src/utils/globMatcher.js`, 100% client-side, com `compileGlob` transcrevendo cada linha em RegExp (`*`/`?` que nunca cruzam a barra, `**` de segmento inteiro virando `(?:[^/]+/)*` no começo/meio e `(?:/.*)?` no fim, classe `[a-z]`/`[!a]` e chaves `{a,b}` por segmento, padrão com `/` no meio ancorado na raiz vs sem `/` casando em qualquer profundidade, `/` no final marcando "só diretório" — exclui o diretório e tudo dentro, mas um arquivo com o mesmo nome do diretório não é excluído —, e o `**` fora de segmento inteiro virando `*` normal, como manda a spec) e `matchFile` aplicando as regras do gitignore (a ÚLTIMA linha que casa decide — a negação `!` vira a última — e um `!` NÃO re-inclui arquivo dentro de diretório já excluído, então `coverage/` + `!coverage/.gitkeep` não surte efeito a menos que `!coverage/` venha antes); a página tem: duas TextAreas (padrões um por linha e caminhos da árvore) com presets de um clique (gitignore típico e GitHub Actions paths) e limpar, resultado ao vivo via `useMemo` com a lista filtrada por Segmented (Todos/Mantidos/Ignorados com contagem), cada linha mostrando Tag Mantido/Ignorado e o padrão que decidiu (`decidido por`), Tags de estatística (arquivos, ignorados, mantidos, nº de padrões) e a lista de "sem efeito" — os padrões que nunca foram a última linha decisiva de nenhum caminho (o caso do `!coverage/.gitkeep` cai aqui, ensinando a regra do diretório excluído) —, contagem de padrões inválidos e botão de copiar a lista de ignorados; Alert com as pegadinhas do dialeto (`*` nunca cruza `/` — é o `**` que cruza, ancoragem pela presença de `/` no meio, `/` final = diretório, última linha que casa decide + a regra do re-incluir dentro de diretório excluído); o algoritmo-fonte (`compileGlob`/`matchFile`/`expandBraces`) exibido na própria página num Collapse; complementa o `regex-tester` (regex ≠ glob: aqui não há âncora/anchor nem grupos de captura, é o dialeto de arquivo), o `gitignore-generator` (gera templates, mas não testa padrões contra a árvore real) e o `github-actions-cheatsheet` (cujos filtros `paths:`/`files:` usam exatamente esse dialeto) — é o primeiro item do projeto que testa globs/matching de arquivos; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de TypeScript — `/references/typescript-cheatsheet` (cheat sheet pesquisável do JS tipado — a primeira referência de "linguagem de tipos" do projeto, complementando o `javascript-cheatsheet` (a linguagem base, sem tipos) e o `python-cheatsheet` (o "segundo idioma" com tipagem de graça), e fechando o gap do lado do `json-to-typescript` (que gera, mas não ensina): 79 entradas em 10 categorias — tipos & anotações (anotação explícita vs inferência, `string[]`/`Array<T>`, tupla `[string, number]`, o `unknown` que força narrowing vs o `any` que vaza, `void` de callback, `string | null` e o array `readonly`), interfaces & aliases (`interface` contrato de shape vs `type` alias, `extends` de interface, interseção `&`, `readonly` de prop, `?` opcional, index signature `[key: string]: unknown` e call signature de função), unions, narrowing & assertions (união de literais matando string mágica, `typeof` narrowing, `in` por propriedade, a discriminated union com `kind` + switch exaustivo, type assertion `as` como mentira consciente e o type predicate `x is string`), generics (`identity<T>`, o `<T,>` do arrow em .tsx, `Box<T>`, `ApiResult<T>` de genérico+union, multi-parâmetro, o par `K extends keyof T` + `T[K]` e classe genérica), utility types (`Partial`, `Pick`, `Omit`, `Required`, `Readonly`, `keyof`, indexed access `User['age']`, `ReturnType`/`Parameters` e `Record`), funções & overloads (parâmetros+retorno, arrow tipada, alias de função-callback, callback inline na assinatura, default param, rest params e overloads com uma implementação só), classes & modificadores (`public/private` no construtor como parameter property, `private` vs `protected` vs `readonly`, `abstract` + método abstrato, `override` do TS 4.3+, `implements` e `static`), módulos & `.d.ts` (`export`/`import`, default export, o `import type` que some no bundle, re-export, `declare module '*.css'`, `declare global` augmentando Window, `export declare` de .d.ts e o reference directive `/// <reference types="vite/client" />`), tsconfig & CLI (`tsc --init`, o `"strict": true` que liga tudo, `target`, o par `module: ESNext` + `moduleResolution: bundler` do Vite, `noEmit` como lint de tipos, `jsx: react-jsx`, aliases `paths` e o `tsc --noEmit --watch` contínuo) e pegadinhas & dicas (`any` contagioso do `JSON.parse`, array vazio inferindo `never[]`, `as const` congelando literais, o "enum sem enum" com `as const` + `keyof typeof`, `Object.keys` devolvendo `string[]`, `?.` + `??` à prova de null, brand types pra tipo nominal, `satisfies` do TS 4.9 e o cast duplo `as unknown as T`) —, cada entrada com código em `<pre>` pronto pra colar, descrição PT/EN, categoria em Tag colorida, busca que casa código ou descrição, filtro por categoria em botões de rádio com contagem e copiar cada trecho ou a lista filtrada como Markdown (com fence ` ```ts `); Alert com o que mais pega — strict é o produto (null-check/noImplicitAny antes do deploy), tipos são apagados em runtime (`typeof`/`instanceof` operam sobre valores reais, interface some do bundle), `any` contagioso na fronteira parse/fetch, tipagem estrutural comparando FORMAS (brand type pra identidade), `interface` vs `type` na convenção prática e o `tsc` checando enquanto o bundler constrói; 100% client-side, nada sai do navegador)

## 2026-08-11
- Front-end: Gerador de Tipografia Fluida (clamp) — `/frontend/clamp-generator` (monta o `font-size: clamp(min, Xvw + Ypx, max)` que faz o texto escalar sozinho com a largura da tela — sem media query nem breakpoint —, com a matemática da reta resolvida em `src/utils/fluidType.js`: `buildClamp` normaliza as entradas (troca min/max se vierem invertidos e garante um intervalo de viewport positivo contra divisão por zero), deriva `slope = (max−min)/(maxVw−minVw)` px por px de viewport e `intercept = min − slope×minVw` (o valor no viewport 0) e emite `clamp(min, (slope×100)vw + intercept, max)`; sliders de tamanho mínimo/máximo (10–72/12–120px) e viewport mínimo/máximo (320–800/801–2560px, com ranges que nunca colidem), unidade de output px ou rem (`1rem = 16px`, o rem acompanha preferência de acessibilidade do usuário), 5 presets de um clique (H1 hero 34→64, seção 24→42, card 18→26, corpo 14→18, legenda 12→14); preview ao vivo com o clamp real aplicado em título + texto de apoio (aumentar/encolher a janela escala a fonte), gráfico SVG "tamanho × viewport" desenhando a curva — reta entre os dois pontos de ancoragem (círculos com o valor em px), linhas tracejadas em min/max e nos dois viewports, e um marcador laranja que anda pela curva conforme o slider "conferir tamanho em" (320–2560px) mostrando o tamanho renderizado naquela largura (`sizeAt`, que congela no mínimo antes de minVw, no máximo depois de maxVw e interpola linear no meio); estatísticas do cálculo em Tags (`1.5023vw`, o intercept em px/rem, o `+0.0151px` de crescimento por px de viewport) e copy do bloco com comentário do CSS gerado; Alert com as pegadinhas — `vw` olha o viewport INTEIRO, não o container (num layout de sidebar/largura máxima o texto cresce mais que a coluna; o caminho é `cqw` de container queries ou calc manual), e a diferença rem vs px em acessibilidade; o algoritmo-fonte (`buildClamp`/`sizeAt`) exibido na própria página num Collapse; complementa a família de geradores de CSS (`css-unit-converter` de unidades, `border-radius`, `gradient`, `clip-path`, `keyframe`...) — é o primeiro item do projeto sobre tipografia responsiva/fluida; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet do GitHub CLI (gh) — `/references/gh-cli-cheatsheet` (cheat sheet pesquisável do `gh`, a CLI oficial do GitHub — o elo que faltava entre o `git-commands` (git puro, local, sem GitHub) e o `github-actions-cheatsheet` (o pipeline, não o CLI): 81 entradas em 8 categorias — login & config (`gh auth login` uma vez e tudo autenticado, `auth status` de validade do token, `auth switch` trocando conta, `auth token` que imprime UMA CREDENCIAL VIVA — tratada como segredo —, o `--with-token` de CI além da env `GH_TOKEN`, `config set git_protocol ssh`, `alias set` criando `gh pc`, `completion` e o `--help` de cada subcomando como manual mais rápido), repositórios (`repo create --clone`, `repo clone` sem digitar senha no HTTPS, `repo fork --remote` já com o upstream, `repo sync` de fork, o `repo set-default` que fixa o repo da pasta, `repo edit --add-topic`, `repo rename` que redireciona o antigo, `repo delete --yes` IRREVERSÍVEL e o `search repos` com qualificadores), pull requests (`pr create --fill`/`--draft`/`--web`, `pr checkout` que configura o fetch de `refs/pull/*` sozinho, `pr view`, `pr list --search review:required`, `pr diff --patch`, `pr review --approve`/`--request-changes`, `pr merge --squash --delete-branch` e o `--auto` de auto-merge, `pr checks --watch`, `pr comment --edit-last`, `pr ready`/`pr reopen` e `pr edit --add-reviewer`), issues (`issue create/list/view`, `close --reason completed` alimentando insights, `comment --edit-last` de status locker, `edit --add/--remove-label`, `transfer` entre repos, `issue develop -c` de branch vinculada e o `issue delete` que NÃO é close), releases & tags (`release create --generate-notes`, a versão com assets via glob `dist/*.tar.gz`, `release list/view --json`, `release download --pattern`, `release delete --cleanup-tag`), Actions / CI (`run list --status failure`, `run view --log-failed` que mostra SÓ o step que quebrou, `run watch`, `run rerun --failed`, `run download --name`, `workflow list/run dispatch with -f inputs/enable/disable`, `run cancel` e `run delete`), gh api & automação (`gh api repos/{owner}/{repo} --jq`, o `user --jq .login` de "quem sou eu", `--paginate` percorrendo TODAS as páginas sozinho, `-X PATCH/POST/DELETE` com `-f` string e `-F` tipado, `graphql -f query=`, o `.mergeable` em JSON pra CI decidir, `secret set < file`/`secret list`/`variable set --env`, `gist create -p`, `ssh-key add` e o `auth setup-git` do runner) e codespaces & mais (`codespace create/list/code/ssh/stop/delete`, `browse 42`, `label list/create`) —, cada entrada com o comando em `<pre>` (vários em duas linhas), categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert cobre o que mais engana — gh ≠ git (git versiona, gh gerencia o GitHub via token), o setup de auth que esquece (GH_TOKEN/`--with-token` em CI, scopes mínimos), o token do `gh auth token` como credencial viva a não vazar, o repo "default" da pasta (senão `-R owner/repo` em qualquer subcomando) e o `gh api` como o curl do GitHub com o par `--json + --jq` das listagens (complementa o `jq-cheatsheet`), além do aviso de que `repo/issue/release delete` são permanentes; validado em produção com Puppeteer (página nova + home, PT + EN, busca/categoria, sem pageerror); 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Redis — `/references/redis-commands` (cheat sheet pesquisável do Redis, o banco em memória de cache/fila/sessão/ranking/lock — o primeiro item do projeto sobre um banco NoSQL, fechando o lado "armazenamento em memória" que o `sql-commands` (SQL relacional) não cobre: 69 entradas em 10 categorias — conexão & CLI (`redis-cli -h -p -a`, o `--scan` em produção no lugar do `KEYS`, `--bigkeys` de auditoria de memória, `-n` do banco lógico, `--latency` medindo rede, `--eval` de script Lua de arquivo), strings (`SET`/`GET`, o `(nil)` como null de verdade, `INCR`/`INCRBY`/`DECRBY` de contador atômico, `SETEX` com TTL embutido em vez de SET+EXPIRE, o lock `SET key 1 NX EX 30`, `GETSET` de ler-e-zerar, a família `MSET`/`MGET` de round-trip único, `APPEND`/`STRLEN` e `GETRANGE` fatia por byte), chaves & expiração (`EXISTS` multi, `TTL`/`PTTL` com o significado de -1/-2, `EXPIRE`/`PEXPIRE` em chave existente pra estender sessão, `PERSIST`/`EXPIRETIME`, `RENAME`/`COPY`, `TYPE` + `OBJECT ENCODING` revelando a representação interna (int/embstr/raw/ziplist), `SORT`, e o par crítico `KEYS` — NUNCA em produção pois bloqueia o servidor single-thread — vs `SCAN` de cursor), listas (`LPUSH`/`RPUSH` cabeça/cauda, `LRANGE 0 -1`, o par `LPOP` vs `RPOPLPUSH` de fila confiável, o `BLPOP` bloqueante que dispensa polling, `LLEN`/`LINDEX`/`LSET` e o padrão `LTRIM` de "últimos 100"), sets (`SADD` idempotente, `SISMEMBER` O(1)/`SREM`, `SUNION`/`SINTER`/`SDIFF` + versões *STORE, `SMEMBERS`/`SCARD`, o sorteio `SPOP` vs `SRANDMEMBER`, `SMOVE` de transição de estado), hashes (`HSET` como objeto unitário, `HGET` vs `HGETALL`, `HMGET`/`HEXISTS`, `HINCRBY` de contador por-campo, `HSETNX`, `HKEYS`/`HVALS`/`HLEN`), sorted sets (`ZADD` com score, `ZREVRANGE` de Top-N, o re-`ZADD` que recalcula o ranking, `ZINCRBY` de "+10 sem ler score", `ZSCORE`, faixa `ZRANGEBYSCORE` com `(`/`[` de exclusivo/inclusivo, `ZRANK`/`ZREMRANGEBYRANK`, `ZREM`/`ZCARD`), streams (`XADD` com `"*"` de ID timestamp-seq, `XREAD` não-destrutivo de 0 até `$`, consumer groups `XGROUP CREATE`/`XREADGROUP ... '>'` com `XACK`/`XPENDING` de fila de jobs persistente, `XRANGE`/`XLEN`, `XTRIM MAXLEN ~` de poda aproximada), transações & scripts (`MULTI`/`EXEC` de tudo-em-sequência sem intercalação, `WATCH` otimista que aborta se a chave mudar, `EVAL` Lua atômico com `KEYS`/`ARGV`, e `SCRIPT LOAD`/`EVALSHA` de rodar por hash) e administração (`INFO` por seção, `CONFIG GET/SET` em runtime + `CONFIG REWRITE`, `MEMORY USAGE` do custo de uma chave, `DBSIZE`/`FLUSHDB`/`FLUSHALL`, `CLIENT LIST`/`CLIENT KILL ID`, `BGSAVE` vs `SAVE` bloqueante, `SLOWLOG` de gargalo single-thread e `MONITOR` de stream de comandos) —, cada entrada com o comando em `<code>` (multi-linha com `\n`), categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert destaca os conceitos que mais pegam — single-threaded (comando O(N) travar TODO o servidor; em produção `SCAN`, nunca `KEYS`), "o tipo decide o comando" (cada estrutura tem a família própria; comece pelo `TYPE`), TTL por chave com `(nil)` no `GET` de cache expirado (cache-aside) e a atomicidade que cobre UMA operação só (lógica multi-comando = Lua/transaction) —, além do Redis como estado em memória e não disco (persistência RDB/AOF discricionária entre snapshots; banco SQL é a fonte da verdade, Redis acelera); complementa o `sql-commands`/`sql-formatter` (relacional vs em-memory) e o `rate-limit-calculator` (o próprio INCR+EXPIRE como receita raiz) — é o primeiro item do projeto sobre Redis/NoSQL; 100% client-side, nada sai do navegador)

## 2026-08-11
- Referências: Cheat Sheet de Nginx — `/references/nginx-cheatsheet` (cheat sheet pesquisável do servidor web e reverse proxy que sustenta metade da internet — o irmão que faltava ao lado do `caddyfile-generator` (que é Caddy, não nginx) e dos cheat sheets de docker/systemd: 52 entradas em 8 categorias — config & comandos (`nginx -t` de validação, `-s reload` que recarrega SEM downtime, `-s stop` vs `-s quit`, `-T` despejando a config efetiva com includes resolvidos, `-g 'daemon off;'` do container Docker, o esqueleto do server block e o `include conf.d/*.conf`), location matching (`=` exato, `^~` prefixo que para a busca, `~`/`~*` regex em ordem de aparição, prefixo simples com prioridade de MAIS LONGO, o fallback `/ { try_files $uri $uri/ /index.html; }` do SPA, `@nome` de location nomeado e a dupla `root` vs `alias` com o 404 clássico), reverse proxy (`proxy_pass` nas duas formas — sem URI vai inteiro, com URI substitui a parte casada —, os headers X-Forwarded-*/Host que o backend precisa pra enxergar o cliente real, `proxy_http_version 1.1` + `Connection ""` do keep-alive, timeouts e `proxy_redirect`), load balancing (bloco `upstream`, `least_conn`, `ip_hash` sticky, `weight`/`max_fails`/`fail_timeout` do health-check passivo, `keepalive 32` e `down`/`backup`), arquivos estáticos (gzip com types/min_length, `expires`+`immutable` pro cache de assets versionados, `try_files`, `index`/`autoindex`, `alias` de fora do root e o trio sendfile/tcp_nopush/tcp_nodelay), HTTPS & TLS (`listen 443 ssl` + cert/key, o redirect http→https `return 301`, `ssl_protocols TLSv1.2 1.3`, HSTS com `always`, ciphers e `ssl_session_cache`), segurança & limites (`limit_req_zone` + `burst`/`nodelay`, `limit_conn`, `allow`/`deny`, `client_max_body_size` (o 413 do nginx antes do app ver), `server_tokens off` e headers de segurança) e logs (log_format com `$request_time`, `access_log off`, níveis de error_log, `map $status` filtrando o access log e o `-s reopen` pós-logrotate) —, cada entrada com o trecho em `<pre>` pronto pra colar, categoria em Tag colorida e descrição PT/EN; busca que casa diretiva/comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada trecho ou a lista filtrada como tabela Markdown; um Alert destaca o que mais pega — a hierarquia do location matching (regex ganha de prefixo simples), `root` ≠ `alias`, o reload sem downtime (`nginx -t` antes), as duas formas de `proxy_pass` e que o `413` de upload é o nginx na frente, não o app; é o primeiro item do projeto sobre servidor web/proxy reverso HTTP propriamente dito (o Caddyfile gerava config de proxy, mas de Caddy — aqui é o nginx mesmo); 100% client-side, nada sai do navegador)

## 2026-08-11
- Ferramenta: Conversor CSS ↔ JS (style object) — `/frontend/css-to-js` (cola um trecho de CSS e vira o objeto de estilo do React — `style={...}` — ou o caminho inverso; o núcleo vive em `src/utils/cssToJs.js` com as duas direções: CSS→JS usa `parseDeclarations` (que entende comentários/strings/`url(...)`, colhe declarações de dentro de cada bloco `{ ... }` — o texto de fora é seletor e é ignorado — ou de listas de declarações soltas sem chaves) + `styleKey()` (cameliza com as 3 pegadinhas que a regra simples erra: `font-size`→`fontSize`, `float`→`cssFloat` palavra reservada, prefixos de vendor na capitalização do React `-webkit-*`→`Webkit*`/`-ms-*`→`ms*`/`-moz-*`→`Moz*`/`-o-*`→`O*`, e propriedade custom `--x` intocada) + `normalizeValue()` (`14px` e `0` viram número — o React agrega o px sozinho —, `!important` é detectado/removido e contado, unidades `rem/em/%` continuam string) com o resto convertido em string JS com escape correto; o JS→CSS usa `cssProperty()` (camelCase→kebab com os prefixos de volta ao hífen) e `jsToCss` (remove comentários `//`/`/* */`, divide membros de nível superior por vírgula respeitando strings/aninhamento e pula objetos/arrays aninhados com contador, e número vira `Npx` EXCETO na lista de propriedades unitless do React — z-index, flex, opacity, font-weight, line-height, orden etc. —, que não ganham o `px`; modo de chaves com aspas (JSON-style) no CSS→JS; conversão ao vivo via `useMemo`, sem botão; 2 exemplos de um clique (regra do DevTools/Figma e objeto JS), estatísticas em Tags (declarações, `!important` removidos, valores aninhados pulados) e copy do resultado; Alert com as pegadinhas da style prop — camelCase vs kebab, o `px` implícito dos números, `!important` não aceito na style prop (caminho é CSS Module/styled-components), `--x` suportado no style mas pseudo-classes/seletores sem tradução pra inline style; o algoritmo-fonte exibido na própria página num Collapse; complementa o `/tools/css-formatter` (que reformata o texto do CSS) e fecha a ponte entre os geradores de CSS (gradient/box-shadow/filter) e o código React — é o primeiro item que converte CSS ⇄ objeto de estilo JS; validado em produção com Puppeteer nas duas direções e na home; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de GitHub Actions — `/references/github-actions-cheatsheet` (cheat sheet pesquisável do CI/CD nativo do GitHub — 80 entradas em 7 categorias: estrutura do workflow (o arquivo `.github/workflows/*.yml`, o esqueleto mínimo `name/on/jobs`, `run-name` da execução individual, o `permissions:` de least-privilege do `GITHUB_TOKEN` automático e `defaults.run`), eventos de disparo (o `on:` em três formas — string/mapa/array —, filtros `branches`/`paths`/`tags`, `pull_request` com `types:`, e a ressalva de PR de fork: o workflow roda em merge-ref com token read-only e SEM secrets; `workflow_dispatch` como botão manual com `inputs:`, `schedule` cron que só roda na branch default, `workflow_run` que encadeia pipelines e `release`), jobs & execução (`runs-on` hospedado ou self-hosted por labels, `needs:` serializando, `strategy.matrix` explodindo em 3×2 combinações, `container:` rodando o job na mesma image do deploy, `concurrency`/`cancel-in-progress` contra deploy em corrida, `timeout-minutes`, `environment` com aprovação e env-scoped secrets, e o transporte de dados entre jobs via `outputs:`+`needs.<job>.outputs`), steps & comandos (`uses: actions/checkout@v4`, `setup-node` com cache, o bloco `run: |` múlti-linha no mesmo shell com a pegadinha de que cada step é um processo novo, `GITHUB_ENV`/`GITHUB_OUTPUT` como canal entre steps, `if:` condicional por step/job com as tintas `success()`/`failure()`/`always()`, `shell: bash` e `working-directory`), expressões & contexts (o `${{ }}` com os contexts `github.ref/sha/head_ref/event_name/actor/workspace`, `runner.os/temp`, `env` vs `vars` (variables que NÃO são segredo) vs `secrets` (duplamente mascarado), `needs.*.outputs`, `matrix.*`, operadores `!`/`contains()`/`startsWith()`/`format()`/`fromJSON()` e as functions de status `success()`/`failure()`/`always()`, actions prontas (checkout/setup-node/setup-python/cache/upload-download-artifact, `docker/login-action` via `GITHUB_TOKEN` pra GHCR, `github-script` com octokit autenticado, e a trindade `gh release create`/`gh pr merge`/`gh run list` que já vêm instalados e autenticados no runner) e deploy & dicas do dia a dia (OIDC trocando o token por credenciais AWS TEMPORÁRIAS com `permissions: id-token: write`, o perigo real do `pull_request_target` — roda no contexto da base com secrets mas executa o CÓDIGO do PR, a fixação de action por SHA em vez de tag prata supply-chain, artefato `if-no-files-found: error` pra não virar pipeline mudo e os debug-flags `ACTIONS_STEP_DEBUG`/`ACTIONS_RUNNER_DEBUG`) —, cada entrada com o trecho YAML em `<pre>` pronto pra colar, descrição PT/EN, categoria em Tag colorida, botão de copiar por snippet e copiar a lista filtrada como Markdown; busca que casa trecho de código ou descrição, filtro por categoria em botões de rádio com contagem e contador de resultados; um Alert cobre o que mais pega — jobs rodam em paralelo (needs/matrix/outputs/artefatos), secrets não vão pra PR de fork, o `GITHUB_TOKEN` automático pede `permissions:` mínimas, e o `$GITHUB_OUTPUT`/`$GITHUB_ENV` como único canal entre steps; fecha a família de cheat sheets de automação ao lado do `git`/`docker`/`kubectl`/`systemd` — é o primeiro item sobre CI/CD pipelines; 100% client-side, nada sai do navegador)
- Correção de bug: bandeira Elo lida como Visa — `/tools/credit-card-tool` (a detecção de bandeira em `src/utils/creditCard.js` verificava a faixa genérica `4` da Visa ANTES das faixas da Elo, e a Elo tem IINs começando com "4" (401178–401179, 431274, 438935, 451416, 457393, 457631–457632) — então todo cartão Elo real dessas faixas era rotulado como "Visa", fazendo o validador parecer que só conhecia cartões americanos; a banda Elo agora é checada antes da Visa, então esses cartões leem como Elo (universal/brasileiro), e as demais bandeiras continuam intactas — os 10 exemplos de um clique e o gerador/validador Luhn seguem passando)

## 2026-08-10
- Ferramenta: Formatador e Minificador de HTML — `/tools/html-formatter` (cola um HTML bagunçado — e-mail renderizado, trecho do DevTools, template — e formata com indentação ou minifica, 100% client-side via `DOMParser text/html` (o parser REAL do navegador, que corrige HTML malformado sozinho — tag sem fechar, `</p>` solto — então não existe "HTML inválido" aqui, a saída é o que o browser renderizaria); o serializador próprio em `src/utils/htmlFormatter.js` re-emite a árvore SEM reutilizar o whitespace original: elementos de linha (span, a, strong, em, code, img...) ficam inline junto do texto, elementos de bloco (div, p, ul, li, section, table...) ganham uma linha por nível de indentação (2/4 espaços), void (img, br, input, meta, link...) nunca têm fechamento, pre/textarea/style/script são preservados verbatim (espaços e quebras nunca reformatados), atributos booleanos (checked/disabled/required/selected...) saem sem `=""` (colapsando também a forma canônica `checked="checked"`) e os demais escapam `& " <`; o whitespace de texto colapsa como na renderização (`\s+` → um espaço), então multi-espaço entre blocos some sem quebrar o texto inline; minify remove indentação/quebras (uma linha só) com toggle de remover comentários `<!-- -->`; detecta fragmento vs documento completo (doctype + html/head/body formatados corretamente, com a pegadinha de meta/link/style/title subirem pro `<head>` mesmo em fragmento — juntando head+body de volta na saída pra nada sumir — e o `<tbody>` que o parser injeta em `<table>`); 2 exemplos de um clique (fragmento bagunçado e documento completo com doctype/tabela) + Limpar; estatísticas na saída — elementos, atributos, comentários, bytes de entrada/saída e % de economia no minify; Alert com as pegadinhas reais: parser corrige HTML malformado (não valida), conteúdo de pre/textarea/style/script é verbatim, whitespace colapsa (múltiplos espaços sem `&nbsp;` SOMEM — se você dependia deles, precisa deles) e head-hoist de meta/link/style em fragmento; o algoritmo-fonte (block × inline × void × preserve + collapseWs + emitChildren) exibido na própria página num Collapse; fecha a família de formatadores do projeto — json/xml/css/toml/sql existiam, HTML era a lacuna (IDEAS.md listava "HTML Minify/Beautify" em aberto) — e complementa o `html-to-jsx`/`html-to-markdown` (que convertem HTML pra outro formato, não reformatam); 100% client-side, nada sai do navegador)
- Ferramenta: Gerador de QR Code — `/tools/qr-code-generator` (transforma qualquer texto em QR Code no navegador, byte a byte, sem API: campo de conteúdo, exemplos de um clique (URL, Wi-Fi `WIFI:T:WPA;S:...`, seed TOTP `otpauth://`, payload JSON, `mailto:`), ECC em Segmented L/M/Q/H com o % de redundância (7/15/25/30%), escala do preview (4/8/12 px/módulo), zona de silêncio ajustável 0–8 módulos (spec exige ≥ 4) e cores de módulo/fundo via ColorPicker; o núcleo usa o pacote `qrcode` (dependência nova, 100% client-side) chamando `QRCode.create` síncrono pra ler a matriz binária e um renderer próprio (`qrToSvg`, em `src/utils/qrRenderer.js`) que monta o SVG do zero com um `<path>` por raia horizontal de módulos escuros (run-length → SVG compacto em versões altas); preview ao vivo, PNG em data URI via `QRCode.toDataURL` e copiar SVG / copiar PNG data URI / copiar `<img>` / baixar `.png`; detalhes da matriz (versão, grade, ECC, payload em bytes UTF-8, módulos escuros, % de densidade), aviso de contraste (razão WCAG entre as cores, Tag verde/laranja/vermelha) e erro amigável quando o texto estoura a versão 40; Alert com o essencial — QR é string SEM criptografia (quem fotografar lê; não coloque segredo/token de recovery), contraste e tamanho de módulo é o que manda no impresso, zona de silêncio exigida pela spec, ECC troca densidade por robustez; algoritmo-fonte (encodeQr + qrToSvg + contrastRatio + toDataURL) exibido na própria página num Collapse; validado de ponta a ponta em produção com Puppeteer + jsQR — os QRs gerados decodificam exatamente pra string de entrada (testou URL, Wi-Fi e JSON); primeiro item do projeto que gera QR/imagem escaneável — a família de geradores existente (uuid, slug, senha, cartão) lidava só com texto/código; 100% client-side, nada sai do navegador)
- Ferramenta: Gerador/Validador de Cartão (Luhn) — `/tools/credit-card-tool` (valida um número de cartão pelo checksum Luhn — módulo 10 — passo a passo e detecta a bandeira pelas faixas de IIN: Visa/Mastercard/Amex/Elo/Discover/Hipercard/Diners/JCB/UnionPay/Maestro, com a ordem de verificação e os ranges no código — incluindo a sutileza dos co-branded 622126–622925, que são Discover e não UnionPay, verificados antes do "62" genérico, e as faixas Elo `65…` checadas antes do "65" do Discover —; ou gera números de teste Luhn-válidos por bandeira: sorteia uma faixa de IIN da banda, preenche o meio e fecha com `luhnCheckDigit`, com retry até o número "ler" como a bandeira pedida (fura sobreposições de range); aba Validar: campo que ignora espaço/traço, 10 exemplos de um clique (os números de teste oficiais aceitos em sandbox — 4242…, 5555…, 3782…, 6011…, 3056…, 3530…, 636297…, 606282…, 625094…, 6304…), Descriptions com bandeira (Tag colorida), checksum Válido/Inválido, contagem de dígitos, comprimento Aceito/Fora do padrão pra bandeira e o número formatado (Amex 4-6-5, resto em grupos de 4); caixa do passo a passo do Luhn mostrando dígito por dígito (dobrados ×2 com soma de dígitos quando passa de 9, verificador destacado na borda) + soma total e veredito múltiplo de 10; aba Gerar: bandeira + quantidade 1–20 e lista com formatado/cru e copiar individual ou todos; Alert separando o conceito — Luhn pega erro de digitação, não prova que o cartão existe nem que o BIN pertence a alguém; um número Luhn-válido com BIN aleatório não é de ninguém e recusaria um pagamento real — use só em sandbox/testes; e que os BINs (primeiros 6 dígitos) identificam o emissor, com o resto alocado pelo banco; o algoritmo-fonte (onlyDigits + luhnCheck + luhnCheckDigit + detectBrand + generateCardNumber) exibido na própria página num Collapse; complementa o `cpf-cnpj-generator`, que usa módulo 11 pra documentos BR — é o primeiro item do projeto com o checksum Luhn/módulo 10 e a primeira ferramenta de cartões/pagamentos; 100% client-side via `src/utils/creditCard.js`, nada sai do navegador)
- APIs: Conversor de cURL → Código — `/apis/curl-to-code` (cola um comando `curl` — do "copy as cURL" do browser, de uma doc, de um log — e gera o equivalente pronto pra colar em `fetch` (JS), `axios` (JS) ou Python `requests`; o inverso exato do `curl-generator`, que monta o comando a partir de um formulário e não faz o caminho inverso; o parser é 100% client-side e entende: tokenizer próprio que respeita aspas simples/duplas com escape `\"` e continuação de linha com `\`, opções longas (`--request`, `--header`, `--data*`, `--user`, `--cookie`, `--user-agent`, `--referer`, `--url`) e curtas com valor anexado tipo `-XPOST`/`-HAceita: json`, método padrão do curl (`-d` sem `-X` => POST), `-G` transformando os `-d` em query string (via URLSearchParams que re-encoda), corpo que parece JSON detectado via `JSON.parse` e emitido embelezado (`JSON.stringify({...}, null, 2)` no fetch, objeto literal no axios, dict literal no Python com `True`/`False`/`None` corretos), `-u user:pass` virando `Authorization: Basic` com base64 unicode-safe via TextEncoder, `-b`/`--cookie` virando header `Cookie`, `-A` virando `User-Agent`, e em Python `auth=("user", "pass")` próprio do requests; badges de resumo (método colorido, URL, nº de headers, tipo de corpo — JSON/texto/query/sem corpo — e flags reconhecidas `--compressed`, `-k`, `-F` em volcano); Alert com as pegadinhas — `--compressed` e `-k` são apenas sinalizados e não recriados no código (fetch do browser descomprime e ignora TLS sozinho; no Python seria `verify=False`), multipart `-F` é consumido e ignorado, e vários `-d` são unidos com `&` como o curl faz; 5 exemplos de um clique (POST JSON, `-G` com query, basic auth, estilo "copy as cURL" com cookie/user-agent/--compressed, PUT com Bearer); o algoritmo-fonte (`tokenize` + `parseCurl` + `normalize` + emissores) é exibido na própria página num Collapse; complementa o `curl-generator` (form → curl) — é o primeiro item que converte curl → código de verdade; 100% client-side, nada sai do navegador)
- DevOps: Calculadora de SLA (orçamento de downtime) — `/devops/sla-calculator` (calcula quanto tempo de indisponibilidade um contrato de disponibilidade permite num período — o "orçamento de downtime" — e confere se incidentes reais estouraram o SLA: presets de 1 clique dos alvos do dia a dia (99, 99.9, 99.95, 99.99, 99.999, 99.9999%, cada 9 adicionado encolhe o orçamento ~10×), alvo custom via InputNumber, período 1 dia/7 dias/30 dias/1 ano ou customizado, e o orçamento em 3 visões — duração adaptativa (`43.2 min`), quebra em componentes (`43 min 12 s`) e a fatia percentual do período (`0.1%`); o segundo card registra incidentes reais (lista de linhas com valor + unidade s/min/h/d, adicionar/remover), soma o downtime total, deriva a disponibilidade efetiva (`1 − downtime/período`), mostra o orçamento consumido numa Progress bar com format customizado e o veredito `Dentro do orçamento`/`Estourou` em Tag verde/vermelha com o valor sobrando ou estourado; Alert explicando a notação dos "9s" e por que `99.999%` ≈ 26 s/mês muda a prioridade de on-call, botão de copiar o resumo e o algoritmo-fonte (`budgetMs`, `effectiveAvailability`, `formatBreakdown`) exibido na própria página num Collapse; complementa o `rate-limit-calculator` (que mede taxa/limit, não SLA) e o `days-until`, que não tem o conceito de contrato de disponibilidade — é o primeiro item do projeto sobre uptime/disponibilidade; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Comandos systemd — `/references/systemd-commands` (cheat sheet pesquisável do systemd, o init system dos Linux modernos (Ubuntu 16+, Debian 8+, Fedora, Arch) — o irmão que faltava ao lado dos cheat sheets de `docker`/`kubectl`, que tratam de containers/cluster mas não do init system nem do log da máquina: 69 entradas em 6 categorias — serviços (`systemctl status/start/stop`, `restart` vs `reload` que exige `ExecReload=`, o par `enable` (liga no boot, cria o symlink de `WantedBy=`) vs `start` (roda agora) com o combo `enable --now`, `daemon-reload` obrigatório depois de editar unit, `is-active`/`is-enabled` com exit-code de condicional de script, `list-units`/`--failed`/`list-unit-files`, `mask` (bloqueia até inicio por dependência) vs `disable` (só tira do boot), `cat` mostrando o conteúdo EFETIVO com drop-ins e `edit` abrindo o override `override.conf`), alvos & boot (`get/set-default`, `isolate`, `reboot`/`poweroff`/`suspend` graciosos, `list-dependencies`/`list-jobs` e a família `systemd-analyze blame/critical-chain/verify` do boot medido), logs (`journalctl -u` filtra por NOME DO UNIT, `-f` follow, `-n`, janelas `--since "10 min ago"`/`--until`/`today`, `-p err` por nível, `-b`/`-b -1` por boot, `-k` kernel persistente, `-o json` pra pipe com jq, `-x` com explicações, `--vacuum-time` curando disco de log), unit files (esqueleto do `.service` de três seções, `Type=simple/forking/oneshot`, `ExecStart` em caminho absoluto, `WorkingDirectory`, `User=`, `Restart=on-failure`+`RestartSec` contra crash-loop, `Environment`/`EnvironmentFile`, `LimitNOFILE` explicando que o rlimit vem do systemd PID 1 e não do `ulimit` do shell, `After`+`Wants` como par ordem+dependência, `Requires` forte e `OnFailure` de alerta), timers (esqueleto `[Timer]` no lugar do cron, `OnCalendar=daily`/`Mon..Fri *-*-* 09:00:00`/`*:0/15`, `OnBootSec`/`OnUnitActiveSec`, `Persistent=true` rodando disparo perdido, `AccuracySec` de coalescência, `list-timers`) e dicas do dia a dia (a linha de auto-cura `is-active --quiet && ... || restart`, `/lib/systemd/system` vs `/etc/systemd/system` que VENCE, `list-dependencies --reverse`) —, cada entrada com o comando em `<pre>` (multi-linha com unit files completos), categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert destaca os pares que mais enganam — `enable` ≠ `start`, `daemon-reload` depois de tocar em unit, `reload` só com `ExecReload=`, `mask` ≠ `disable` e `systemd-analyze blame` pra achar o que segura o boot; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Shell Scripting (Bash) — `/references/bash-scripting` (referência pesquisável do bash como LINGUAGEM de script — o "irmão" que faltava ao lado dos `/references/bash-shortcuts` (atalhos de leitura de linha/processos) e `/references/grep-sed-awk` (filtros de texto): 61 entradas em 8 categorias — fundamentos (shebang `#!/usr/bin/env bash`, `chmod +x`, `set -euo pipefail` explicado flag a flag, `$?`/`exit`, `&&`/`||` vs `;`, argumentos posicionais `$0`/`$1`/`$#` e o fallback em grupo `|| { ...; exit 1; }`), variáveis & expansão (atribuição SEM espaço no `=`, a regra "aspas duplas expandem / simples preservam", os defaults `${var:-}`/`${var:=}`/`${var:?}` do fail-fast, `$(( ))`, `${#var}`, fatia `:2:4`, remoção de prefixo/sufixo `#`/`%` com `##`/`%%`, substituição `//` e `"$@"` com `shift`, `$( )` command substitution moderno vs backticks), condicionais (`if/elif/else/fi`, `[[ ]]` superior ao `[ ]` POSIX, testes de arquivo/string/número, `=~` regex, `!`/`&&`/`||` negados, o `case` switch e o ternário de uma linha), loops (`for in`, `{1..10}`/`seq`, for estilo C, o `while IFS= read -r` linha-a-linha, `until` de "espera o serviço subir", `continue`/`break` com níveis), arrays & strings (acesso por índice, o `"${arr[@]}"` aspado que preserva valores com espaço, `+=`/`unset`, o tamanho `${#arr[@]}` vs `${#arr}` do primeiro elemento, arrays associativos `declare -A` e o case swap `^^`/`,,`), funções (`$1` por função, `local`, `return` + captura via `$(...)` do "retorno" por stdout, argumento com default e `export -f` pro child shell), here-docs & pipes (here-doc com/sem expansão `<<EOF` vs `<<\'EOF\'`, anexar com `>>`, here-string `<<<`, process substitution `<(...)`, o `2>&1` DEPOIS de `> log` (ordem importa), `{ }` agrupando no shell atual e o `tee`) e receitas do dia a dia (batch rename com `${f%.JPG}`, backup datado `$(date +%F)`, `mapfile` de arquivo pra array, o `trap ... ERR` que aponta a LINHA do erro com `$LINENO`, ler CSV com separador custom `IFS=:`, e retry com contagem visível via `printf`) —, cada entrada com o código em `<pre>` com quebra de linha, categoria em Tag colorida e descrição PT/EN; busca que casa código ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada snippet ou a lista filtrada como tabela Markdown; um Alert cobre as pegadinhas que mais derrubam script — espaços em volta do `=` (viram dois comandos), variável SEM aspas na linha de comando (word-splitting e glob: "Meu arquivo.txt" vira dois), "aspas duplas expandem, simples preservam" + `$( )` aninhável, o trio `set -euo pipefail` com o escape `cmd || true`, arrays sempre com `"${arr[@]}"` e o erro `2>&1 > log` vs `> log 2>&1` e o here-doc com expansão acidental `<<'EOF'`; complementa os cheat sheets de terminal (`/references/bash-shortcuts`, `/references/grep-sed-awk`) — é o primeiro item do projeto que cobre o bash como linguagem de script (loops/condicionais/funções/expansão), não apenas atalhos ou filtros; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Scrollbar CSS — `/frontend/scrollbar-generator` (monta a regra de scrollbar customizada de um container `overflow: auto/scroll` cobrindo os DOIS sistemas de estilização — as pseudo-classes WebKit `::-webkit-scrollbar`/`::-webkit-scrollbar-track`/`::-webkit-scrollbar-thumb`/`::-webkit-scrollbar-corner` (Chrome, Edge e Safari), que exigem uma largura/altura no `::-webkit-scrollbar` pra funcionar e têm como truque o thumb "afundado" via `border` na cor da trilha + `background-clip: padding-box`, e as propriedades padrão do Firefox `scrollbar-width` (auto/thin/none) + `scrollbar-color: thumb track` —, com sliders de largura/altura, raio do thumb, raio da trilha e borda do thumb, color pickers pra cor do thumb/trilha/canto, preview ao vivo que injeta o CSS gerado (mesma classe de escopo `DEMO_CLASS` do bloco de saída, então a pré-visualização usa o CSS exato que se copia) e o bloco final pronto pra colar com botão de copiar; Alert com as pegadinhas dos engines — macOS com overlay scrollbars esconde o scrollbar customizado quando parado (só aparece durante o scroll), as pseudo-classes WebKit são não-padrão da spec (leia-se: cada engine segue seu jeito, então o CSS precisa dos dois blocos juntos) e a ausência de raio/borda no Firefox (que só aceita `scrollbar-width`/`scrollbar-color`); o algoritmo-fonte (`buildScrollbarCss`, que monta o bloco na ordem canônica WebKit → Firefox) é exibido na própria página num Collapse via `toString()`; complementa a família de geradores de CSS (`gradient`, `box-shadow`, `border-radius`, `filter`, `clip-path`, `keyframe`), que não cobria a estilização de scrollbar — é o primeiro item do projeto sobre scrollbars; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de jq — `/references/jq-cheatsheet` (cheat sheet pesquisável do jq, o processador de JSON do terminal — o primeiro item do projeto que filtra/transforma JSON fora do navegador, complementando os converters `json-to-yaml`/`json-to-typescript` (que geram código a partir de um JSON colado) e fechando a família de filtros de texto ao lado do `/references/grep-sed-awk`: 72 entradas em 8 categorias — selecionar campos (`.name`, vírgula de múltiplos campos, `.a.b.c` aninhado, `.["a-b"]` de chave com caractere especial, índices `[0]`/`[-1]`, o `[]` que abre o array, o `.` sozinho de pretty-print, `-r` raw tirando as aspas e `-c` compact de JSON Lines), filtrar com select (`.items[] | select(.status == "ok")`, comparação numérica e data ISO lexicográfica, `.active == true`, and/or/not, `map(select(...))` mantendo o array, `index()` como teste de membro em lista, `test()` regex, `contains()` substring e `has()` de existência), transformar & construir (map de projeção/aritmética/renomeação, shorthand `{sku, nome: .name}`, `del(.chave)`, merge `. + {...}`, `map_values` e `with_entries` em massa, objeto literal fixo de saída), funções embutidas (length com sentido por tipo, keys, sort_by crescente com `| reverse`, unique/unique_by, add e o combo map+add de soma-de-coluna, contagem após filtro, max/min e max_by/min_by), strings & números (ascii_upcase/downcase, interpolação `"texto \(.campo)"`, tostring/tonumber, split/join inversos, fromjson/tojson, startswith/endswith, fatia de string `[0:5]`), arrays & fatias (`[1:3]` com fim exclusivo, paginação fora da faixa sem estourar, recolher num array com `[...]`, range, array+array que concatena, unique_by, flatten, `[0,1,2]` multi-índice), agrupar & reduzir (group_by que ordena pela chave, o `-s` slurp de JSON Lines, tabela de frequência e agregação de totais por grupo, reduce explícito, `as $var`, média com add/length) e receitas do dia a dia (curl | jq de API, interpolação no pipe do shell, `@tsv`/`@csv` exportando colunas pra planilha, `--arg` injetando valor do shell com `$(date +%F)`, relatório de alerta filtrando+exportando, sort desc + `.[0]` como `--limit 1`, `tail -f | jq -c` de streaming de logs e a "coluna" de kubectl/docker inspect/gh api) —, cada entrada com o comando em `<pre>`, categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert destaca as pegadinhas — o programa do jq SEMPRE entre aspas simples (em aspas duplas o shell expande `$var`, acentos graves e interpreta `|`/`>`/`()` antes do jq ver), a saída é JSON de novo por padrão (strings com aspas; `-r` tira, `-c` comprime), `select` DISPENSA os não-casados (somem do stream, não viram null), `.items[]` abre o array enquanto `map` devolve o array de volta, `group_by` ordena os grupos pela chave, `sort_by` é sempre crescente, `length` muda de sentido pelo tipo e objetos do jq são imutáveis (cada filtro devolve cópia); 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de grep/sed/awk — `/references/grep-sed-awk` (referência pesquisável dos três filtros de texto do shell — o primeiro cheat sheet do projeto sobre tratamento de texto no terminal, complementando o `/references/bash-shortcuts` (que cobre atalhos de edição de linha, não filtros) e os cheat sheets de ferramentas: 78 entradas em 8 categorias — grep busca básica (`grep errado` substring em qualquer parte da linha, `-i` ignorando case, `-v` invertendo, `-c` contando LINHAS não ocorrências, `-n` com número da linha, `-w`/`-x` palavra/linha inteira, `*.log` com glob, `-r` recursivo e `-l`/`-L` só nomes de arquivo), grep regex & flags (`-E`/ERE com `|` e `+` sem escapar, `^`/`$` ancoras, `-o` só o trecho casado, `-A`/`-B`/`-C` contexto de stack trace, `-q` exit como condicional de `&&`, `-P` PCRE com ressalva do BSD/macOS, `zgrep` em gzip, `--` fim de flags, `--include`/`--exclude-dir` dirigindo a recursão), find & xargs (glob com aspas, `-type`/`-size`, `-mtime`/`-mmin`, `-delete`, o elo `| xargs`, `grep` recursivo via xargs, `-exec {} \;`, `-print0 | xargs -0` segurando espaços e `-I {}` placeholder), sed substituição & edição (só primeira ocorrência vs `g` global vs `N`-ésima, `-i`/`-i.bak` no lugar com o alerta do BSD/macOS, `-E` com `+`, delimitador custom `s|a|b|` quando tem `/`, `\1` de backreference, `-n s///p` imprimindo só linhas mudadas, `\t` tab), sed impressão & deleção (`-n Np` linha certa, faixas `20,30p`, `1d`/`$d` de cabeçalho/rodapé com o `$d` em aspas simples, `2,10d`, deleção por padrão `/^#/d` e `/^$/d`, faixa por conteúdo `/BEGIN/,/END/p`, `5q` corte barato e `1~2p` linhas ímpares), awk campos & padrões (`$1`/`$0`, `-F:` separador, `NR`/`NF`/`$NF` embutidos, `-v` injetando variável do shell, padrao-condição `$3 > 1000`, `NR == 1 || /erro/`, `length($0) > 80`, `NR % 2` par/ímpar), awk agregação & formatação (esqueleto da soma, média com contador, contagem por chave `a[$1]++` com `END {for (k in a)}`, dedupe por ordem `!seen[$0]++`, max/min com if, `BEGIN {FS=","}`, `printf`/`%8.2f`, `toupper`/`tolower`, `substr`, contando linhas de dados com `NR >= 2`) e pipelines & receitas (limpar config `grep -v '^#'`, o `ps aux | grep | grep -v grep` de achar processo, `tail -f | grep --line-buffered` pra saída ao vivo, ranking `sort | uniq -c | sort -rn`, `cut -d`/`-c` colunas e caracteres, `tr` de delimitador brasileiro e o `2>&1 | tee` juntando stderr e salvando) —, cada entrada com o comando em `<pre>`/`<code>` pronto pra colar (multi-linha com quebra `\` quando é o caso), descrição PT/EN, botão de copiar por comando ou a lista filtrada como tabela/buffer Markdown com bloco bash, busca que casa comando ou descrição e filtro por categoria em botões de rádio com contagem; um Alert destaca as pegadinhas que pegam geral — grep é BRE por padrão (prefira `-E`; `-P` não existe no BSD do macOS), cubra o padrão em aspas simples senão o shell expande `$var`/`*`, `grep -c` conta linhas não ocorrências (`-o | wc -l` pra contar de verdade) e o grep só vê o stdout (`2>&1`), o sed não escreve sem `-i` e um `-i` sem backup é destrutivo (`-i.bak`/BSD `-i ""`; `s///` troca só a primeira ocorrência sem o `g`), no awk o `$1` tem de ficar dentro do programa em aspas simples (senão o shell come), `NF`/`NR`/`$0` e `-F`/`BEGIN{FS=...}` são as portas do separador, e `find | xargs` quebra em caminhos com espaço (use `-print0 | xargs -0`); complementa a família de cheat sheets (`/references/git-commands`, `/references/docker-commands`, `/references/sql-commands`, `/references/kubectl-commands`, `/references/openssl-commands`, `/references/ssh-cheatsheet`, `/references/vim-cheatsheet`) — é o primeiro item dedicado a grep/sed/awk do projeto; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Python — `/references/python-cheatsheet` (referência pesquisável da linguagem — o primeiro cheat sheet de uma segunda linguagem, complementando o `/references/javascript-cheatsheet`: 73 entradas em 10 categorias — variáveis & tipos (tipagem dinâmica, anotações `x: int` que o interpretador não valida, unpacking/swap, ternário, `input()` que sempre devolve str, o `is None` vs `==`, conversões int/float/str), strings & texto (f-strings `f"..."`, strip/lower/upper, split/join como inversos, startswith/endswith, `in` de substring, slicing `[inicio:fim]` com índice negativo, count/find, len e `"-" * 40`), listas & tuplas (mutável vs imutável, índices/slices, append vs extend, pop/remove, `in`, sorted vs sort() no lugar, enumerate, zip, range, max/min/sum), dicionários & conjuntos (`d["k"]` vs `d.get()` com KeyError, `in` checa chave, items()/keys()/values(), merge `{**a, **b}`, setdefault, operações de conjunto `&| - ^`, collections.Counter), controle de fluxo (indentação define bloco, for sempre em sequência, while, continue/break, try/except/else/finally e o `match`/`case` do 3.10+), funções (default params, *args, **kwargs, o `*` keyword-only, lambda, `key=` de ordenação, decoradores com @lru_cache), comprehensions & geradores (list/dict/set comprehension, ternário interno, gerador lazy sem alocar, gerador dentro de sum sem `[]`), classes & OOP (__init__/self, @dataclass, herança com super(), @property, classmethod/staticmethod, isinstance respeitando herança, dunder __repr__), arquivos & módulos (with open + encoding, modo "w" vs "a", arquivo iterável linha a linha, json.loads/dumps, os.environ.get, pathlib.Path, import/alias as, o guard `__name__ == "__main__"`, sys.argv) e async/await (corrotinas, asyncio.gather, asyncio.run, async with) —, cada entrada com o código-fonte em `<pre>` pronto pra colar, descrição PT/EN, botão de copiar por snippet e a categoria em Tag colorida; busca que casa código ou descrição, filtro por categoria em botões de rádio com contagem, contador de resultados e botão de copiar a lista filtrada como documento Markdown com bloco `python` por item; Alert com as pegadinhas que mais pegam — indentação define o bloco (sem chaves, tab+espaço quebra com IndentationError), `/` sempre float vs `//` piso (`-5//2 = -3`), sort() retorna None no lugar, `d["k"]` levanta KeyError vs `d.get()`, None sempre com `is` nunca `==`, True/False/None maiúsculos e o clássico parâmetro default mutável `def f(x=[])` que compartilha a MESMA lista entre chamadas; é a primeira página do projeto dedicada a Python; 100% client-side, nada sai do navegador)

## 2026-08-09
- Referências: Cheat Sheet de Comandos SSH — `/references/ssh-cheatsheet` (cheat sheet pesquisável do `ssh`, o acesso remoto que todo dev usa todo dia — 51 entradas em 7 categorias: conexão (`ssh user@host`, `-p` porta, `-l` usuário, `-o ConnectTimeout`, `-v` verboso, `-t` forçando TTY pro sudo remoto, `-q` pra só o comando e múltiplos comandos remotos), chaves (`ssh-keygen -t ed25519`/`-t rsa -b 4096`/`-f` nomeada com `-C` comentário, `-p` troca passphrase, `-y` derivando a pública, `cat ~/.ssh/*.pub` pra colar no GitHub, `ssh-copy-id` e `ssh-keyscan`), agent & ssh-add (`eval "$(ssh-agent -s)"`, `ssh-add` com `-l`/`-L`/`-d`/`-D`/`-t` expiração e `ssh -A` propagando o agent a diante), config (~/.ssh/config: alias `Host github`, `ServerAliveInterval` heartbeat, `ProxyJump` por padrão, `IdentitiesOnly yes` matando o erro "Too many authentication failures" e `Include` de configs por projeto), túneis & port forwarding (`-L` local→remoto `host:porta` incluindo via bastião, `-R` remoto→local, `-D` SOCKS5 proxy e `-f -N` túnel background, `-J` pulo por bastião), transferência (`scp`/`-r`/`-P`, `rsync -avz` com `--progress`/`--delete`/`-e "ssh -p"`, `sftp` interativo) e segurança (`chmod 700/600` que o SSH exige, `ssh-keygen -lf` fingerprint, `ssh-keygen -R` limpando known_hosts após redeploy, `accept-new` e o `sshd_config` de servidor) —, cada entrada com o comando em `<code>` (multi-linha com quebra `\` no config), categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert destaca o fluxo sem senha (`ssh-keygen → ssh-copy-id → ssh-add`) e as pegadinhas que pegam geral — permissão frouxa no `~/.ssh` (SSH recusa a chave; `600`/`700`), o "Too many authentication failures" (via `IdentitiesOnly yes`), a `known_hosts` que muda após redeploy (limpar com `-R`, nunca desligar o StrictHostKeyChecking) e a leitura do túnel `-L` (`porta_local:host_remoto:porta_remota`); complementa os cheat sheets de terminal/editor (`git-commands`/`bash-shortcuts`/`tmux-cheatsheet`/`vim-cheatsheet`/`devtools-shortcuts`) e os geradores de infra (que não cuidam do acesso remoto) — é o primeiro item de SSH/terminal remoto do projeto, que não tinha nada de ssh/scp
- DevOps: Comandos OpenSSL — `/devops/openssl-commands` (cheat sheet pesquisável do `openssl`, o canivete suíço de TLS/certificados/chaves do terminal — 44+ entradas em 7 categorias — chaves (`genpkey` RSA/EC com `-pkeyopt`, `genrsa [com -aes256]`, `ecparam -genkey -noout`, `rsa`/`pkey` extraindo a `-pubout` e inspecionando com `-text -noout`/`-check`), CSR (`req -new`, chave+CSR de uma vez com `-nodes`, inspeção `-text`, `req -verify` e `-pubkey`), certificados (autoassinado `req -x509` pra teste local, `x509 -text`/`-dates`/`-enddate`/`-ext subjectAltName`/`-fingerprint`/`-serial`/`-pubkey`, e `crl` de revogação), conversão & formatos (PEM↔DER nos dois sentidos com `-inform`/`-outform`, `pkey -outform der`, `pkcs12 -export` e extração com `-nodes`, `pkcs8 -topk8` e `crl2pkcs7` pra `.p7b`), validação (`verify -CAfile`/`-untrusted`/`-CApath`, e o `ca` que assina o CSR), TLS na prática (`s_client` com `-connect`/`-servername`/`-showcerts`/`-tls1_2`/`-brief`/`-verify_return_error`, `s_server` sobe um TLS de teste, e o pipe `s_client | x509` resumindo o cert de um host) e hash & encriptação (`dgst -sha256`, assinar `-sign`/verificar `-verify`, `pkeyutl -encrypt`/`-decrypt`, `enc -aes-256-cbc -pbkdf2` e `openssl rand` pra tokens) —, cada entrada com o comando em `<code>` (multi-linha com quebra `\`), categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem de resultados e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert destaca que `openssl` é uma coleção de subcomandos independentes (cada `openssl X` tem flags próprias), a pegadinha PEM vs DER — o mesmo cert existe nos formatos (o texto com `BEGIN/END` pra copiar vs o binário) — chaves sem senha são o segredo do sistema, e o `-noout` quieto que evita o dump do cert no stdout; complementa os geradores de config (`caddyfile-generator`, `docker-compose-generator`) — é o primeiro item de OpenSSL/certificados do projeto, que não tinha nada de criptografia diária via CLI; 100% client-side, nada sai do navegador)
- DevOps: Gerador de docker-compose.yml — `/devops/docker-compose-generator` (monta o arquivo `docker-compose.yml` de uma stack a partir de um formulário: lista de serviços com toggle Imagem/Build — imagem via AutoComplete com sugestões comuns (node:22-alpine, postgres:16-alpine, nginx:alpine...) ou `build.context` com tag de imagem opcional —, `container_name`, `restart` (no/always/on-failure/unless-stopped), `command`, portas (`- "host:container"` com a mesma regex de validação `PORT_RE` que alimenta o aviso), variáveis de ambiente em pares CHAVE/valor, volumes livres (bind `./origem:/destino` ou nomeado `pgdata:/...`, com o bloco `volumes:` no rodapé deduzido automaticamente das entradas que não começam com `.` nem `/`), `depends_on`, `networks` e bloco global `networks:` com driver (bridge-host-none-overlay); presets de um clique — API Node + Postgres, Full-stack (web+api+db com duas redes separando front/back), estático Nginx e Em branco —; saída em `<pre>` com a string YAML montada linha a linha com indentação de 2 espaços e scalars que o YAML leria como número/bool (portas, valores com espaço) envolvidos em aspas via `JSON.stringify`; avisos em Alert com a mesma lista de warnings do build (serviço sem nome pulado, sem imagem nem build, porta em formato inesperado, volume sem `:`, depends_on/network apontando pra id que não existe); Alert explicando que `depends_on` só ordena o `up` e não espera prontidão (healthcheck + `condition: service_healthy` para espera real) e que porta é sempre `host:container`; o algoritmo-fonte (`buildCompose`) é exibido na própria página num Collapse via `buildCompose.toString()`; complementa o `dockerfile-generator` (imagem individual) e o `caddyfile-generator`, que não cuidam do orquestrador — é o primeiro item que gera o compose multi-serviço; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Comandos Vim — `/references/vim-cheatsheet` (cheat sheet pesquisável do `vim`, o editor de terminal que mora em todo Linux — mesmo nas boxes mais peladas, onde é o único editor disponível —: 70+ entradas em 8 categorias — básico (modos, entrar/voltar do insert `i/I/a/A/o/O`, salvar/fechar `:w`/`:wq`/`:q`/`:q!`), movimento (hjkl, w/b/e por palavra, `0`/`^`/`$` na linha, `gg`/`G`, `%` de parêntese, `fX`/`tX` com `;`/`,` repetindo), edição (`x`/`X`, `dd`, `dw`/`daw`, `cc`/`cw`, `rX`, `u`/`Ctrl-r`, o `.` que repete a última mudança, `yy`/`p`/`P`), busca & substituição (`/`/`?`, `n`/`N`, `*`/`#` sob o cursor, `:s`/`:%s/.../g` e a variante `:gc` com confirmação) e modo visual (`v`/`V`/`Ctrl-v`/`gv` com `d`/`y`/`>`/`<`/`~` na seleção), janelas & abas (`:sp`/`:vsp`, `Ctrl-w` + setas, `:tabnew`/`gt`/`gT`, buffers `:ls`/`:bn`/`:bp`), registradores & macros (prefixo `"a`, `@a`/`@@`, gravação `qa...q` e `:'<,'>normal @a` pra "uma macro pra N linhas da seleção") e configuração (`:set number`/`relativenumber`/`paste`/`mouse=a`/`tabstop=4`/`expandtab`/`ignorecase`/`smartcase`, `:syntax on`, `:source ~/.vimrc`, `:help`) —, cada entrada com o comando em `<code>`, categoria em Tag colorida e descrição PT/EN, busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert destaca os 4 modos (NORMAL/INSERT/VISUAL/COMANDO) e a gramática "ação + alvo + contagem" (`3dd`/`d2w`) com o `.` como "fazer uma vez, repetir pra sempre", e as pegadinhas clássicas — `:q` RECUSA a sair com alterações, `u` desfaz vs. `Ctrl-r` refaz, e o `~/.vimrc` só é lido na abertura (`:source` ativa sem fechar) —, além do fato de que os movimentos não param no vim: tmux (modo copy), VS Code (extensão Vim) e Chrome (Vimium) reusam a mesma gramática; complementa os cheat sheets de terminal/editor (`/references/bash-shortcuts`, `/references/tmux-cheatsheet`, `/references/vscode-shortcuts`, `/references/devtools-shortcuts`) — é o primeiro item do projeto sobre editor vi/Vim; 100% client-side, nada sai do navegador)
- Ferramenta: Gerador de Changelog — `/devops/changelog-generator` (cola a saída do `git log` ou de um PR e vira um changelog Markdown agrupado por tipo — pronto pra release notes ou README; cada linha é lida como `<hash?7-40> tipo(escopo)!: descrição` via duas regex — `HASH_PREFIX_RE` engole o hash curto/longo opcional e `COMMIT_RE` captura tipo/escopo/breaking/subject —, com tipos normalizados por alias (`ci` → `build`, `deps`/`revert` → `chore`) e linhas que não casam o formato convencional agrupadas em `Outros`; o Markdown sai na ordem canônica das seções com contagem por tipo e um bullet por commit no formato `- **escopo**: assunto 💥 (hash-curto)` (o `!` liga o emoji de breaking change), com switch pra incluir ou omitir o hash curto; estatísticas de commits/breaking/outros, distribuição por tipo em Tags coloridas, botão de copiar e exemplo de um clique; complementa o `commit-message-generator` (monta UMA mensagem a partir de um formulário) — é o primeiro item que agrega uma lista de commits em release notes; o algoritmo-fonte é exibido na própria página; 100% client-side, nada sai do navegador)
- Ferramenta: Formatador e Validador de TOML — `/tools/toml-formatter` (cola um TOML — `pyproject.toml`, `Cargo.toml`, config de servidores — e formata com indentação 2/4, minifica ou valida, 100% via tokenizer + parser próprios em `src/utils/tomlFormatter.js` que nunca reutilizam o whitespace original: strings simples/literais/multilinha `"""`/`'''` são varridas intactas (um `#` dentro de string não vira comentário), comentários `#` são tokens à parte, e os tokens estruturais `=`/`[`/`]`/`{`/`}`/`,` alimentam um parser em statements — tabela `[a.b]`, array de tabelas `[[...]]`, par `chave = valor` com chave pontilhada, e valor recursivo que aceita escalar (string, número com `_` separador e hex/octal/binário, float com expoente e `inf`/`nan`, datetimes ISO-8601 e hora solta, `true`/`false`), tabela inline `{ a = 1 }` e array multilinha que pode ter virgula à direita; erros de sintaxe apontam a chave traduzida + linha/coluna (string sem fechamento, array/tabela inline/header sem fechador, `=` esperado, chave inválida, valor desconhecido, quebra de linha em header); o re-emitidor `renderStatements` tem dois modos — pretty (cada par `chave = valor`, tabelas separadas por linha em branco, arrays multilinha um item por linha no indent escolhido) e min (compacto `chave=valor`, inline tables `{k="v"}`, arrays `[1,2,3]`, sem espaços, com switch para manter ou remover comentários) —, com estatísticas na saída: bytes de entrada/saída, economia % no minify e contagem de tabelas/chaves/inline tables/arrays; 4 exemplos de um clique (`pyproject.toml`, `Cargo.toml` com `[[...]]` e tabela inline de features, Dockerfile-style e números&datas); Alert com as pegadinhas do TOML — valor sempre precisa de aspas enquanto chave `[A-Za-z0-9_-]` não (um número puro como par é válido `32 = "x"`, como valor não), string literal crua `'...'` não faz escape, cada tabela é definida uma vez na árvore, e datas sem timezone/horas soltas são valores válidos —; o algoritmo-fonte (tokenizeToml → parseDoc → renderStatements) é exibido na própria página num Collapse; complementa a família de formatadores (json/xml/css/sql), nenhum dos quais entendia a sintaxe de tabelas `[a.b]` — é o primeiro formatador de TOML do projeto; 100% client-side, nada sai do navegador)
- Ferramenta: Conversor de HTML → Markdown — `/tools/html-to-markdown` (converte um trecho de HTML colado — artigo, e-mail renderizado, trecho do DevTools — em Markdown pronto pra colar em PR, README, Notion ou chat de IA, usando o parser do navegador (`DOMParser`): HTML malformado é corrigido como o browser faria e `<script>`/`<style>`/`<svg>` são descartados, então a saída reflete a árvore renderizada, não o texto digitado. O conversor re-serializa em blocos: `h1..h6` viram `#`, `p` vira parágrafo (com dois espaços no fim da linha convertendo `<br>` em hard break GFM), `ul`/`ol` viram listas com sublista aninhada em 2 espaços por nível e atributo `start` respeitado, `pre` vira bloco de código delimitado em crases que preserva quebras de linha, `blockquote` prefixa cada linha de cada bloco interno com `> `, `hr` vira `---` e `table` vira a tabela GFM com linha separadora `---` e escape de `|` nas células; e em inline: `strong`/`b` viram `**…**`, `em`/`i` viram `*…*`, `s`/`del` viram `~~…~~`, `code`/`kbd`/`samp` viram crases, `a` vira `[texto](href "título")`, `img` vira `![alt](src)`, `sup`/`sub` viram `^`/`~` e `br` quebra a linha, enquanto caracteres de marcação no texto (`*`, `_`, `[`, `]`, `\``, `\`) são escapados pra não virarem sintaxe. 3 exemplos de um clique (artigo completo com imagem e lista ordenada `start=3`, tabela + lista aninhada, citação + código), estatísticas de linhas/blocos/caracteres da saída e botão de copiar; Alert com as pegadinhas — o `DOMParser` normaliza como navegador (tag sem fechamento fecha, script/style são descartados), a saída é GFM (tabela exige linha separadora `---`, que editores antigos não renderizam) e múltiplos espaços / `&nbsp;` colapsam num espaço só; o algoritmo principal é exibido na própria página num Collapse; complementa o `/tools/html-to-jsx-converter` (que também parseia HTML com `DOMParser`, mas emite JSX) e o `/tools/markdown-previewer` (que faz Markdown → HTML) — nenhum dos dois cobre o sentido HTML → Markdown)
- Front-end: Gerador de clip-path (Polygon) — `/frontend/clip-path-generator` (monta o recorte `clip-path: polygon(...)` de um elemento arrastando os vértices direto no preview — cada ponto vira uma dupla `x% y%`, mapeada do cursor pra coordenadas 0..100 via `getBoundingClientRect` num listener global de `pointermove` que entra com o `pointerdown` no ponto e sai no `pointerup` —, com o contorno e os pontos numerados desenhados por cima num `<svg viewBox="0 0 100 100" preserveAspectRatio="none">` com `vector-effect: non-scaling-stroke`, preview real do recorte sobre checkerboard (gradiente + texto central, o CSS da página e o do preview compartilham o mesmo `buildPolygon`); 8 formas prontas de um clique — diagonal, triângulo, entalhe à direita, canto cortado, relâmpago, hexágono e pentágono regulares gerados por `ring(n)` (polígono de N lados centrado) e estrela de 5 pontas via `star(n, inner)` alternando raio externo 50/interno 22 —, espelhar horizontal/vertical (`x → 100−x`), botão de adicionar vértice que acha a aresta MAIS LONGA e insere o ponto médio dela (`insertAtLongestEdge`), vértice removível com mínimo de 3, e edição numérica de cada ponto por InputNumber X/Y com contagem de vértices; saída `clip-path: polygon(0% 0%, ...);` com botão de copiar; Alert com as pegadinhas — `clip-path` corta a PINTURA, não a caixa (o elemento continua ocupando a área original pra layout e eventos, então um link "cortado" ainda recebe clique fora do visível), porcentagens relativas ao border box (o polígono se adapta a qualquer tamanho e corta também bordas/box-shadow), as outras formas `circle()`/`ellipse()`/`inset()`/`path()` (curvas/arredondados, que polígono não tem) e a falta de transição entre formas distintas (não interpola `polygon()`→`circle()`, a mudança salta); o algoritmo-fonte (insertAtLongestEdge + ring + star) é exibido na própria página num Collapse; complementa o `/frontend/border-radius-generator`, que arredonda cantos — é o primeiro item do projeto que corta a forma do elemento (nenhuma das ferramentas de CSS anteriores mexiam em `clip-path`); 100% client-side, nada sai do navegador)
- Front-end: Gerador de Keyframes (CSS Animation) — `/frontend/keyframe-generator` (monta a regra `@keyframes` de uma animação visualmente: cada frame define a posição em % e os valores de opacidade + transform — translate X/Y, scale e rotate — por sliders independentes, com "adicionar frame" que encaixa o novo stop no MAIOR intervalo entre frames existentes interpolando as propriedades dos dois vizinhos (`insertStop`), frame removível com limite de 2, e configurações globais — duração (100–5000 ms), easing (as 5 keywords CSS + dois cubic-bezier de overshoot/spring), iterações 1/∞, direção (normal/reverse/alternate/alternate-reverse) e fill-mode (none/forwards/backwards/both) — que viram o shorthand `animation: devtools-kf-anim 1400ms ease-in-out infinite normal none;`; o preview é um quadrado em gradiente que toca um `@keyframes` de verdade injetado na página (mesmo `ANIM_NAME` do CSS de saída), com restart automático a cada mudança de frame/configuração enquanto toca (remount via `key` + `runId` no `useEffect` — o remontar força o navegador a recomeçar a animação do zero), switch Play/Pausa + botão Repetir, timeline visual com marcadores na posição de cada frame e tags `%`; saída em `<pre>` com o bloco `@keyframes` pronto e o shorthand `animation:` separados, botão de copiar; Alert com as regras que mais importam — animar só `transform`/`opacity` (o par que roda no compositor/GPU; width/height/layout por frame trava), a ordem das funções do transform é da esquerda pra direita (`translate → scale → rotate` preserva a distância), frame repetido no mesmo % vence o anterior e `0%`/`100%` ausentes recebem o estado-base do elemento, e o easing vale por trecho (um `animation-timing-function` dentro do frame sobrepõe o easing global naquele intervalo); o algoritmo-fonte (buildKeyframes + insertFrame interpolando vizinhos) é exibido na própria página num Collapse; complementa o `cubic-bezier-editor`, que só modela a curva de easing — é o primeiro item que gera o bloco de `@keyframes` + shorthand de `animation` completos; 100% client-side, nada sai do navegador)
- Front-end: Gerador de Meta Tags (SEO / Open Graph) — `/frontend/meta-tags-generator` (monta o bloco de `<head>` de uma página — title, description, canonical, theme-color e o conjunto completo de Open Graph e Twitter Card — a partir de um formulário, com a regra central "cada tag só entra se o campo tem valor" e a necessidade de omissão de tags vazias; o Open Graph espelha title/description/canonical (`og:title`/`og:description`/`og:url`), o `og:type` selecionável (website/article/profile/product/video.movie/music.song/blog) liga campos extras quando é "article" (`article:published_time` ISO 8601 e `article:author`), o Twitter Card herda os mesmos valores do OG e a opção `twitter:card` escolhe summary (só texto) ou summary_large_image; opções de incluir charset+viewport, theme-color (com color picker) e o handle `twitter:site`; saída em `<pre>` escuro estilo terminal com contagens de linhas e bytes e botão de copiar, com `escAttr` que escapa `& " < >` pra nenhum valor digitado quebrar o atributo; contadores de título (`title 48/60`) e descrição (`description 152/160`) com Tag verde/vermelha conforme a faixa recomendada (60 e 160 caracteres); 3 modelos de um clique (landing, artigo de blog, app), botão de restaurar e `nome do site`/`og:locale` configuráveis (com `og:locale:alternate`); prévia visual do cartão de compartilhamento (domínio, título, site e ≤2 linhas da descrição, sem baixar a imagem) mais um Alert sobre o ecossistema: og espelha os meta crus quando faltam tags, Twitter herda og:*, og:image sempre URL absoluta que o crawler baixa na hora de renderizar o cartão, e canonical ≠ noindex; o algoritmo-fonte (`escAttr` + `buildHeadTags`) exibido na própria página num Collapse; complementa o `/security/robots-txt-generator`, que cuida do controle de crawlers em arquivo — nenhum dos dois gera as tags de SEO/cabeçalho (é o primeiro item do projeto no assunto de SEO/OG)
- Rede: Explorador IPv6 (formas + sub-rede) — `/network/ipv6-explorer` (cola um endereço IPv6 com ou sem `/prefixo` e a página expõe as formas expandida (minúscula/maiúscula) e a comprimida canônica RFC 5952 — omite os zeros à esquerda de cada grupo (`0db8` → `db8`) e comprime a maior sequência de grupos zero (empate → 1ª) —, o layout dos 128 bits em 8 hextets de 16 com o corte rede/host colorido, o tipo do endereço por prefixo canônico (`::` não especificado, `::1` loopback, `::ffff:0:0/96` IPv4-mapeado com o IPv4 embutido revelado, `::/96` IPv4-compatível obsoleto, `64:ff9b::/96` NAT64, `2001:db8::/32` documentação, `ff00::/8` multicast com escopo, `fe80::/10` link-local, `fec0::/10` site-local obsoleto, `fc00::/7` ULA, `2002::/16` 6to4, `2001:0000::/32` Teredo, `2001:2::/48` benchmarking, unicast global `2000::/3`) e, quando há `/N`, o bloco da sub-rede calculado em BigInt puro (rede, último endereço, tamanho 2^(128−N) em decimal com separadores de milhar); parser tolerante a maiúsculas e a IPv4 embutido só nos 32 bits finais (`::ffff:192.168.1.1` revela o dotted `192.168.1.1`), 12 exemplos de um clique e um Alert com o essencial (8 grupos de 16 bits, `::` comprime a maior corrida de zeros uma única vez, IPv6 não tem máscara — sempre prefixo /N — e um IPv4 inteiro cabe nos últimos 32 bits, por isso túneis mostram `::ffff:...`); o algoritmo-fonte (parseIpv6, compressedForm, classify) é exibido na própria página num Collapse; complementa o `/network/subnet-calculator`, que é exclusivo IPv4 — é o primeiro item do projeto que cobre IPv6; 100% client-side via `BigInt`, nada sai do navegador)
- Front-end: Gerador de Filtros CSS — `/frontend/css-filter-generator` (monta a regra CSS `filter` visualmente, com as funções em ordem canônica e prontas pra colar: sliders de brilho/contraste/saturação (0–300%), matiz (0–360°), escala de cinza/sépia/inverter/opacidade (0–100%) e blur (0–20px), mais drop-shadow opcional com X/Y/desfoque e cor; o agregador `buildFilterParts` descarta cada função no valor neutro — `blur(0)`, `brightness(100%)`, `saturate(100%)` etc. somem do CSS — e devolve a lista em ordem fixa, `none` quando nada está ativo; preview ao vivo sobre checkerboard de uma imagem-exemplo SVG gerada inline (céu em gradiente, sol, montanhas e triângulo), com linha de texto real estilizada pela mesma regra e contador de funções ativas; 7 efeitos prontos de um clique — Original, Sépia, Noir (P&B), Vívido, Frio, Vintage e Invertido —, botão de restaurar e saída `filter: ...;` com copiar; um Alert cobre o que mais pega na prática: a ordem importa (esquerda→direita, `sepia(1) hue-rotate(90deg)` ≠ o inverso), neutro é descartado pra CSS curto, custo de renderização (filter cria camada de composição própria), `drop-shadow` segue o contorno alpha e não o retângulo do elemento (≠ box-shadow) e `backdrop-filter` afeta o que está atrás, não o elemento; o algoritmo-fonte é exibido na própria página num Collapse; complementa a família de geradores de CSS (gradient, box-shadow, border-radius, cubic-bezier), que não cobria `filter` — primeiro item da categoria Front-end que processa imagem; 100% client-side, nada sai do navegador)
- Referências: Atalhos do DevTools do Navegador — `/references/devtools-shortcuts` (cheat sheet pesquisável dos atalhos de teclado do Chrome DevTools — o depurador embutido de todo navegador Chromium (Edge, Opera, Brave...) —: mais de 50 entradas em 8 categorias — abrir (F12/Ctrl+Shift+I como painel da última vez, Ctrl+Shift+J pulando direto pro Console, Ctrl+Shift+C pra inspecionar), globais (Ctrl+Shift+M Device Mode, Ctrl+Shift+D alterna o dock, Command Menu `Ctrl+Shift+P` que roda qualquer comando, Ctrl+Shift+F busca em todos os recursos, Ctrl+O abre arquivo no Sources, reload/hard reload), painel Elements (seta ↑/↓/←/→ navegam o DOM, `H` esconde o elemento, Enter/F2 editam atributos/HTML, Ctrl+Z/Ctrl+Shift+Z desfaz/refaz), painel Styles (Ctrl+clique no valor pula pra declaração, Shift+clique no swatch cicla o formato da cor, cliques no valor com ↑/Shift+↑/Ctrl+↑/Alt+↑ incrementam 1/10/100/0.1), painel Sources/debugger (F8 pausa/retoma, F10 step-over, F11 step-into, Shift+F11 step-out, Ctrl+clique na linha roda até ela, Ctrl+B/Ctrl+Shift+B breakpoint e breakpoint condicional, Ctrl+G vai pra linha), Console (Ctrl+` limpa, Ctrl+L e Shift+Enter, navega de histórico, autocomplete Tab/Escape), Network (Ctrl+E gravação, Ctrl+R grava um reload, `R` repete o XHR, Escape fecha detalhes) e gravação/tabs de código (Performance/Memory/Recorder com Ctrl+E, salvar com Ctrl+S) —, cada entrada com a combinação **Windows/Linux E Mac lado a lado** em `<code>`, Tag de cor de categoria e descrição PT/EN; busca que casa atalho ou descrição, filtro por categoria em botões de rádio com contagem de resultados e botão de copiar a lista filtrada como tabela Markdown; um Alert destaca o trio de abertura do dia a dia (F12/⌘⌥I, ⌘⌥J console, ⌘⌥C inspecionar), o Command Menu `?**` como "coringa" que executa qualquer comando do DevTools mesmo sem atalho, e o incremento de valores CSS por atalho no Styles; complementa o `/references/vscode-shortcuts` (fonte de atalhos de editor) e o `/references/bash-shortcuts` (shell) — é o primeiro item do projeto focando no depurador de navegador, que não tinha nada disso; 100% client-side, nada sai do navegador)
- Referências: Comandos tmux — `/references/tmux-cheatsheet` (cheat sheet pesquisável do tmux, o terminal multiplexer que roda dentro do SSH e deixa o trabalho sobreviver à queda da conexão — 54 entradas em 7 categorias (sessões, janelas, panes, copiar & colar, configuração, barra de status e automação/scripts) cobrindo tanto os atalhos de prefixo `^b` (`d` desanexa mantendo a sessão viva, `%` divide lado a lado, `"` empilha, setas mudam a pane ativa, `z` maximiza, `` `[` `` entra em copy-mode, `` `]` `` cola, `Space` gira o layout, `=` escolhe buffer) quanto os comandos externos `tmux ...` (`new -A -s` anexa-ou-cria, `ls`, `attach`, `kill-session`, `new-session -d` subindo sessão em background já rodando um comando, `send-keys`, `capture-pane`, `display-message`); também cobre as configurações do `~/.tmux.conf` (`set -g mouse on`, `set -g prefix C-a`, `base-index 1`, `mode-keys vim`, `status-*`); cada entrada com o comando em `<code>`, categoria em Tag colorida e descrição PT/EN; busca que casa comando ou descrição, filtro por categoria em botões de rádio com contagem de resultados e botão de copiar cada comando ou a lista filtrada como tabela Markdown; um Alert explica o esqueleto em camadas servidor→sessão→janela→pane, o papel do prefixo `^b` como "elevador" que engole o atalho antes de ele chegar ao programa rodando na tela, e as pegadinhas que mais pegam — mouse desabilitado por padrão (`set -g mouse on` + `tmux source-file ~/.tmux.conf`) e o conflito do prefixo `^b` com o insert-mode de vim/shell (muitos trocam pra `C-a`) —, além do `tmux kill-server` que derruba tudo de uma vez; complementa o `/references/git-commands` (git) e o `/references/bash-shortcuts` (atalhos do shell), que tratam de ferramentas separadas — é o primeiro item de terminal multiplexor do projeto, que não tinha nada de tmux; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de JavaScript (ES6+) — `/references/javascript-cheatsheet` (referência pesquisável dos recursos modernos do JS do dia a dia — 56 entradas em 9 categorias — variáveis & escopo (const/let, `??`, `?.`, swap, ternário), strings & templates (template literal com `${...}`, multilinha, includes/replaceAll/padStart/split/trim), arrays (map/filter/reduce/find/some/every/flat/Set-único/sort numérico/spread/Array.from/at(-1)), objetos (destructuring com rename, spread merge, chave computada, entries/fromEntries, shorthand, delete), funções & arrow (retorno implícito, retornar objeto com `({ })`, default params, rest, method shorthand, `this` lexical), async & promises (async/await, Promise.all, try/catch, allSettled, helper delay), módulos (import nomeado/default, export, dynamic import), coleções (Map, Set, BigInt, crypto.randomUUID) e números (Number.isNaN vs global, parseInt com radix, toFixed/Math.*) —, cada entrada com o código-fonte em bloco `<pre>` pronto pra colar, descrição PT/EN, botão de copiar por snippet e a categoria em Tag colorida; busca que casa código ou descrição, filtro por categoria em botões de rádio com contagem, contador de resultados e botão de copiar a lista filtrada como documento Markdown com bloco `js` por item; Alert com as armadilhas que mais pegam — `sort()` lexicográfico sem comparador, arrow não tem `this` próprio, `==` coage, zona morta temporal de let/const e spread como cópia rasa; é o primeiro item de referência de linguagem de programação do projeto — até aqui só havia cheat sheets de ferramentas (git/docker/kubectl/sql/bash) e protocolos, nenhum de linguagem; 100% client-side, nada sai do navegador)
- Referências: Cabeçalhos HTTP — `/references/http-headers` (cheat sheet pesquisável dos cabeçalhos HTTP do dia a dia — ~60 entradas em 10 categorias — negociar (Accept*/Vary/Accept-Ranges), conteúdo (Content-Type/Length/Encoding/Language/Location/Range/Content-Disposition), cache (Cache-Control/Expires/Age/ETag/Last-Modified), condicionais (If-Match/If-None-Match/If-Modified-Since/If-Unmodified-Since/If-Range), CORS (Origin e os 9 Access-Control-* de preflight e resposta), autenticação (Authorization/WWW-Authenticate/Proxy-*), cookies (Set-Cookie/Cookie), segurança (HSTS/CSP/X-Frame-Options/nosniff/Referrer-Policy/Permissions-Policy/COOP/CORP), transporte (Connection/Keep-Alive/chunked/Upgrade) e roteamento/metadados (Host/Date/User-Agent/Referer/Location/Retry-After/Allow/Server mais os X-Forwarded-* e Forwarded) —, cada um com tag direcional Req/Res/Ambos colorida, valor de exemplo em monospace, o que faz e botão de copiar `Nome: exemplo`; busca que casa nome/valor/descrição/categoria, filtro por categoria em botões de rádio com contagem, legenda do sentido dos headers, botão de copiar a lista filtrada como tabela Markdown; Alert explica que cabeçalhos trabalham em pares (ETag → If-None-Match vira 304, Set-Cookie → Cookie), a pegadinha do CORS — o servidor precisa devolver Access-Control-Allow-* coerentes tanto no preflight OPTIONS quanto na resposta real — e que prefixo X- (X-Forwarded-For) é padrão de facto, não da spec, então não vale confiar sem validar a cadeia de proxies, e que headers de segurança (HSTS/CSP/X-Frame-Options/nosniff) deveriam ser default em qualquer API pública; complementa o `/references/http-methods` (os métodos) e o `/references/http-status-codes` (as respostas) — fecha o trio de referências HTTP do método ao header; 100% client-side, nada sai do navegador)
- Ferramenta: Conversor de Unidades Físicas — `/tools/units-converter` (converte valores do dia a dia entre unidades físicas em tempo real, digitando em qualquer campo e vendo todos os outros se atualizarem: 8 categorias — comprimento (mm/cm/m/km/pol/pé/jarda/milha/milha náutica), massa (mg/g/kg/t/onça/libra/stone), temperatura (°C/°F/K), velocidade (m/s, cm/s, km/s, km/h, mph, nó, ft/s), área (mm²…km², polegada/pé quadrados, hectare/acre), volume (mL/L/m³/pol³/pé³, onça fluida e galões US/UK), tempo (ms a ano) e dados (B, KB…PB decimais ×1000 E KiB…PiB binários ×1024 lado a lado) —, cada categoria definida por unidades lineares em relação a uma unidade-base: `base = v × factor + offset`, em que só a temperatura usa offset ≠ 0 (por isso 32 °F vira 0 °C indo e voltando por kelvin); fatores com os valores exatos das definições (polegada = 25,4 mm, libra = 453,59237 g, milha = 1.609,344 m, milha náutica = 1.852 m, nó = nmi/h), mês = ano/12 de 365,25 d; formatação com ~12 algarismos significativos que esconde artefatos de ponto flutuante (1 ft = 0,3048, sem 0,30480000000000004) e inteiros grandes sem notação até 1e21; campo ativo mostra o que você digitou enquanto digita mesmo durante estados inválidos intermediários (ex.: "1."), entrada tolerante a vírgula decimal, botão de copiar por campo; Alert explica a pegadinha dos prefixos de dados (500 GB decimal ≈ 465 GiB binários, o GB do disco vs o GiB do sistema) e por que temperatura não é "fator simples"; o algoritmo-fonte é exibido na própria página num Collapse; complementa o `css-unit-converter`, que só cobre unidades de tela (px/rem/vw) e o `base-converter`, que é base numérica — nenhum dos dois cobre unidades físicas de mundo real; 100% client-side, nada sai do navegador)
- DevOps: Gerador de Caddyfile — `/devops/caddyfile-generator` (monta um `Caddyfile` — o arquivo de configuração do Caddy, o servidor web com HTTPS automático via Let's Encrypt — a partir de um formulário: domínio(s) (vários nomes num bloco, `example.com, www.example.com`), função do servidor em três modos — `reverse_proxy` (com matcher de caminho opcional pra prefixar só uma rota, ex. `/api/*`), `file_server` estático (com `root * + file_server`) e `php_fastcgi` (root + php_fastcgi + file_server na ordem canônica) — e um conjunto de opções que viram diretivas: `encode zstd gzip`, bloco `header` que remove `Server` e injeta headers de segurança (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`), `tls internal` (cert CA local autoassinado pra hosts internos) e `basic_auth` (usuário + hash bcrypt, com o lembrete de gerar via `caddy hash-password`); campo de linhas adicionais verbatim (ex.: `redir`) e modelos de um clique — reverse proxy Vite/Node, site estático, subdomínio de API e PHP-FPM; saída em monospace com contagem de linhas/bytes e botão de copiar; o builder junta os domínios separados por vírgula num único bloco `site`, monta as diretivas por seção e concatena o extra sem indentação manual; Alert explica que Caddy é self-managed de TLS (por isso "https" não aparece no arquivo) e que num deploy comum ele fica na frente repassando a um container interno — como este próprio devtools —, com dicas de `caddy validate`/`caddy reload`; o algoritmo-fonte é exibido na própria página num Collapse; complementa o `dockerfile-generator` (gera o container) e o `gitignore-generator` no trio de infra-as-code — é o primeiro item da família a cobrir o proxy reverso/Caddy; 100% client-side, nada sai do navegador)
- Ferramenta: JSON → YAML — `/data/json-to-yaml` (cola um JSON — response de API, config, payload — e gera o YAML equivalente pronto pra colar em CI/CD, Compose, Ansible ou k8s; serialização própria 100% client-side: objetos viram mapeamentos em bloco indentado, arrays viram listas `- item` uma linha por item, e array dentro de array vira notação flow `[1, 2]` — válida e legível em vez de `- - 1` aninhado; o coração é a decisão de aspas `quotePlain`: um scalar só sai sem aspas quando o parser não leria outro tipo — números, booleans `true/false`, `null`, `yes/no/on/off` (booleans em YAML 1.1) e inícios reservados `- ? : , [ ] { } # & * ! | > ' " % @ \`` obrigam aspas duplas, assim como `:` seguido de espaço (ou no fim), `#` antes de espaço e espaços nas pontas — preservando os tipos de verdade do JS (`plan: "on"` fica string, `007` não vira 7); chaves com nome inválido/caractere especial saem citadas também; opções de indentação 2/4, prefixo `---` de início de documento e `null` como placeholder (`key:` vazio); objeto dentro de array recebe `- ` na primeira propriedade e as demais chaves alinhadas na coluna do travessão; ordem das chaves preserva a do `JSON.parse`; o YAML gerado foi validado publicamente com `yaml.safe_load` (PyYAML) em cenários indent 2/4, objetos-aninhados e arrays de arrays; o algoritmo-fonte é exibido na própria página num Collapse; complementa o `json-formatter` (formata JSON), o `json-to-sql` (emite INSERTs) e o `csv-markdown-table` — nenhum deles produz o formato YAML consumido por Compose/CI/Ansible/k8s)

## 2026-08-08
- Ferramenta: JSON → TypeScript — `/data/json-to-typescript` (cola um JSON de exemplo — response de API, mock, dump — e gera as `interface`/`type` do TypeScript que descrevem aquela estrutura: cada objeto vira uma interface nomeada, arrays viram `Tipo[]` (com união `(A | B)` quando os elementos têm formas diferentes e `unknown[]` quando vazios), chaves com nome inválido ou palavra reservada saem entre aspas, e formas idênticas são deduplicadas por uma assinatura de FORMA (`shapeSig`) que ignora valores — duas estruturas iguais com dados diferentes reutilizam a mesma interface em vez de gerar uma nova; opções de nome do tipo raiz (vira `export interface Root` no objeto-raiz ou `export type Root = ...` quando a raiz é array/primitivo, com o nome reservado antes pra interface de elementos não roubar o identificador), camelCase nas chaves (`user_id` → `userId`), prefixo `export` e `null` como `any`; singularização de nomes pra arrays ("products"/"tags" → `Product`/`Tag`, e nomes já no singular como "Root" ganham sufixo `Item`); é o primeiro item que gera código TypeScript, fechando a lacuna ao lado do `json-schema-generator`, que emite schema, e do `json-to-sql`, que emite INSERTs — nenhum dos dois gera os tipos que o frontend consome; o algoritmo-fonte (`typeOf` + `shapeSig` + `elementBaseName`) é exibido na própria página num Collapse; 100% client-side, nada sai do navegador)
- Ferramenta: Calculadora de CRC — `/tools/crc-calculator` (calcula checksums `CRC-8`/`CRC-16`/`CRC-32` de texto ou de bytes hex — 9 algoritmos do catálogo RevEng (CRC-8, CRC-8/MAXIM 1-Wire, CRC-16/MODBUS, CCITT-FALSE, XMODEM, KERMIT, CRC-32 ISO-HDLC, CRC-32/C Castagnoli e CRC-32/MPEG-2), cada um com a tabela de look-up de 256 entradas construída na hora via a variante refletida ou normal do polinômio (`refin` decide o shift right com polinômio espelhado ou shift left com top-bit; `refout` ≠ `refin` espelha o registrador antes do `xorout`); entrada em dois modos — texto (UTF-8) ou hex (tolera espaços/vírgulas/prefixo `0x` e aponta dígito ímpar ou inválido) — e tabela de resultados com CRC em hex e decimal, linha pra copiar por algoritmo e a descrição de onde cada variante é usada (ZIP/gzip/PNG chunk/Ethernet FCS pro CRC-32, iSCSI e Btrfs pro Castagnoli, Modbus RTU pro CRC-16/MODBUS, XMODEM/bootloaders, Bluetooth/X.25 e sensores 1-Wire); o diferencial de confiabilidade é a coluna "referência": o catálogo RevEng documenta o valor que cada algoritmo deve dar para a entrada ASCII `123456789` — clicar nesse exemplo acende um selo verde `OK` em todas as 9 linhas, provando que a implementação bate com a spec (valores canônicos: F4, A1, 4B37, 29B1, 31C3, 2189, CBF43926, E3069283, 0376E6E7 — validados com vetores conhecidos em teste); um Alert separa o conceito de checksum (pega erro acidental: ruído/bit flip) de hash criptográfico (não é resistente a ataque — pra adulteração use o `/tools/hash-generator` com SHA), e alerta a pegadinha clássica de nomes equivocados em variantes ("CRC-16" ≠ CCITT-FALSE ≠ XMODEM ≠ KERMIT, ordem de bytes e polinômios diferentes); o algoritmo-fonte (`reflect` + `makeTable` + `crcBytes`) é exibido na própria página; complementa o `hash-generator`, que cobre a família SHA mas não CRC; 100% client-side, nada sai do navegador)
- Referências: Cheat Sheet de Regex — `/references/regex-cheatsheet` (catálogo pesquisável da sintaxe de expressões regulares em JavaScript: 56 entradas em 9 categorias — âncoras, classes de caracteres, quantificadores (inclusive os preguiçosos), grupos e captura, retroreferências, lookaround, escapes, flags e receitas prontas —, cada uma com o token em `<code>`, o que ele casa e um exemplo do que o padrão encontra; resolve o símbolo que escapa da memória na hora (quantificador lazy? lookbehind?) e traz receitas do dia a dia — e-mail, data ISO, hora, URL, UUID, CPF, CNPJ, cor hex, slug, IPv4, JWT — em que o botão de copiar entrega o mesmo padrão em duas formas: literal `/.../flags` e `new RegExp(...)`; busca casa token/nome/descrição/exemplo em PT e EN, filtro por categoria em botões de rádio, contagem de resultados e botão de copiar a lista filtrada como tabela Markdown; um Alert lembra que "flavors" de regex não são idênticos (Python usa `(?P<nome>...)`, PCRE tem condicionais `(?(...))`, lookbehind de tamanho variável não vale em toda engine) e que a página segue o `RegExp` do JS moderno; complementa o `/tools/regex-tester`, que é o playground de teste e não cataloga a sintaxe; é o mais novo da família de referências; 100% client-side, nenhum dado sai do navegador)
- Front-end: Gerador de Onda SVG — `/frontend/svg-wave-generator` (monta ondas decorativas em SVG — o divisor clássico de seção de landing pages/hero/rodapé — com N camadas empilhadas, cada uma com cor, opacidade, amplitude e linha média próprias além do formato senoidal puro ou com harmônico em dobro que sobrepõe uma ondinha extra; amostragem em ~1 ponto por 3px (mínimo 64) gera path liso sem inchar o atributo `d`, cada camada fecha no fundo do SVG e a última da lista renderiza por cima; preview ao vivo sobre checkerboard, tamanho de canvas configurável, página flipped pra inverter topo↔base, presets de um clique (pôr do sol, oceano, harmônico, monocromático) e saídas prontas pra copiar — o SVG fonte e a regra CSS `background-image` com data URI — mais botão de baixar `.svg`; o algoritmo-fonte (`waveY` + `layerPath`) é exibido na própria página; complementa o `svg-placeholder-generator`, que gera image de placeholder retangular, não a forma orgânica de onda; 100% client-side, nada sai do navegador)
- Referências: Tabela ASCII — `/references/ascii-table` (consulta dos valores 0–255 de um byte: cada linha mostra o mesmo número nas três bases — decimal, hex e binário — com o caractere correspondente (ASCII imprimível 32–126 em destaque, controles 0–31 e 127 sem carácter) e a extensão Latin-1 128–255 num toggle; busca que casa o código em qualquer base (digitar `65`, `0x41` ou `01000001` acham o A) ou o nome, e filtro por categoria em botões de rádio com contagem (Todas/Controles/Letras/Dígitos/Símbolos/Latin-1); coluna extra "Escape" que mostra o token JS de cada byte (`\n`=10, `\t`=9, `\\x1B`=27, C0/C1 viram `\\xNN`), útil pra escrever a string certa em código; nome legível de cada caractere em PT/EN, tag `U+xx` nos de controle e botão de copiar a lista filtrada como TSV (dec/hex/bin/escape/nome); um Alert ensina a usar o mapa inverso — `"ABC".charCodeAt(0)` → 65 é o valor da coluna Dec, `String.fromCharCode(65)` → `'A'` — e lembra os grupos do dia a dia (`\\r\\n` CR/LF 13/10, `\\x00` NUL); o algoritmo-fonte (byteName + escapeToken + buildRows) é exibido na própria página; complementa o `unicode-inspector`, que olha textos/code points mas não o grid numérico do byte; 100% client-side, nada sai do navegador)
- Ferramenta: Cores ANSI (escapes de terminal) — `/devops/ansi-colors` (cola a saída colorida de um terminal — log, deploy, npm, git — com os `ESC` escapes e vê o texto renderizado com as cores reais aplicadas e o mesmo texto limpo, sem nenhum código, pronto pra copiar; também serve de referência completa de paletas: parser próprio de sequências CSI que normaliza `\e`/`\x1b`/`\u001b`/`\033` pro byte ESC, entende os SGR do dia a dia (0 reset, 1 bold, 2 dim, 3 itálico, 4 sublinhado, 7 reverse, 9 riscado), cores 30–37/90–97/40–47/100–107, `38;5;N` (paleta xterm) e `38;2;R;G;B` (truecolor, inclusive misturando 48;2 e 38;2 no mesmo escape), e descarta sequências de cursor/limpeza (`2J`, `1;1H`) contando-as como CSIs ignorados; preview em fundo escuro estilo terminal com tags de estatística (SGRs, CSIs ignorados, trechos), três exemplos de um clique (16 básicas, balé 256, truecolor) e três paletas clicáveis — básica 16 com o código SGR de cada swatch, o grid completo da xterm (cubo 6×6×6 + 24 cinzas) com número e cor de contraste automática, e truecolor com amostras RGB — além de botões que copiam o escape certo de cada cor; o algoritmo-fonte (tokenizer + applySgr + xterm→RGB) é exibido na própria página; complementa os que trabalham com texto, que não enxergam os bytes de cor; 100% client-side, nada sai do navegador) (playground visual pra montar a função de temporização de `transition`/`animation` CSS: dois eixos arrastáveis P1/P2 sobre um gráfico SVG — grade, eixos y=0 e y=1 destacados, retas de controle tracejadas e extremidades P0/P3 — com valor `cubic-bezier(x1, y1, x2, y2)` gerado em tempo real (4 casas) e botão de copiar; o coração é o solver do caminho inverso — dado o progresso p (0..1), achar o t de modo que X(t)=p e devolver Y(t) via Newton-Raphson com fallback de bissecção (monotonia garantida porque o CSS exige x1,x2 ∈ [0,1], enquanto y é livre pro "overshoot"/"back"); atalhos de um clique das 5 keywords CSS (linear/ease/ease-in/ease-out/ease-in-out) com seus valores exatos e curvas de efeito (ease-in-back, ease-out-back, ease-in-out-back, out-elastic-lite, snap-in); 4 InputNumbers sincronizados com os handles; preview animado que roda um retângulo num trilho com a curva gerada e um marcador vermelho percorrendo a própria curva B(t) — duração 100–4000ms e loop on/off; blocos de "valor pronto pra copiar" e uso num `transition` real; Alert explicando como ler a curva (inclinação = velocidade, y < 0 antecipa, y > 1 estoura) e que keywords são atalhos de curvas fixas; complementa os geradores de Box Shadow/GRadient/Border Radius do site, que geravam propriedades mas não curva de temporização; o algoritmo-fonte (`makeEasing`, Horner + Newton/bissecção) é exibido na própria página; 100% client-side, nada sai do navegador)
- DevTools: Gerador de Dockerfile — `/devops/dockerfile-generator` (monta um `Dockerfile` a partir de um formulário, na ordem convencional das diretivas — FROM → WORKDIR → LABEL → ENV → COPY → RUN → EXPOSE → USER → ENTRYPOINT/CMD —: imagem base com AutoComplete de imagens comuns (node:22-alpine, python:3.12-slim, nginx:alpine, golang, ubuntu, debian, alpine...), ENV uma KEY=VALUE por linha, linhas de COPY origem→destino com add/remove, comandos de RUN com atalhos de comandos comuns (npm ci, pip install, apt-get, go mod download...), par ENTRYPOINT/CMD com três modos cada — nenhum/exec/shell — onde o exec usa um mini tokenizer que separa argumentos por espaço respeitando aspas duplas, transformando "npm start" em `["npm","start"]`, e atalhos prontos de CMD/ENTRYPOINT; modelos de stack de um clique (Node.js, Python, Nginx estático, Go, Em branco) que preenchem o scaffold completo; toggle de comentários de seção; painel de avisos em tempo real (base vazia quebra o FROM, ENV sem `=`, porta não numérica, exec/shell sem argumentos, linha de COPY incompleta) e Alert quando sobra sem nenhum CMD/ENTRYPOINT; saída com contagem de linhas e bytes e botão de copiar; Alert de boas práticas explicando camadas/cache (padrão `COPY package.json package-lock.json ./` → `RUN npm ci --omit=dev` → `COPY . .`) e multi-stage; o algoritmo-fonte é exibido na própria página; complementa o `/devops/docker-commands`, que é cheat sheet de comandos e não gera o arquivo; 100% client-side, nada sai do navegador)
- Ferramenta: Gerador de Placeholder SVG — `/frontend/svg-placeholder-generator` (monta uma imagem placeholder como SVG — largura/altura, canto arredondado, fundo sólido ou gradiente diagonal de duas cores, cor do texto e texto central opcional (vazio = "L × A"), fonte auto proporcional ou fixa — e entrega pronta pra embutir em três versões com botão de copiar: o atributo `src` como data URI URL-encoded (`data:image/svg+xml;charset=utf-8,...`), a versão `base64` (sem lib: TextEncoder → bytes → btoa) e a regra CSS `background-image: url(...)`; preview ao vivo via `<img>`, exemplos de um clique (grade 320×240, avatar circular 120×120, capa OG 800×450, banner 1280×300), contagem de bytes de cada saída, um Alert com os cuidados de data URI (URL-encoded é menor que base64, que infla ~33%; `#` precisa permanecer codificado `%23`; data URIs não são cacheados como asset individual) e o algoritmo-fonte exibido na própria página; é a primeira ferramenta do projeto com área de imagem — não havia nada que gerasse imagens/placeholders, só gradientes/CSS; 100% client-side, nada sai do navegador)
- Ferramenta: Explorador de Float (IEEE-754) — `/tools/float-explorer` (digite um número e veja exatamente como ele é armazenado na memória: os 64 bits do `Number` nativo do JS divididos em sinal (1) / expoente (11, com o viés 1023) / mantissa (52), a classificação conforme a spec — normalizado, subnormal/denormal, ±0, ±Infinity, NaN —, o valor decimal EXATO que a máquina guarda (regra de ouro: `m × 2^(e−1023−52)` calculado com BigInt, por isso `0.1` vira `0.1000000000000000055511151231257827...` e `0.1 + 0.2` vira `0.30000000000000004`, com Alert explicando o caso clássico), a mesma vista em 32 bits (float32, de C/Python/GLSL, com nota de que a mantissa cai de 53 pra 24 bits efetivos e `0.1` vira `0.10000000149011612`), os vizinhos representáveis (prev/next por ±1 no padrão de bits, com a distância em 1 ulp), o hex em big-endian e os bits completos em monospace; exemplos de um clique (0.1, 0.3, `0.1+0.2`, π, `2^53`, `min subn.` 5e-324, `1e308`, Infinity, NaN, `-0`); o algoritmo-fonte (DataView + BigInt) é exibido na própria página num Collapse; complementa o `base-converter`, que trabalha com inteiros/BigInt e não enxerga floats — é o primeiro item que abre o interior binário dos números de ponto flutuante; 100% client-side, nada sai do navegador)
- DevOps: Calculadora de Permissões (chmod) — `/devops/chmod-calculator` (converte entre o modo octal e a notação simbólica `rwxr-xr-x` das permissões POSIX de arquivos e pastas, incluindo os bits especiais setuid/setgid/sticky — o quatro dígito opcional —; marque os checkboxes de cada grupo (dono/grupo/outros × leitura/escrita/execução) ou digite o octal e os dois lados sincronizam na hora, com a notação estilo `ls` mostrando o `s`/`t` maiúsculo quando um especial está ligado sem o `x` correspondente (`S`/`T`); gera o comando `chmod` numérico e o simbólico (`chmod u=rwx,g=rx,o=rx` com `+u+s`/`+g+s`/`+o+t`) prontos pra copiar, campo de arquivo/pasta que entra no comando, alerta explicando que cada grupo é a soma `r`=4/`w`=2/`x`=1 e como descobrir o modo atual com `stat -c %a`/`ls -l`, e tabela de modos comuns clicáveis (400/600/644/755/4755/2775/1777...) que aplicam o modo; complementa o `env-tool`, que valida `.env` mas não trata permissões; 100% client-side, nada sai do navegador)
- Referências: Cheat sheet de comandos kubectl — `/devops/kubectl-commands` (referência pesquisável dos comandos `kubectl` mais usados — kubeconfig/contextos, cluster e inspeção, namespaces e escopo, pods, deployments/statefulsets/daemonsets e rollout, logs/exec/debug, services/Ingress/port-forward, ConfigMaps/Secrets, filtros e formato de saída, e operação/manutenção (apply/diff/drain/autoscale/CronJob) — cada um com o comando em `<code>` e descrição PT/EN, categoria em Tag colorida com filtro por botões de rádio com contagem e busca que casa o comando ou a descrição; um Alert explica o esqueleto `kubectl <verbo> <tipo> [nome] [-n namespace] [-o formato] [flags]`, os aliases de tipo (po/deploy/svc), o `-n` de namespace e a pegadinha do `kubectl get all`, que não lista tudo — o caminho certo pra descobrir campos de um recurso é `kubectl explain <tipo>`; botão de copiar cada comando e copiar a lista filtrada como tabela Markdown; primeiro item de Kubernetes do projeto, que não tinha nada de cluster/k8s; soma-se aos cheat sheets de git, docker, sql e bash)
- Referências: Sintaxe Markdown — `/references/markdown-syntax` (cheat sheet pesquisável dos elementos de Markdown do dia a dia — títulos ATX e Setext, itálico/negrito/riscado, código inline e bloco (com/sem linguagem), listas ordenadas/não ordenadas/de tarefa, links, imagens, tabelas com alinhamento e escape de caracteres —, cada um com o trecho-fonte em `<pre>` e o preview renderizado ao vivo do lado por um mini parser próprio (mesma família do `/tools/markdown-previewer`, com tabela, task list e Setext a mais), filtro por categoria em botões de rádio com contagem, botão de copiar o exemplo de cada item e um Alert que explica os "flavors" de Markdown (CommonMark × GitHub Flavored); funciona dos dois sentidos — vendo o efeito você acha a sintaxe e vendo a sintaxe você prevê o efeito —; imagem de exemplo embutida em base64 local, nenhum recurso externo; o parser-fonte é exibido na própria página num Collapse; complementa o `markdown-previewer`, que renderiza texto que você digita mas não cataloga a sintaxe)
- Ferramenta: CSV/TSV → Tabela Markdown — `/data/csv-markdown-table` (cola uma planilha exportada em CSV/TSV e gera uma tabela Markdown pronta pra documentação, comentário de PR ou README, mais o equivalente em HTML pra rich text; parser próprio estilo RFC4180 — campos entre aspas podem conter o delimitador, aspas escapadas `""` e quebras de linha, e CRLF vira linha única; detecção de cabeçalho automática que trata a primeira linha como header só quando não parece toda numérica ("Auto"/"Sim"/"Não"); alinhamento da separadora da tabela em Padrão/Esquerda/Centro/Direita (`---`, `:---`, `:---:`, `---:`), geração de nomes de coluna "Col 1...N" quando não há header, escape de `|` → `\|` e quebra de linha → `<br>` nas células; ambos os formatos com botão de copiar; nenhum dado sai do navegador; o algoritmo-fonte (parser e montagem) é exibido na própria página; complementa o `csv-json-converter`, que converte CSV ↔ JSON mas não vira tabela de documentação)
- Rede: Referência de Portas TCP/UDP — `/network/common-ports` (cheat sheet pesquisável das portas bem conhecidas — 80/443, 22/SSH, 53/DNS, 3306/MySQL, 5432/PostgreSQL, 6379/Redis, 27017/MongoDB, 6443/kubernetes, 9092/Kafka, 11211/Memcached, MQTT, SMB, NFS e muitas outras — cada uma com número, protocolo TCP/UDP, categoria colorida e descrição PT/EN, com busca que casa o número, o nome do serviço ou a descrição e filtro por categoria em botões de rádio com contagem; funciona também no sentido inverso — dado um serviço você acha a porta padrão e vice-versa — e num Alert explica que "padrão well-known do IANA" não é garantia: a maioria dos serviços pode ser reconfigurada pra outra porta, então vale confirmar com `ss -ltnp` na hora de investigar; botão de copiar a lista filtrada como tabela Markdown pronta pra documentação; complementa o `/network/mime-lookup` (que lida com tipos de conteúdo, não portas) e o `/references/http-status-codes`/`/references/http-methods` (códigos e métodos, não portas); 100% client-side, nada sai do navegador)
- Ferramenta: Validador de JSON Schema — `/data/json-schema-validator` (cola um schema e um JSON e o validador aponta todas as violações do documento contra o contrato, cada uma com o caminho e uma descrição legível; valida um subconjunto prático do draft-07 sem dependência alguma — `type` (com união `["string","null"]` etc.), `enum`/`const`, `required`, `properties`, `patternProperties`, `additionalProperties` (false ou schema), `items` (schema ou tupla), limites de tamanho/quantidade, `pattern`, `format` (email, URI, date-time, IPv4, UUID...), `multipleOf`, `uniqueItems`, `contains`, `anyOf`/`oneOf`/`allOf`/`not` e `$ref` local (`#/definitions/...`), com guarda de profundidade pra `$ref` circular não travar a aba; o núcleo é uma função recursiva `walk(schema, valor, path, depth)` que produz erros `{path, code, params}` traduzidos pela página em PT/EN — anyOf/oneOf/not avaliam cada braço num checkpoint de `errors.length` e descartam erros parciais; exemplo válido e inválido de um clique, tabela das violações com caminho em monospace e botão de copiar, e erro de parse de schema × instância separados; complementa o `json-schema-generator`, que gera o schema a partir de um JSON, e o `json-diff`/`json-formatter`, que não validam contra contrato; 100% client-side, nada sai do navegador)
- Ferramenta: Formatador e Minificador de SQL — `/database/sql-formatter` (cola um SQL e formata com indentação de 2/4 espaços ou minifica — tokenizer próprio que trata strings `'...'` (com `''` escapado e prefixos `E'...'`/`N'...'`), identificadores entre aspas/backtick/colchetes, comentários `--`/`/* */` e números como tokens únicos, nada tocado por dentro deles; o re-emissor usa poucas regras: cláusulas novas (SELECT/FROM/WHERE/GROUP/...) ganham linha própria no indent-base do escopo, cláusulas de lista (SELECT/FROM/WHERE/HAVING/VALUES/SET) jogam o conteúdo pra linha seguinte com +1 de indent e um item por linha, `(` que abre subquery (próximo token SELECT/WITH/...) ou as colunas de CREATE TABLE vira bloco com conteúdo indentado e `)` alinhado à linha de abertura, AND/OR em WHERE/HAVING/ON quebram linha no nível do conteúdo, GROUP BY/ORDER BY emendam o BY e depois listam, `.`/`::`/`->`/`(` de função colam no token vizinho e ON CONFLICT DO UPDATE/SET fica inline até o `;`; minificar remove comentários e só mantém os espaços que a fusão mudaria (SELECT * precisa de espaço, count(x) não); a validação nasce do próprio tokenizer — string/comentário sem fechamento e parêntese desbalanceado são apontados antes de formatar; saída com botão de copiar e estatísticas — statements, tokens, bytes de entrada/saída e % de economia no minificar —, exemplos clicáveis (SELECT, INSERT, CREATE TABLE, subquery) e opções de maiúsculas em keywords on/off; complementa o `json-to-sql`, que emite INSERTs mas não reformata consultas; é o quarto da família de formatadores 100% client-side, fechando a lacuna ao lado de `json-formatter`/`xml-formatter`/`css-formatter`; o algoritmo-fonte é exibido na própria página)
- Ferramenta: Formatador e Minificador de CSS — `/tools/css-formatter` (formata com indentação de 2 espaços, minifica ou valida um CSS colado — tokenizer próprio que trata strings, comentários e `url(...)` como tokens únicos (nada é tocado por dentro deles) e acompanha profundidade de parêntese pra nunca dividir `;`/`,` dentro de `url(data:image/png;base64,...)`; a validação nasce do próprio tokenizer, apontando chave, parêntese, aspas e comentário sem fechamento; no formatar, seletores virgulados viram uma linha por item, declarações saem `prop: value;` por linha com indentação e regras de raiz ganham linha em branco entre si; no minificar, remove espaços conservando o mínimo que evita fundir tokens (`and (`, `url(...)no-repeat`) e a opção de preservar comentários; mostra estatísticas — regras, declarações, comentários, bytes de entrada/saída e % de economia —, exemplos clicáveis prontos (bonito e minificado) e nada sai do navegador; é o terceiro da família de formatadores 100% client-side, complementando o `json-formatter` e o `xml-formatter`, que não entendem CSS; o algoritmo-fonte é exibido na própria página)
- Ferramenta: Gerador de JSON Schema — `/data/json-schema-generator` (cola um JSON de exemplo — payload de API, mock, dump — e gera um JSON Schema compatível com draft-07 descrevendo a estrutura: tipos por valor (`null`/`boolean`/`string`/`integer`/`number`), objetos varridos por chave com `required`, arrays com `items` inferido dos elementos e união de tipos mistos em `anyOf`; arrays de objetos são fundidos por chave — só entram em `required` as chaves presentes em todos os elementos — e valores primitivos repetidos com poucas variações viram `enum: [...]` (opcional); opções: todas as chaves obrigatórias on/off e inferência de enum on/off, saída com botão de copiar e contagem de bytes, exemplos clicáveis; algoritmo-fonte exibido na própria página; 100% client-side, sem API externa; complementa o `json-to-sql`, que converte JSON em INSERTs, e o `json-diff`/`json-path-explorer`, que analisam a estrutura de outro ângulo)

## 2026-08-07
- Correção de bug: moeda Real em inglês mostrava "dollars" em vez de "reais" — `/tools/number-to-words` (quando o idioma era inglês, o modo R$ Real sempre renderizava "dollars"; `enMoney` passou a receber a moeda e usa "real/reais" para BRL)

## 2026-08-07
- Ferramenta: Número por Extenso — `/tools/number-to-words` (escreve números e valores em dinheiro por extenso em português e inglês — útil pra cheques, contratos, notas e faturas; PT implementa a regra do `e`: liga dezenas/unidades dentro do grupo (`cento e vinte`) e entra antes de um grupo cujo valor-líder é `< 100` ou múltiplo exato de 100 (`mil e vinte`, `mil e cem`, mas `mil duzentos e trinta`), com `de` antes da moeda em valores redondos acima de 1 milhão (`dois milhões de reais`); EN usa o estilo britânico formal de cheques com `and` só antes da dezena/unidade final sem centena (`one thousand and one` vs `one thousand one hundred`); três modos — número puro, R$ e US$ com centavos —, limitado a 999.999.999.999.999 dentro do range seguro de `Number`, parser de moeda tolerante a `1.234,56`/`1,234.56`/`1234.56`, formatação de milhares conforme o idioma, botão de copiar e exemplos clicáveis; nenhum dado sai do navegador; algoritmo-fonte exibido na página)
- Referências: Métodos HTTP — `/references/http-methods` (cheat sheet pesquisável dos 9 métodos de requisição HTTP — GET, HEAD, OPTIONS, POST, PUT, PATCH, DELETE, TRACE, CONNECT —, o que cada um faz, quando usar e um exemplo de endpoint, com filtro por categoria Leitura (seguros)/Escrita/Infraestrutura; cada método expõe em Tags coloridas as quatro propriedades que decidem retry e cache — seguro/safe, idempotente, tem corpo, cacheável —, os status HTTP de sucesso típicos e um Descriptions detalhado; um Alert explica a diferença entre safe e idempotente, o conceito que justifica o retry automático de GET/PUT vs. os riscos de repetir um POST; complementa o `/references/http-status-codes`, que trata dos códigos de resposta em vez dos métodos de requisição)
- Rede: Lookup de MIME Types — `/network/mime-lookup` (referência pesquisável de tipos MIME/media types: ~100 tipos comuns do registro IANA — application, audio, font, image, text, video, multipart —, cada um com seu `Content-Type`, a lista de extensões e uma descrição curta PT/EN; um único campo de busca casa tanto a extensão (`.png`/`png`), quanto o tipo MIME (`image/png`) ou a descrição, e há filtro por categoria em botões de rádio com contagem de resultados; funciona também como mapa inverso — dado um tipo você acha as extensões e vice-versa, incluindo os aliases comuns como `.js` → `text/javascript` (o navegador só executa esse) vs `application/javascript` (spec) e `.ico` → `image/x-icon`/`image/vnd.microsoft.icon`; um Alert explica como o cabeçalho `Content-Type` funciona, pra reforçar que o que define o tratamento é o header/atributo `type`, não a extensão; tudo client-side, sem API; complementa o `/references/http-status-codes`, que trata de códigos de resposta em vez de tipos de recurso)
- Ferramenta: Construtor de Expressão Cron (visual) — `/tools/cron-builder` (o inverso do explicador de cron já existente: monta uma expressão de 5 campos a partir de controles — por campo, alterna "Qualquer (*)" ou um conjunto específico marcando os valores como tags `CheckableTag`, agrupados por fuso de valores 0–59/0–23/1–31/1–12/0–6 com rótulos de dia da semana e mês — e vê a expressão gerada ao vivo com botão de copiar e as próximas 5 execuções calculadas no navegador caminhando minuto a minuto a partir de agora, respeitando a regra OR entre dia-do-mês e dia-da-semana quando ambos estão restritos; campo de cada linha usa `Segmented` Qualquer/Específicos e o token vira `*` ou a lista separada por vírgula; complementa o `/tools/cron-parser`, que descreve expressões já escritas; o build é 100% client-side, sem API externa)
- Rede: Parser de User-Agent — `/network/user-agent-parser` (descobre navegador, motor de renderização, SO e tipo de dispositivo a partir de uma string `User-Agent` — tudo client-side via regex, sem dependência externa; a ordem de teste importa: tokens específicos `Edg/`/`OPR/`/`CriOS`/`FxiOS`/`SamsungBrowser` antes dos genéricos `Chrome`/`Firefox`/`Safari`, SO por marcadores `Windows NT`/`Android`/`iPhone OS`/`Mac OS X`/`CrOS`/`X11; Linux` e dispositivo pela presença de tokens de mobile/tablet; resultado em Tags coloridas + `Descriptions`, com botões de exemplos prontos (Chrome, Safari, Firefox, iPhone, Android, Edge Chromium) e atalho "usar meu navegador" que insere o `navigator.userAgent` atual; a string nunca sai do navegador; complementa o `/network/subnet-calculator` e o `/mobile/deep-link-tester`, que não interpretam a User-Agent; esboço da lógica de detecção exibido na própria página)
- Ferramenta: Conversor de Fuso Horário — `/tools/timezone-converter` (escolhe um momento e vê a que horas ele é em vários fusos ao mesmo tempo — deploy, reunião remota, oncall — com o campo de data/hora interpretado como horário de PAREDE do fuso de origem via `Intl.DateTimeFormat`; o núcleo faz o parse de "horário de parede → instante" com o truque em duas passadas — assume UTC, descobre o offset no fuso, corrige e reconfere pra acertar DST —, lista de alvos com hora HH:MM:SS, data, offset `UTC±HH:MM`, delta vs. origem (+12h etc.) e selo de mesmo/dia seguinte/anterior, além de trocar o fuso de origem reescrever o campo mantendo o mesmo instante, botão "agora" e add/remove de fusos; complementa o `timestamp-converter`, que lida com unix↔data num fuso só; algoritmo-fonte exibido na página)
- Estilo: Input OTP (código de verificação) — `/styles/otp-input` (componente reutilizável de campo de código — N caixas de um dígito cada — controlado por uma string única concatenada, sem inputs "fantasma": cada caixa é um input real; micro-interações que deixam o fluxo fluido — digitar avança sozinho pro próximo campo vazio, Backspace apaga e, se o campo já estiver vazio, volta pro anterior apagando ele, setas ←/→ navegam e colar um código inteiro preenche a partir do primeiro campo vazio; só aceita dígitos via `inputMode="numeric"`/`replace(/\D/g)`; página com demo — slider de 4 a 8 dígitos, switch pra exibir como senha, contador preenchido/vazio, tag de "preenchido" e botão de preencher de exemplo — e o código-fonte do componente exibido na própria página; complementa o `floating-label-input` e o switch iOS, que são inputs/textos livres em vez de código por dígito)
- Fora da caixa: Visualizador de Ordenação — `/extras/sorting-visualizer` (anima a ordenação de um array de barras com Bubble, Selection, Insertion e Quick Sort, com tamanho/velocidade configuráveis, play/pausa/passo e geração de novo array; o segredo da animação sem travar nem corromper estado é pré-computar TODOS os frames de cada algoritmo — cada frame é uma snapshot imutável `{arr, pos, done}` — e o play só avança um índice num `setInterval` com atualização funcional de estado, sem closures async; barras laranja = comparando/trocando, verde = já na posição final; mostra passo atual/total, contadores de frames e a ideia do algoritmo-fonte na própria página; complementa o `fisher-yates-shuffle`, que embaralha em vez de ordenar)
- Banco de Dados: Gerador de SQL INSERT a partir de JSON — `/database/json-to-sql` (cola um array de objetos — saída de API ou dump — e gera os comandos `INSERT` prontos pra semear o banco: colunas inferidas pela união das chaves na ordem de primeira ocorrência, serialização por tipo — strings com aspa escapada `''`, booleans como `TRUE`/`FALSE`, `null` como `NULL`, objetos/arrays aninhados como JSON textual ou `NULL` —, dialeto de aspas em identificadores PostgreSQL (`"col"`)/MySQL (`` `col` ``)/SQLite, linhas por `INSERT` (1/10/50/100/500/todas, quebrando o `VALUES` em lotes) e suporte a objeto único ou array de valores primitivos (coluna `value`); mostra contagens de linhas/colunas/comandos/tamanho em bytes e o algoritmo-fonte na própria página; nada é executado, só texto pra copiar; complementa o `csv-json-converter`, que gera JSON/CSV mas não emite SQL)
- Ferramenta: Inspetor de Unicode — `/tools/unicode-inspector` (descobre todos os caracteres de um texto: tabela por caractere único com code point `U+xxxx`, bytes em UTF-8 via `TextEncoder`, entidade HTML numérica e categoria Unicode classificada por propriedades de regex — `\p{L}`/`\p{M}`/`\p{N}`/`\p{S}`/`\p{Extended_Pictographic}` —, com varredura por code point de verdade via `Array.from` em vez de unidades UTF-16; destaca os caracteres "fantasmas" que quebram display/validação — zero-width, marcas bidi e de controle — num Alert de atenção listando os code points, filtro "só os incomuns", contadores (total/distintos/incomuns) e botão de copiar a tabela como TSV; complementa o `remove-accents`, que mexe nos diacríticos combinados, e o `case-converter`, que não expõe a estrutura dos caracteres)
- Fora da caixa: Simulador de Colisão de UUID — `/extras/uuid-collision-simulator` (aplica o paradoxo do aniversário ao espaço de um UUID v4 [122 bits] e deixa a impossibilidade na prática virar números: mostra a matemática `P ≈ 1 − e^(−n²/2M)`, tabela de probabilidade de colisão por n gerado no espaço real `2^122`, o limiar de 50% (~2,7 × 10^18 IDs) e um simulador interativo com entropia reduzida — deslize os bits de 8 a 28 e o n (até 1M) e rode sorteios comparando as colisões observadas contra o esperado pela fórmula, com barra teórico vs. observado e média de 20 rodadas; complementa o gerador de UUID e o de NanoID, que só criam IDs)
- Ferramenta: Processador de Linhas — `/text/lines-tool` (transforma um bloco de linhas de texto com operações encadeadas em ordem — aparar espaços, remover linhas vazias, deduplicar preservando a primeira ocorrência com opção de ignorar maiúsculas/minúsculas, ordenar de A a Z ou de Z a A e numerar —, resultado atualizado em tempo real via `useMemo` com contagem de linhas antes/depois e botão de copiar; complementa o `word-counter` e o `remove-accents`, que não reorganizam linhas)
- DevOps: Gerador de Nome de Branch — `/devops/branch-name-generator` (gera um nome de branch git consistente no padrão `tipo/escopo-descricao` a partir de uma descrição de tarefa — em minúsculas, sem acentos e sanitizado contra as regras do `git check-ref-format`: remove espaço, `..`, `@{`, `~`/`^`/`:`/`?`/`*`/`[`, barra e `-` nas pontas; prefixos de tipo comuns `feature/fix/hotfix/refactor/chore/docs/test/style/build/ci` e escopo opcional, ambos desligáveis, campo de slug manual pra controle total e truncamento em ~50 chars; gera o nome pronto e o comando `git checkout -b` pra copiar; complementa o gerador de mensagem de commit, que lida com o texto do commit em vez do nome da branch, e o gerador de slug, que não tem as regras/padrões de branch do git)
- Snippet: `deepClone` & `deepEqual` do zero — `/snippets/deep-clone-deep-equal` (implementações em `src/utils/deep.js` de cópia profunda e comparação estrutural profunda, sem `JSON.parse(JSON.stringify(...))` — preservam `Date`/`RegExp`/`Map`/`Set` e lidam com referências cíclicas via `Map` de nós visitados na cópia; a página mostra o código-fonte e duas demos: comparar dois JSONs com `deepEqual`/`firstDiff` apontando a primeira diferença por caminho (`$.meta.open`), e provar que o clone é cópia de verdade — muta um campo aninhado só na cópia e mostra o original intacto; complementa o `json-diff`, que trabalha sobre pares de JSONs colados de forma diferente)
- Ferramenta: Formatador de XML — `/tools/xml-formatter` (cola um XML e formata com indentação, minifica ou valida — tudo client-side via `DOMParser` com MIME `application/xml`, que já valida e devolve um DOM cujo nó `<parsererror>` acusa o problema com a posição exata; a formatação re-serializa a árvore recursivamente com indentação de 2 espaços, preservando comentários, CDATA, atributos e a declaração `<?xml?>`/DOCTYPE no topo, e opção de adicionar a declaração quando ausente; mostra contagem de nós/elementos e tamanho em bytes; complementa o `json-formatter`, que não entende XML; o algoritmo de parse/formação é exibido na própria página)
- Ferramenta: Remover Acentos — `/text/remove-accents` (tira acentos e diacríticos de qualquer texto — á, ç, ã, è, ü etc. — em tempo real, tudo client-side via normalização Unicode NFD: `normalize('NFD')` separa o caractere base da marca de combinação e uma regex remove só as marcas do bloco U+0300–U+036F; varredura com `for...of` percorre por code point (não por unit UTF-16), então preserva case, pontuação, letras compostas e emojis; mostra contagem de diacríticos removidos e de caracteres do resultado, e lista cada diacrítico encontrado com seu code point U+xxxx e nº de ocorrências; botões de exemplo aleatório, limpar e copiar; complementa o `case-converter`, que não trata acentuação; o algoritmo é exibido na própria página)
- Ferramenta: Validador de arquivo .env — `/devops/env-tool` (parser/validador de `.env` 100% client-side: mostra cada variável parseada numa tabela — chave, valor, tipo de aspas e linha de origem — e aponta problemas comuns: chave duplicada, chave sem valor, nome de chave inválido, linha sem `=`, aspas sem fechamento e referência a variáveis indefinidas em valores (`$NOME`/`${NOME}`); suporta comentários, prefixo `export`, valores com aspas simples/duplas e comentário inline em valores sem aspas; parser puro em `src/utils/envParser.js` com código-fonte exibido na própria página)
- Correção de bug: tela branca com texto muito grande — `/ai/token-counter` (reportado via o botão "Reportar um problema"; colar dezenas de milhões de caracteres derrubava a página porque `text.trim().split(/\s+/)`, `text.replace(/\s/g, '')` e o `split` dentro de `estimateTokensCJK` alocavam arrays/strings proporcionais ao tamanho do input a cada render; reescrito como varredura única — `trimLength`/`countWords`/`countCharsNoSpace` por índice e `estimateTokensCJK` com regex `/\S+/g` sem montar array de palavras —, mesma saída numérica de antes, sem alocação extra; reproduzido e verificado com Puppeteer contra o site real antes e depois da correção)

## 2026-08-06 (rodada 16)
- IA: Contador de Tokens — `/ai/token-counter` (estima quanto um texto ocuparia em tokens de um LLM a partir de duas heurísticas client-side — a regra de bolso "chars / 4" do GPT/LLaMA e uma variante ciente de I18N que cobra mais por caracteres CJK/emoji —, seleção de janela de contexto de modelos comuns — Claude Sonnet 4.5, GPT-4o (mini), Llama 3.1, Command R+, Gemini Flash — e barra de Progress mostrando a ocupação percentual do contexto, com counts de caracteres/palavras; complementa o `/ai/anthropic-cost-calculator`, que precisa do número de tokens pra estimar custo; tudo local)
- Ajuste: `routes.jsx` ganhou a rota `/ai/token-counter`, `AppLayout.jsx` ganhou o item de menu (com badge "Novo"/"New"), `src/newItems.js` marcou a rota desta rodada, `IDEAS.md` marcado com `[x]`
- Ferramenta: Timer Pomodoro — `/extras/pomodoro-timer` (timer Pomodoro 100% client-side com foco/pausa curta/pausa longa, pausa longa a cada N focos configurável, com `setInterval` decrementando o tempo só com atualização funcional de estado pra evitar closures desatualizadas; quando zera a fase avança sozinha — contabilizando focos concluídos — e o anel de progresso (`Progress type="circle"`) reflete a ocupação do bloco atual; seleção manual de fase, auto-start do próximo bloco, pular bloco e reposição de durações)
- Ajuste: `routes.jsx` ganhou a rota `/extras/pomodoro-timer`, `AppLayout.jsx` ganhou o item de menu no grupo "Fora da caixa" (com badge "Novo"/"New"), `src/newItems.js` acrescentou a rota desta etapa (mantendo as chaves anteriores do dia), `IDEAS.md` marcado com `[x]`

## 2026-08-05 (rodada 15)
- Ferramenta: Timeline de Expiração do JWT — `/tools/jwt-timeline` (cola um JWT e vê as claims `iat`/`nbf`/`exp` plotadas numa linha do tempo, com marcador de "agora" que se move sozinho a cada segundo e contagem regressiva ao vivo até expirar ou desde que expirou; complementa o `/tools/jwt-decoder`, que mostra header/payload crus em vez da timeline)
- Estilo: Efeito de Máquina de Escrever — `/styles/typewriter-effect` (hook `useTypewriter` que digita/apaga uma lista de frases em loop via `setTimeout` encadeado, sem lib; cursor piscando só em CSS com `@keyframes` alternando opacity; demo com frases, velocidade de digitação/apagar e pausa configuráveis)
- Snippet: LRU Cache do zero — `/snippets/lru-cache` (implementação em `src/utils/lruCache.js` usando só um `Map` nativo — `delete`+`set` reinsere a chave no fim, então a primeira chave é sempre a menos recentemente usada —, `get`/`put` O(1) sem lista duplamente encadeada; demo interativa com capacidade ajustável, chips mostrando a ordem atual do cache, log de operações e uma sequência de exemplo pra ver a evicção acontecer)
- Segurança: Gerador de Content-Security-Policy — `/security/csp-generator` (monta um cabeçalho `Content-Security-Policy` a partir de diretivas — `default-src`, `script-src`, `frame-ancestors` etc. — e listas de valores por linha, com atalhos pra inserir tokens comuns como `'self'`/`'unsafe-inline'`/`data:`, toggle de `upgrade-insecure-requests`, e saída tanto como cabeçalho HTTP quanto como tag `<meta>` equivalente)
- Ajuste: `routes.jsx` ganhou as 4 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 4 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes

## 2026-08-04 (rodada 14)
- DevOps: Gerador de Mensagem de Commit — `/devops/commit-message-generator` (monta uma mensagem no padrão Conventional Commits — tipo(escopo)!: descrição, corpo, breaking change, rodapé — a partir de um formulário, com tabela de referência dos 11 tipos padrão e quando usar cada um; tudo local, só montagem de texto)
- Segurança: Gerador de robots.txt — `/security/robots-txt-generator` (monta um `robots.txt` a partir de N grupos de regras por user-agent — Disallow/Allow/Crawl-delay — mais sitemap(s) opcionais; atalhos prontos pra bloquear tudo, permitir tudo ou bloquear a lista de crawlers de IA mais conhecidos — GPTBot, ClaudeBot, CCBot, Google-Extended etc.)
- Estilo: Indicador de Progresso de Scroll — `/styles/scroll-progress-bar` (barra fina com `position: sticky` no topo de um container rolável, cuja largura reflete `scrollTop / (scrollHeight - clientHeight)` calculado no `onScroll`, sem lib nem `IntersectionObserver`; código-fonte exibido na página)
- Estilo: Efeito de Confete — `/styles/confetti-effect` (explosão de partículas em `<canvas>` puro disparada por clique, com gravidade/arrasto constantes, rotação própria por partícula e fade perto do fim da vida útil; loop em `requestAnimationFrame` que se auto-encerra quando não sobra partícula viva; código-fonte exibido na página)
- Snippet: `debounce`/`throttle` (função pura) — `/snippets/debounce-throttle-functions` (implementação em `src/utils/debounceThrottle.js` das duas técnicas como funções JS puras, sem depender de React — complementa os hooks `useDebounce`/`useThrottle` já existentes, que só funcionam dentro do ciclo de vida de um componente; throttle é "trailing", garante que a última chamada da rajada também executa)
- Ajuste: `routes.jsx` ganhou as 5 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 5 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes (incluindo a referência de tabela de Conventional Commits, coberta pela mesma página do gerador de commit)

## 2026-08-03 (rodada 13)
- Ferramenta: Gerador de JWT — `/tools/jwt-generator` (monta e assina um JSON Web Token válido — HS256/384/512 — a partir de um payload JSON e uma chave secreta, via `crypto.subtle.sign` no navegador; complementa o `/tools/jwt-decoder`, que faz o caminho inverso)
- Estilo: Toast com Barra de Auto-Dismiss — `/styles/toast-notification` (notificação empilhável que entra deslizando via `@keyframes` e some sozinha após um tempo, com barra de progresso na base sincronizada por `animation-duration`; JS só empilha/remove cada toast do array de estado com um `setTimeout` individual por item; código-fonte exibido na página)
- Snippet: `useUndo` — `/snippets/use-undo` (hook em `src/hooks/useUndo.js` que dá histórico de undo/redo a qualquer valor de estado via pilhas `past`/`present`/`future` — `set()` empilha e limpa o redo pendente, `undo()`/`redo()` movem um item entre as pilhas; página mostra o código e uma demo com input de texto)
- Referências: Atalhos de Terminal/Bash — `/references/bash-shortcuts` (cheat sheet pesquisável de atalhos de edição de linha readline — movimento, edição, histórico —, controle de processos, jobs/background e pipes/redirecionamento, com descrição PT/EN de cada um)
- Ajuste: `routes.jsx` ganhou as 4 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 4 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes

## 2026-08-02 (rodada 12)
- Arquivos & Dados: JSON Path Explorer — `/data/json-path-explorer` (testa uma expressão JSONPath colada contra um JSON, com tokenizer/avaliador próprio cobrindo um subconjunto prático da sintaxe — `$`, `.prop`, `..prop` recursive descent, `[*]`, `[n]`, `[n,m]`, `[start:end]`, `['prop']` — sem filtros `?()` nem `eval`; tabela de resultados com caminho e valor de cada match, mais referência da sintaxe suportada)
- Referências: Cheat Sheet Interativo de CSS Grid — `/references/css-grid-cheatsheet` (playground ao vivo pra `grid-template-columns`/`gap`/`justify-content`/`align-content`/`justify-items`/`align-items` com preview de caixas coloridas, exemplo de item ocupando várias colunas via `grid-column: span N`, CSS gerado pronto pra copiar e tabela de referência rápida das propriedades de container vs. item; complementa o cheat sheet de Flexbox já existente)
- Snippet: Fisher-Yates Shuffle — `/snippets/fisher-yates-shuffle` (implementação do algoritmo de embaralhamento uniforme, com demo de cartas e uma simulação visual comparando a distribuição de posições do Fisher-Yates contra o `array.sort(() => Math.random() - 0.5)` ingênuo e enviesado — roda N vezes e mostra a frequência de cada item cair na posição 0 num gráfico de barras pareado)
- Fora da caixa: Roleta de Sorteio — `/extras/team-roulette` (adiciona nomes — ex. "quem revisa esse PR" — e gira uma roleta em CSS `conic-gradient` que sorteia um deles com chance uniforme via `Math.random`; ângulo final calculado pra alinhar a fatia sorteada com o ponteiro fixo no topo, com voltas extras pro efeito de giro)
- Ajuste: `routes.jsx` ganhou as 4 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 4 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes, novo `src/utils/shuffle.js` com `fisherYatesShuffle`/`naiveSortShuffle`

## 2026-08-01 (rodada 11)
- Ferramenta: Gerador de HMAC — `/tools/hmac-generator` (calcula HMAC-SHA1/256/384/512 de uma mensagem a partir de uma chave secreta via `crypto.subtle.importKey`/`sign`; útil pra conferir assinaturas de webhook como GitHub/Stripe ou gerar tokens de teste, tudo local)
- Ferramenta: Gerador de Lorem Ipsum — `/tools/lorem-ipsum-generator` (gera texto de preenchimento em pseudo-latim por palavras/frases/parágrafos, quantidade configurável e opção de começar com "Lorem ipsum dolor sit amet")
- Arquivos & Dados: JSON Diff (estrutural) — `/data/json-diff` (compara dois JSONs por estrutura — chaves e valores, não linha de texto — via percurso recursivo em paralelo dos dois valores, listando adicionado/removido/alterado por caminho tipo `root.a.b[0]`; complementa o `/tools/diff-checker`, que compara texto linha a linha)
- Estilo: Dark Mode Toggle Animado — `/styles/dark-mode-toggle` (switch sol/lua sobre o mesmo padrão de checkbox nativo escondido do switch iOS já existente — trilha troca de gradiente claro→escuro, estrelinhas surgem via opacity e a bolinha desliza com `translateX` girando 360° em `cubic-bezier` trocando ícone `SunOutlined`/`MoonOutlined` e cor de fundo; código-fonte exibido na página)
- Snippet: `useThrottle` — `/snippets/use-throttle` (hook em `src/hooks/useThrottle.js` que limita a propagação de um valor a no máximo uma atualização a cada `limitMs`, mesmo com mudanças mais frequentes — diferente do `useDebounce`, que só reage após o valor parar de mudar; página mostra o código e uma demo com contador bruto vs. contador com throttle e slider de intervalo)
- Referências: Atalhos de Teclado do VSCode — `/references/vscode-shortcuts` (cheat sheet pesquisável dos atalhos mais usados do VSCode, com a combinação Windows/Linux e Mac lado a lado, agrupados por categoria — geral, edição, navegação, busca, exibição)
- Ajuste: `routes.jsx` ganhou as 6 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 6 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes

## 2026-07-31 (rodada 10)
- Ferramenta: Gerador de NanoID — `/tools/nanoid-generator` (gera IDs curtos aleatórios via `crypto.getRandomValues` com rejection sampling — descarta bytes fora do intervalo pra evitar viés de módulo, mesma técnica do pacote `nanoid` —, comprimento e alfabeto configuráveis — padrão, alfanumérico, minúsculas, maiúsculas, números, hex ou personalizado —, geração em lote e copiar individual/tudo)
- Ferramenta: HTML Entity Encode/Decode — `/tools/html-entity-encoder` (codifica `& < > " '` em entidades nomeadas, com opção de converter todo caractere não-ASCII em entidade numérica `&#code;`; decodifica no sentido inverso via `DOMParser` sobre um documento sem scripting habilitado, seguro contra XSS mesmo com HTML de origem desconhecida)
- Front-end: Gerador de Border Radius — `/frontend/border-radius-generator` (ajusta o raio de cada canto independentemente com preview ao vivo; modo avançado gera a sintaxe elíptica completa de `border-radius` com valores horizontal/vertical distintos por canto, separados por `/`)
- Estilo: Switch Estilo iOS — `/styles/ios-toggle-switch` (checkbox nativo visualmente escondido, ainda acessível por teclado/leitor de tela, controlando uma trilha com bolinha posicionada via `::before` que desliza em `cubic-bezier` com leve overshoot; cor de "ligado" configurável por variável CSS; código-fonte exibido na página)
- Snippet: `useEventListener` — `/snippets/use-event-listener` (hook em `src/hooks/useEventListener.js` genérico pra registrar listener em `window` ou em qualquer elemento via `ref`, guardando o handler mais recente num `useRef` pra evitar closures desatualizadas sem precisar re-registrar o listener a cada render; página mostra o código e uma demo de rastreamento de mouse dentro de uma caixa)
- Referências: npm vs yarn vs pnpm — `/references/package-manager-commands` (tabela comparativa pesquisável dos comandos mais usados no dia a dia nos três gerenciadores de pacote do ecossistema Node.js — install, add, remove, update, scripts, workspaces, audit, link, etc.)
- Ajuste: `routes.jsx` ganhou as 6 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 6 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes

## 2026-07-30 (rodada 9)
- Ferramenta: Conversor de Unidades CSS — `/tools/css-unit-converter` (converte um valor entre `px`/`rem`/`em`/`pt`/`vw`/`vh` em tempo real a partir de bases ajustáveis — tamanho de fonte raiz, tamanho de fonte do pai e dimensões da viewport —, editando qualquer um dos seis campos)
- Ferramenta: Calculadora de Especificidade CSS — `/tools/css-specificity-calculator` (cola um ou mais seletores CSS, um por linha ou separados por vírgula, e calcula a especificidade `(a, b, c)` de cada um — IDs, classes/atributos/pseudo-classes, tags/pseudo-elementos —, seguindo a spec; trata `:where()` como zero e desembrulha `:not()`/`:is()` somando os tokens internos; tabela ordenada da maior pra menor especificidade)
- Estilo: Input com Label Flutuante (Floating Label) — `/styles/floating-label-input` (padrão clássico só com CSS via truque `placeholder=" "` + `:not(:placeholder-shown)` combinado com `:focus`, sem nenhum JavaScript; código-fonte exibido na página)
- Snippet: `useKeyPress` — `/snippets/use-key-press` (hook em `src/hooks/useKeyPress.js` que escuta `keydown`/`keyup` na `window` e retorna se uma tecla específica — pelo nome de `KeyboardEvent.key` — está pressionada agora; página mostra o código e uma demo com quatro teclas diferentes acesas ao segurar)
- Referências: Comandos SQL Essenciais — `/references/sql-commands` (cheat sheet pesquisável de comandos SQL — consultas, agregação, todos os tipos de JOIN, escrita, DDL, transações e recursos avançados como CTE e window functions —, com descrição PT/EN de cada um)
- Ajuste: `routes.jsx` ganhou as 5 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 5 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos itens correspondentes

## 2026-07-29 (rodada 8)
- Ferramenta: Comparador/Validador de SemVer — `/tools/semver-comparator` (valida uma string contra o regex oficial de Semantic Versioning 2.0.0 e compara duas versões pelas regras de precedência da spec — incluindo comparação alfanumérica de identificadores de prerelease e a regra de que uma versão sem prerelease é sempre maior que a mesma com prerelease —, além de gerar o próximo major/minor/patch a partir de uma versão base)
- Ferramenta: Gerador de Slug — `/tools/slug-generator` (converte texto livre em slug seguro pra URL — remove acentos via `normalize('NFD')`, troca espaços por separador configurável `-`/`_`, remove caracteres especiais e duplicados nas pontas; complementa o conversor de case, que não trata acentuação)
- Front-end: Gerador de Paleta de Cores — `/frontend/palette-generator` (gera paleta complementar, análoga, triádica ou monocromática a partir de uma cor base, girando o matiz em HSL; conversão HEX↔HSL própria, sem dependência)
- Estilo: Botão com Efeito Ripple — `/styles/ripple-button` (cria dinamicamente um `<span>` circular na posição exata do clique via `getBoundingClientRect` e coordenadas do evento, anima com `@keyframes` e remove o nó sozinho no `animationend`; código-fonte exibido na página)
- Snippet: `useOnScreen` — `/snippets/use-on-screen` (hook em `src/hooks/useOnScreen.js` que usa `IntersectionObserver` pra saber se um elemento está visível na viewport/container com scroll; página mostra o código e uma demo com caixa scrollável)
- Referências: Cheat Sheet Interativo de Flexbox — `/references/flexbox-cheatsheet` (playground ao vivo pra `flex-direction`/`justify-content`/`align-items`/`flex-wrap`/`gap` com preview de caixas coloridas e CSS gerado pronto pra copiar, mais tabela de referência rápida das propriedades de container vs. item)
- Ajuste: `routes.jsx` ganhou as 6 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 6 rotas nesta rodada, `IDEAS.md` marcado com `[x]` nos 6 itens correspondentes

## 2026-07-28 (rodada 7)
- Ferramenta: Gerador de CPF/CNPJ Fake — `/tools/cpf-cnpj-generator` (gera números fictícios de CPF e CNPJ com dígitos verificadores matematicamente válidos — algoritmo módulo 11, fórmulas distintas para cada documento —, quantidade em lote e copiar individual/tudo; nunca representam pessoas/empresas reais)
- Ferramenta: Conversor de Base Numérica — `/tools/base-converter` (converte um número entre binário, octal, decimal e hexadecimal em tempo real via `BigInt`, editando qualquer um dos quatro campos)
- Snippet: `useToggle` — `/snippets/use-toggle` (hook em `src/hooks/useToggle.js` pra estado booleano com toggle/set explícito num único setter — sem argumento inverte, com `true`/`false` força o valor; página mostra o código e uma demo com switch e botões)
- Snippet: `useWindowSize` — `/snippets/use-window-size` (hook em `src/hooks/useWindowSize.js` que retorna largura/altura da janela e se atualiza sozinho no evento `resize`; página mostra o código e uma demo ao vivo)
- Estilo: Loading de Dots Saltitantes — `/styles/bouncing-dots-loader` (três pontos animados em sequência via `@keyframes` e `animation-delay` escalonado, cor e tamanho configuráveis por variável CSS; código-fonte exibido na página)
- Referências: Comandos Docker & Docker Compose — `/references/docker-commands` (cheat sheet pesquisável dos comandos Docker e Docker Compose mais usados no dia a dia, com descrição PT/EN de cada um)
- Ajuste: `routes.jsx` ganhou as 6 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 6 rotas nesta rodada

## 2026-07-27 (rodada 6)
- Ferramenta: Markdown → HTML (Preview) — `/tools/markdown-previewer` (parser Markdown próprio, sem dependência externa, cobrindo títulos, negrito/itálico, código inline/bloco, listas, citação, link e linha horizontal; preview renderizado lado a lado com o textarea e HTML gerado pronto pra copiar)
- Estilo: Cartão Neumórfico (Soft UI) — `/styles/neumorphic-card` (cartão e botão em estilo neumórfico via duas sombras opostas — clara e escura — sobre fundo da mesma cor do elemento, com variante clara/escura selecionável e estado pressionado com sombra inset; código-fonte exibido na página)
- Snippet: `usePrevious` — `/snippets/use-previous` (hook em `src/hooks/usePrevious.js` que guarda o valor anterior de uma variável entre renders via `useRef` atualizado num `useEffect`; página mostra o código e uma demo com contador comparando valor atual vs. anterior)
- Arquivos & Dados: Gerador de Dados Fake — `/data/fake-data-generator` (gera N objetos fake com campos configuráveis — nome, e-mail, uuid, booleano, inteiro, float, data, telefone, cidade, país, palavra, frase, cor hex — via `Math.random`/`crypto.randomUUID`, saída em JSON ou CSV)
- APIs: Gerador de Payload de Webhook — `/apis/webhook-payload-generator` (templates estáticos de payloads de exemplo pra webhooks comuns — GitHub push/pull_request, Stripe charge/invoice/checkout, Slack message event — preenchidos com dados fictícios a cada clique, sem disparar nenhuma requisição real)
- Ajuste: `routes.jsx` ganhou as 5 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 5 rotas nesta rodada

## 2026-07-26 (rodada 5)
- Ferramenta: Comparador de Texto (Diff) — `/tools/diff-checker` (compara duas versões de um texto linha a linha via algoritmo LCS próprio, destacando adicionadas/removidas/iguais; opções pra ignorar maiúsculas/minúsculas e espaços extras; guarda contra travar a aba em textos gigantes limitando o tamanho da tabela de programação dinâmica)
- Front-end: Gerador de Box Shadow — `/frontend/box-shadow-generator` (construtor visual de `box-shadow` com N camadas editáveis — deslocamento X/Y, desfoque, expansão, cor, opacidade e inset —, preview ao vivo e CSS combinando as camadas por vírgula pronto pra copiar)
- Arquivos & Dados: Conversor CSV ↔ JSON — `/data/csv-json-converter` (parser CSV próprio no estilo RFC4180 — suporta campos entre aspas com vírgulas, quebras de linha e aspas escapadas — convertendo pra array JSON de objetos e vice-versa, com delimitador vírgula/ponto-e-vírgula/tab selecionável)
- Snippet: `useInterval` — `/snippets/use-interval` (hook em `src/hooks/useInterval.js`, no padrão clássico de callback sempre atualizado via `useRef` pra evitar closures desatualizadas dentro do `setInterval`; passar `null` como delay pausa sem desmontar; página mostra o código e uma demo com contador play/pause e slider de intervalo)
- Ajuste: `routes.jsx` ganhou as 4 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 4 rotas nesta rodada

## 2026-07-25 (rodada 4)
- Ferramenta: Gerador de Senha — `/tools/password-generator` (gera senhas aleatórias via `crypto.getRandomValues`, com comprimento ajustável, seleção de tipos de caractere, opção de excluir caracteres ambíguos — i/l/1/L/o/0/O —, geração em lote e copiar individual/tudo; tudo local)
- Ferramenta: URL Encode/Decode — `/tools/url-encoder` (codifica/decodifica texto com `encodeURIComponent`/`decodeURIComponent` para uso em query strings, ou `encodeURI`/`decodeURI` para URLs completas preservando caracteres reservados; detecta e avisa sobre %-encoding malformado)
- Front-end: Gerador de Gradiente CSS — `/frontend/gradient-generator` (construtor visual de `linear-gradient`/`radial-gradient` com N stops de cor editáveis por posição e ângulo ajustável para o modo linear, preview ao vivo e CSS pronto pra copiar)
- Snippet: `useCopyToClipboard` — `/snippets/use-copy-to-clipboard` (hook em `src/hooks/useCopyToClipboard.js` que encapsula `navigator.clipboard.writeText` e devolve um estado `copied` que reseta sozinho após um tempo, pra dar feedback visual em botões de copiar sem repetir `useState`/`setTimeout`; página mostra o código e uma demo ao vivo)
- Referências: HTTP Status Codes — `/references/http-status-codes` (cheat sheet pesquisável e filtrável por categoria — 1xx a 5xx — dos códigos de status HTTP mais usados no dia a dia, com descrição PT/EN de cada um)
- Ajuste: `routes.jsx` ganhou as 5 rotas acima, `AppLayout.jsx` ganhou os itens de menu correspondentes (com badge "Novo"/"New"), `src/newItems.js` substituído para marcar só essas 5 rotas nesta rodada

## 2026-07-24 (fix manual, fora de rodada — Edson)
- Ajuste: `AppLayout.jsx` — Sider e Content agora rolam em containers independentes (`height: 100vh` + `overflow: auto` cada um), em vez do scroll único da página inteira. Antes, com o menu já tendo muitos grupos, rolar até o último item do menu rolava a página toda; ao clicar, o conteúdo novo renderizava fora da área visível e parecia que "nada tinha acontecido". Content também reseta o próprio scroll pro topo a cada troca de rota (`useEffect` em `location.pathname`).

## 2026-07-24 (rodada 3)
- Ferramenta: Regex Tester — `/tools/regex-tester` (testa uma expressão regular contra um texto colado, destacando as correspondências ao vivo via `String.matchAll`; mostra grupos capturados por match e traz padrões prontos — e-mail, URL, IPv4, cor hex, CPF, telefone BR — pra inserir rápido; flags g/i/m/s/u/y selecionáveis)
- Ferramenta: Base64 Encode/Decode — `/tools/base64-tool` (codifica/decodifica texto com truque unicode-safe via `encodeURIComponent`, e também arquivos: lê um arquivo local via `FileReader`/`readAsDataURL` e mostra o Base64, ou decodifica um Base64 colado de volta em arquivo baixável via `Blob`/`URL.createObjectURL`; tudo client-side)
- Ferramenta: Gerador de UUID — `/tools/uuid-generator` (gera N UUIDs v4 via `crypto.randomUUID()`, com opções de maiúsculas/sem hífen e formato de saída — lista, array JS/JSON, separado por vírgula —, copiar tudo ou um por um)
- Ferramenta: Conversor de Case — `/tools/case-converter` (converte um texto entre camelCase, PascalCase, snake_case, kebab-case, CONSTANT_CASE, Title Case, dot.case e path/case, detectando limites de palavra tanto por separador quanto por transição minúscula→maiúscula)
- Ajuste: adiciona `src/newItems.js` (`NEW_ITEM_KEYS`) como fonte única de quais rotas foram alteradas na rodada mais recente; `AppLayout.jsx` agora lê esse array e mostra uma `Tag` "Novo"/"New" ao lado do item de menu correspondente, substituída por completo a cada rodada

## 2026-07-24 (pontapé inicial de categorias)
Rodada especial: cobre as 13 categorias que estavam totalmente zeradas em `IDEAS.md` (incluindo Front-end, que também não tinha nenhum `[x]`), pegando um item de cada uma. Categorias já com itens (Código, Estilos, Snippets) foram puladas de propósito. Nenhuma categoria precisou ser abandonada — todos os itens escolhidos são 100% client-side.
- Front-end: Checador de Contraste de Cores (WCAG) — `/frontend/contrast-checker` (calcula razão de contraste via luminância relativa entre duas cores e mostra se passa AA/AAA pra texto normal/grande, fórmula da própria spec WCAG)
- APIs: Gerador de comando cURL — `/apis/curl-generator` (monta um comando `curl` a partir de método, URL, headers e corpo digitados num formulário; nunca dispara uma requisição de verdade, só gera o texto)
- DevOps: Gerador de .gitignore — `/devops/gitignore-generator` (combina templates embutidos — Node, Python, Java, React, macOS, Windows, VSCode, IntelliJ, Docker, Terraform — removendo linhas duplicadas)
- Banco de Dados: Calculadora de Rate Limit — `/database/rate-limit-calculator` (converte um limite de requisições por janela em taxa por segundo/minuto/hora/dia, intervalo mínimo entre requisições e burst máximo num esquema fixed window)
- Cloud: AWS ARN Parser — `/cloud/arn-parser` (decompõe um ARN colado em partition/service/region/account/resource type/resource id)
- Segurança: Checador de Força de Senha — `/security/password-strength` (estima entropia em bits a partir do alfabeto usado e um tempo aproximado de quebra por força bruta; senha nunca sai do navegador)
- Arquivos & Dados: Visualizador de Árvore JSON — `/data/json-tree-viewer` (renderiza um JSON colado como árvore colapsável via componente `Tree` do Ant Design)
- Rede: Calculadora de Sub-rede IP (CIDR) — `/network/subnet-calculator` (calcula máscara, wildcard, endereço de rede, broadcast, range de hosts utilizáveis e total de endereços a partir de operações de bits)
- IA: Calculadora de Custo Anthropic — `/ai/anthropic-cost-calculator` (estima custo de uma requisição por modelo Claude e quantidade de tokens de entrada/saída, com suporte a escrita/leitura de prompt cache; preços fixos embutidos na página, podem ficar desatualizados)
- Mobile: Testador de Deep Link — `/mobile/deep-link-tester` (parseia uma URL/deep link — esquema customizado ou universal link — em scheme/host/path/query params via `URL` do navegador, com botão pra tentar abrir)
- Texto: Contador de Palavras/Caracteres/Linhas — `/text/word-counter` (conta palavras, caracteres com/sem espaço, linhas, frases aproximadas e tempo de leitura estimado enquanto digita)
- Referências: Comandos Git Essenciais — `/references/git-commands` (cheat sheet pesquisável dos comandos git mais usados, com descrição PT/EN de cada um)
- Fora da caixa: Quantos dias até... — `/extras/days-until` (contagem regressiva ao vivo até uma data escolhida, padrão próxima sexta-feira às 18h)
- Ajuste: `AppLayout.jsx` ganhou 13 grupos de menu novos (um por categoria), `routes.jsx` ganhou as 13 rotas correspondentes, `package.json` passou a declarar `dayjs` explicitamente (já vinha como dependência transitiva do Ant Design, usado direto na página de contagem regressiva)

## 2026-07-24 (rodada 2)
- Ferramenta: Conversor de Timestamp — `/tools/timestamp-converter` (converte timestamp Unix em segundos ou milissegundos, com detecção automática da unidade, para data local e UTC ISO 8601; e o caminho inverso, de um `DatePicker` para timestamp em segundos/ms, com botão "usar agora" e copiar; tudo client-side via `Date`)
- Estilo: Botão com Borda Gradiente Animada — `/styles/gradient-border-button` (contorno em `conic-gradient` girando continuamente via `@keyframes` e variável CSS `--gradient-angle`, usando pseudo-elementos `::before`/`::after`; sem biblioteca de animação; código-fonte exibido na página)
- Snippet: `useMediaQuery` — `/snippets/use-media-query` (hook em `src/hooks/useMediaQuery.js` que retorna se uma media query CSS está ativa e atualiza sozinho via `window.matchMedia`/evento `change`; página mostra o código e uma demo ao vivo com breakpoint mobile/desktop e preferência de tema do sistema)
- Ajuste: `HomePage.jsx` ganhou uma esteira animada em SVG explicando que o devtools cresce sozinho todas as noites (sem rota nova, só redesign do texto/visual da Home, PT/EN mantido)

## 2026-07-24 (retrofit)
- Todas as páginas existentes em `src/pages/` (exceto `NotFoundPage.jsx`) ganharam suporte PT/EN, seguindo o padrão local `translations = { pt, en }` + `useLanguage()` já usado em `HomePage.jsx`.

## 2026-07-24
- Ferramenta: Formatador e Validador de JSON — `/tools/json-formatter` (formata com indentação, minifica e valida sintaxe via `JSON.parse`/`JSON.stringify`, mostra contagem de chaves e tamanho em bytes, com botão de copiar; tudo client-side)
- Ferramenta: Conversor de Cor — `/tools/color-converter` (converte HEX ↔ RGB ↔ HSL em tempo real a partir de um seletor de cor nativo ou input de texto, com swatch de preview e botão de copiar por formato)
- Estilo: Skeleton Shimmer — `/styles/skeleton-shimmer` (placeholder de carregamento com brilho animado via gradiente CSS e `@keyframes`, alternativa mais viva ao `Skeleton` padrão do Ant Design; demo com toggle carregando/carregado e código-fonte exibido na página)
- Snippet: `useClickOutside` — `/snippets/use-click-outside` (hook em `src/hooks/useClickOutside.js` que dispara um callback ao clicar fora de um elemento via `ref`, útil pra fechar dropdowns/menus customizados; página mostra o código e uma demo com um menu que fecha ao clicar fora)
- Ferramenta: Explicador de Expressão Cron — `/tools/cron-parser` (parser próprio de expressões cron de 5 campos com suporte a `*`, `,`, `-`, `/`, nomes de mês/dia e atalhos `@daily`/`@hourly`/etc.; gera descrição em português e lista as próximas 5 execuções, respeitando a regra OR entre dia-do-mês e dia-da-semana quando ambos são restritos)
- Ferramenta: Gerador de Hash — `/tools/hash-generator` (calcula SHA-1/256/384/512 de um texto via `crypto.subtle`, com botão de copiar por hash; tudo local, nenhum dado sai do navegador)
- Estilo: Botão de Copiar Animado — `/styles/copy-button` (componente `CopyButton` reutilizável com micro-interação de ícone copiar → check via transição CSS de escala/rotação; código-fonte exibido na página)
- Snippet: `useLocalStorage` — `/snippets/use-local-storage` (hook em `src/hooks/useLocalStorage.js` que sincroniza estado com `localStorage`; página mostra o código e uma demo com input e contador que persistem entre reloads)
- Ferramenta: Decodificador de JWT — `/tools/jwt-decoder` (decodifica header/payload de um JWT colado, mostra status de expiração via `exp`; tudo client-side, sem verificar assinatura)
- Estilo: Glass Card (glassmorphism) — `/styles/glass-card` (cartão translúcido com blur, borda sutil e sombra, mostrado sobre fundo gradiente; código-fonte exibido na própria página)
- Snippet: `useDebounce` — `/snippets/use-debounce` (hook em `src/hooks/useDebounce.js` que atrasa atualização de um valor até parar de mudar; página mostra o código, explicação e uma demo ao vivo com input)
