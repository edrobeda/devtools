// Array mutado em runtime por src/hooks/useNewItemKeys.js a partir de
// GET /api/items (últimos 24 itens por created_at) — não é mais editado
// manualmente pelo agente nem substituído por rodada. Ver esse hook.
export const NEW_ITEM_KEYS = []
